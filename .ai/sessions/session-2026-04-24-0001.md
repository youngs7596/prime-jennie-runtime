# Session Handoff — 2026-04-24-0001

## 프로젝트 개요

**prime-jennie-runtime** — v3 실행 엔진. 2026-04-24 (금) 아침 세션.
DeepSeek 전환 실측 검증 → 잔여 Anthropic 호출 경로 발굴 (WSJ gmail + briefing)
→ 해당 경로도 DeepSeek 로 전환 → 동시에 briefing "데이터 불러오지 못함" 문구
원인 파악 + 실데이터 로딩 개선 → daily_asset_snapshot / daily_macro_insights
수집 중단 조사 + retry 로직 + v2 잔재 읽기 경로 정리. 5 commit push, 전부
MS-01 auto-deploy 성공. trading_flags:stop=1 유지 (실매매 없음).

## 최근 작업 내역

### 1) DeepSeek 전환 실측 검증 (직전 세션 2026-04-22 후속)

- `macro_runs` / `scout_runs` 4-23 08:30 첫 cron 부터 `model_used=deepseek-chat`
  로 전환 완료, `metadata_json->'shadow'` 키 부재 확인
- Redis llm:stats 기준 4-22 vs 4-23 비교: Scout+Macro Opus 비용 $3.19/일 → $0.033/일
  (99% 감소)
- 사용자 Anthropic 콘솔에서 "$2/일만 감소" 관찰 → 갭 원인 조사 착수

### 2) 잔여 Anthropic 호출 경로 발굴

`docker logs job-worker | grep api.anthropic.com` 으로 일일 패턴 추적:

| 시각 (KST) | Job | Opus 호출 수 | DB 집계 여부 |
|---|---|---:|---|
| 07:20 | `wsj_gmail_ingest` | **4회** (뉴스레터별) | ❌ 미집계 |
| 17:00 | `daily_briefing_report` | 1회 | ❌ 미집계 |

`jobs/app.py:194-221` 의 `briefing_llm_caller` (Opus 4.7) 가 briefing 과 WSJ
**둘 다**에 주입되고 있었음. 이전 세션에서 "briefing 1일 1회라 비용 미미" 라고
단정했던 부분이 WSJ 4회를 빼먹은 과소평가.

### 3) WSJ + briefing DeepSeek 전환 (커밋 `08201d0`)

- `briefing/reporter.py`: `build_claude_llm_caller` → `build_chat_llm_caller`
  리네임. 이미 model-agnostic (langchain `Any` ChatModel) 이었는데 이름만 Claude
  였음
- `jobs/app.py::_build_briefing_llm_caller`: `ChatOpenAI(base_url=DeepSeek,
  temperature=0.3)` 로 교체. DEEPSEEK_API_KEY 없으면 None → briefing fallback
  HTML, WSJ 요약 skip
- `dashboard/routers/llm_stats.py`: briefing provider 를 Anthropic → DeepSeek 로
  갱신
- `docker-compose.yml`: job-worker 에서 `ANTHROPIC_API_KEY` / `ANTHROPIC_MODEL`
  env 제거 (slow-loop 는 shadow 재활성화 대비 유지)

### 4) briefing "데이터 불러오지 못함" 원인 + 수정 (커밋 `8255bd0`, `b630b0f`, `7623d7a`)

**원인**: `context_builder._collect_macro` 가 kospi/vix/usd-krw/sectors/consensus
등을 **전부 하드코딩 None 으로 반환**. `assets` 도 `None` 하드코딩. 동시에
prompts.py 에 "데이터가 없으면 솔직히 '데이터를 불러오지 못했어요' 라고 적어주세요"
라는 지시가 있어 LLM 이 충실히 그 문구를 섹션마다 박았음.

**수정**:
- `_fetch_latest_indices`: `index_daily_prices` 에서 KOSPI/KOSDAQ 최신 종가 + 전일
  대비 % 계산 (7일 lookback)
- `_collect_assets`: `daily_asset_snapshots` 최신 — **3일 신선도 컷** (장마감~월요일
  T-3 커버). 오래되면 None 으로 섹션 생략 유도
- `_fetch_fresh_insight`: 처음엔 `daily_macro_insights` 3일 컷으로 포팅했다가
  v3 write 경로가 없음을 확인하고 **통째로 제거** (C-2)
- `prompts.py`: "데이터 없으면 '불러오지 못했어요'" → "데이터 없는 섹션은 아예
  생략. 그런 문구 절대 사용 금지"
- 시장 현황 지시에서 "VIX, 환율" 삭제

### 5) daily_asset_snapshot / daily_macro_insights 수집 중단 조사 (커밋 `e9e2c7d`)

**daily_asset_snapshot** 상태:
| 일자 | 결과 | 에러 |
|---|---|---|
| 4-20 ~ 4-23 (4일) | failed | `ConnectError: All connection attempts failed` |
| 4-17 이전 | success | - |

원인 추정: 장 마감 직후 15:45 에 kis-gateway 일시 장애 (재시작/auth refresh/
네트워크 jitter). 다른 시간대엔 정상 응답. 컨테이너 로그 rotate 로 확정 불가.

조치:
- **A. 수동 트리거**: `docker exec job-worker python -c "from ...asset_snapshot
  import daily_asset_snapshot; await daily_asset_snapshot(pool,http,gw)"` → 4-24
  snapshot 즉시 생성 (총자산 2억 140만원, 미실현 -1,055만, 3포지션)
- **B. retry 로직**: `_fetch_balance_with_retry` 추가. 5s/15s 백오프 3회 시도.
  ConnectError/ConnectTimeout/ReadTimeout/RemoteProtocolError 만 재시도, 4xx/5xx
  는 즉시 실패. 테스트 5건 (retry 성공/최종실패 케이스 2건 추가)

**daily_macro_insights** 상태:
- v3 에 write job **아예 없음**. `dashboard/routers/macro.py:5` 주석: "v2
  daily_macro_insights → v3 macro_runs (migrations/001)"
- 4-17 까지 데이터는 v2 migration 시 포팅된 **잔존**
- `global_macro_snapshots` 가 v3 공식 VIX/USD/국채 저장소로 설계됐지만 0 row
  (수집 job 미포팅)
- → **C-2 결정**: briefing 에서 읽기 제거, 관련 필드(vix_*/usd_krw/sectors_to_*
  /council_consensus/key_themes) 통째로 context 에서 삭제. 복원 필요 시
  global_macro_snapshots 수집 포팅 + 포맷터/프롬프트 재추가 (3파일 50줄 내외)

## 현재 상태 (2026-04-24 09:07 KST)

### 컨테이너
17/17 정상 (MS-01). 배포 5회 모두 success. job-worker/slow-loop/dashboard
재기동 완료.

### LLM 라우팅
- Scout primary = DeepSeek chat. Shadow OFF
- Macro primary = DeepSeek chat. Shadow OFF
- News extractor = vLLM Qwen3-30B-A3B
- **Briefing = DeepSeek chat** (신규 전환)
- **WSJ gmail summary = DeepSeek chat** (신규 전환)
- → **Opus 실사용처 = 0**

### briefing macro dict 새 스키마
남은 키: `sentiment`, `sentiment_score`, `regime_hint`, `kospi_index`,
`kospi_change_pct`, `kosdaq_index`, `kosdaq_change_pct`, `risk_factors`,
`trading_reasoning`, `next_review_hint`.

제거된 키: `vix_value`, `vix_regime`, `usd_krw`, `sectors_to_favor`,
`sectors_to_avoid`, `council_consensus`, `key_themes`.

### DB 상태
- `macro_runs` / `scout_runs` — 4-23 까지 deepseek-chat, 4-24 08:30 첫 cron 대기
- `index_daily_prices` — KOSPI/KOSDAQ 4-23 까지 (288 rows each)
- `daily_asset_snapshots` — **4-24 복원 완료** (수동 트리거). 오늘 15:45 retry
  적용 후 자동 재기동 검증 예정
- `daily_macro_insights` — 4-17 이후 미갱신 (v3 write 없음, briefing 에서도
  읽지 않음)
- `global_macro_snapshots` — 0 row (수집 포팅 대기)

### 비용 추정
| 시점 | Scout+Macro | Briefing+WSJ | 월 총 |
|---|---:|---:|---:|
| 4-22 이전 (Opus primary + shadow) | ~$71 | ~$30-50 | ~$100+ |
| 4-23 (Scout/Macro DeepSeek 만) | ~$1 | ~$30-50 | ~$35-50 |
| **4-24 이후 (전부 DeepSeek)** | ~$1 | ~$1-2 | **~$2-3** |

→ **약 98% 절감** (최종). 다음 Anthropic 콘솔 확인에서 일 청구 0 근접 예상.

## 이번 세션 커밋

| 해시 | 요약 |
|---|---|
| `08201d0` | refactor(briefing): Opus → DeepSeek 전환 (WSJ 요약 + daily briefing) |
| `8255bd0` | fix(briefing): 실데이터 누락 해소 — KOSPI/KOSDAQ + 자산 snapshot 실 테이블 읽기 |
| `b630b0f` | fix(briefing): 자산 snapshot 신선도 컷 7일 → 3일 축소 |
| `e9e2c7d` | fix(jobs): daily_asset_snapshot 에 kis-gateway retry(3회 backoff) 추가 |
| `7623d7a` | refactor(briefing): VIX/USD-KRW/sectors/consensus/themes 필드 제거 |
| `<이 핸드오프>` | docs(handoff): session-2026-04-24-0001 |

## 다음 단계

### 즉시 검증 (오늘 ~ 주말)
- [ ] **15:45 KST daily_asset_snapshot** — retry 적용 후 success 확인.
  4-25 월요일 cron 도 관찰 (구조적 원인 해소 여부)
- [ ] **17:00 KST daily_briefing_report** — 텔레그램 수신 확인 포인트:
  1) "데이터를 불러오지 못했어요" 문구 완전 부재
  2) KOSPI/KOSDAQ 실값 + 전일 대비 % 표기
  3) 자산 섹션 복원 (2억 140만, 3포지션)
  4) 포트폴리오 현황 / 매매 / 워치리스트 / 뉴스 존재
  5) VIX/환율 섹션 아예 부재 (C-2 대로)
- [ ] **4-25 07:20 KST wsj_gmail_ingest** — DeepSeek 호출 확인 (httpx 로그에
  `api.deepseek.com/v1/chat/completions` 만 보이고 `api.anthropic.com` 부재)
- [ ] **Anthropic 콘솔** — 일 청구가 실제로 0 에 수렴하는지 (credit balance
  변동 속도로 판단). 잔여 $0.4-1/일 선이면 정상

### 중기 (결정 필요)
- [ ] `global_macro_snapshots` 수집 job 포팅 — VIX/USD-KRW/국채/sectors 를
  briefing 에 다시 넣고 싶을 때. v2 원본 참조 가능
- [ ] daily_macro_insights 테이블 drop 여부 (v3 에서 완전히 안 쓰게 됐으므로)
- [ ] 현재 미실현 -1,056만원 손실 중인 포지션 (현대차/고려아연/267250) 의
  익절/손절 전략 — 실매매 재개 전 briefing 에서 주목

### 직전 세션 백로그 (그대로 이월)
- [ ] event_type 분포 재측정 — 4-21 재분류 후 16종 enum 분포 변화 확인
- [ ] eval baseline 실행 — `scripts/news_event_eval.py`
- [ ] eval fixture 30~50건 확장 (실 news_events 샘플링)
- [ ] 남은 `other` 2,333건 (15.6%) 2차 재분류 — 장 대기 시간

### 옵션
- [ ] shadow 코드 dead path 청소 (env=0 이라 호출 안 되지만 노이즈)
- [ ] minyoung-mah 0.1.2 PyPI publish
- [ ] GDDR6X junction temp (`sudo nvidia-smi -pl 220`)
- [ ] `/docker_data/qdrant_data` 볼륨 삭제

## 중요 참고사항

### briefing context dict 새 스키마 (C-2 이후)
```python
{
  "date": "YYYY-MM-DD",
  "positions": [...],
  "trades": [...],
  "trade_summary": {buy_count, sell_count, ...},
  "macro": {
    "sentiment", "sentiment_score", "regime_hint",
    "kospi_index", "kospi_change_pct",
    "kosdaq_index", "kosdaq_change_pct",
    "risk_factors", "trading_reasoning", "next_review_hint"
  } | None,
  "watchlist": [...],
  "assets": {total_asset, cash_balance, stock_eval, position_count} | None,
  "news": [...],
}
```

### 신선도 컷 정책
- `daily_asset_snapshots`: 3일 (T-3 월요일 주말 gap 커버)
- `index_daily_prices`: 7일 lookback (전일대비 계산용, 최신만 선택)
- VIX/FX/sectors: **제거됨** (C-2)

### daily_asset_snapshot retry 로직
- `_fetch_balance_with_retry`: 5s → 15s 백오프, 총 3회 시도
- 재시도 대상: `ConnectError` / `ConnectTimeout` / `ReadTimeout` /
  `RemoteProtocolError`
- 즉시 실패: `HTTPStatusError` (4xx/5xx 는 로직 오류 가능성)
- 수동 트리거 템플릿 (컨테이너 내):
  ```python
  import asyncio, asyncpg, httpx
  from prime_jennie_runtime.jobs.asset_snapshot import daily_asset_snapshot

  async def main():
      pool = await asyncpg.create_pool(
          "postgresql://pj_admin:dev_admin@postgres:5432/prime_jennie_v3"
      )
      async with httpx.AsyncClient() as http:
          await daily_asset_snapshot(pool, http, "http://kis-gateway:8080")
      await pool.close()
  asyncio.run(main())
  ```

### Anthropic 비용 갭 해석 (사용자 질문 해소)
- 사용자가 본 "$2/일만 감소" = DB 집계 $3.19 + 미집계 WSJ(~$0.5-1) + 미집계
  briefing(~$0.3-1) 합 vs 전환 후 DeepSeek 전체로 내려간 차이
- DB 는 Scout/Macro 의 input/output 토큰만 추적 (`_PRICING` 고정가). WSJ/briefing
  은 `build_chat_llm_caller` 가 `usage_metadata` 기록 로직 부재 (기존 TODO 가
  `briefing/reporter.py` 92-95 에 있음) → 이번 전환으로 이슈 무의미해짐
  (DeepSeek 비용은 $0.1/M 단위라 집계 없어도 실무 영향 미미)

### C-2 선택 근거 요약
- `daily_macro_insights` 는 v2 잔존, v3 write 경로 없음 → dead data
- 3일 컷으로 자동 None 이 되긴 하지만 코드 path 가 남아있으면 "읽는 거 같은데
  왜 None?" 오해 소지
- 복원 시 `global_macro_snapshots` 수집 job 포팅 + 3파일 재작업 — 한꺼번에
  하는 게 맞음

## 참조

- 이전 세션: `.ai/sessions/session-2026-04-22-0001.md`
- 글로벌 메모리: `~/.claude/global-memory-youngs7596/prime-jennie-family.md`
  (LLM tier 최종판 4-24 반영 예정)
- 로컬 auto-memory: `project_llm_tier_assignment.md` (briefing/WSJ 도 DeepSeek
  반영 필요)
- 배포 파이프라인: push to main → `ms-01-v3` runner → `docker compose --profile
  full up -d --build`. 이번 세션 5 deploy 각 ~2-3분
- 변경 파일:
  - `prime_jennie_runtime/briefing/context_builder.py` (`_collect_macro`
    단순화, `_fetch_latest_indices` 신설, `_collect_assets` 신설)
  - `prime_jennie_runtime/briefing/formatters.py` (VIX/USD/sectors 블록 제거)
  - `prime_jennie_runtime/briefing/prompts.py` (섹션 생략 지시 + VIX/환율 삭제)
  - `prime_jennie_runtime/briefing/reporter.py` (`build_chat_llm_caller` 리네임)
  - `prime_jennie_runtime/briefing/__init__.py` (export 갱신)
  - `prime_jennie_runtime/jobs/app.py` (DeepSeek LLM caller)
  - `prime_jennie_runtime/jobs/asset_snapshot.py` (retry 래퍼)
  - `prime_jennie_runtime/dashboard/routers/llm_stats.py` (briefing = DeepSeek)
  - `docker-compose.yml` (job-worker ANTHROPIC_* 제거)
  - `tests/briefing/test_context_builder.py` (+4 테스트)
  - `tests/briefing/test_formatters.py` (sample macro dict 단순화)
  - `tests/jobs/test_daily_asset_snapshot.py` (+2 retry 테스트)

---
**세션 종료**. Opus 사용처 0, briefing 에 실데이터 복원, daily_asset_snapshot
retry 내성 확보. 오늘 17:00 첫 DeepSeek 브리핑 + 15:45 snapshot retry 실측이
다음 검증 포인트.
