# Session Handoff — 2026-04-25-0006

## 프로젝트 개요

**prime-jennie-runtime** — v3 실행 엔진. 2026-04-25 (토) 여섯 번째 세션.

`session-2026-04-25-0005` (이월 잔여 단기 4건 + main 머지 + 04-25 deploy 검증)
종료 직후 같은 토요일 늦은 밤 **control-ui 풀 점검 세션**. 사용자가 `192.168.31.195`
(MS-01 control-ui) 의 모든 페이지를 돌면서 코드 상태와 어긋난 부분 점검 요청 →
4건 발견 → 3건 코드 fix + 1건 운영 검증 → main 머지 → 라이브 재점검까지 한 세션 안에서.

## 최근 작업 내역

### 1) control-ui 10 페이지 풀 점검 (Chrome MCP 경유)

`Browser 1` (Windows Chrome) 에 select_browser → http://192.168.31.195/ 진입.
페이지 10개 (Overview / Portfolio / Trades / Macro / Scout / News / LLM Stats /
Logs / Jobs / System) 를 차례로 방문하면서 backend dashboard router 코드와
대조.

**발견 정리**:

| # | 페이지 | 증상 | 분류 |
|---|---|---|---|
| A | Portfolio · Asset History | 차트 항상 빈 상태 (7d/14d/30d/60d/90d 모두) | 코드 mismatch |
| B | Logs · 사이드바 | 8 서비스만 노출 — job-worker / cloudflared / control-ui 누락 | 코드 mismatch |
| C | Overview / System | "9/11 services healthy" 표시, 실제는 11/11 정상 | 코드 mismatch (라벨 통일) |
| D | Jobs · daily_asset_snapshot | 04-20~04-24 5일 연속 ConnectError (failed) | 운영 이슈 |
| ⓘ | Macro Run Detail | USD/KRW 1350 placeholder | 0005 에서 BOK 키 추가 → 04-27 자연 해소 |
| ⓘ | News 24h | 시장동향 high 비율 90% / 지정학 100% | 0005 의 impact 프롬프트 튜닝 → 04-27 자연 개선 |
| ⓘ | Scout · attempts 미노출 | control-ui 측 별도 작업 | 별도 리포 작업 |
| ⓘ | Scout candidates=12 | 04-24 LLM 예측치 잔존 | 0005 fix 가 04-27 부터 적용 |

A/B/C 는 본 세션에서 코드 fix. D 는 본 세션에서 운영 검증 (deploy 후 직접
호출 성공). ⓘ 는 0005 에서 이미 처리한 항목 또는 별도 리포 작업.

### 2) Backend fix 3건 (commit `6caae68`)

#### A) `portfolio.py /history` — daily_asset_snapshots 실 데이터 노출

- **기존**: stale 코멘트 (`v3 별도 스냅샷 테이블 미구현`) + `return []` 하드코딩
- **실제**: migration 009 의 `daily_asset_snapshots` 가 매일 채워지고 있음
  (점검 시점 68 rows, 03-26~04-24)
- **수정**: SELECT FROM daily_asset_snapshots WHERE snapshot_date >= since,
  시간 오름차순 반환. SQLite (test) 와 asyncpg (prod) 의 date 표현 차이를
  `_to_snapshot_datetime(value)` helper 로 통일 (datetime / date / str 모두
  처리)
- **파일 docstring** 도 갱신

#### B) `logs.py _SERVICES` — 8 → 11

- **누락**: job-worker (가장 큰 갭 — scheduled jobs 실행 주체), cloudflared,
  control-ui
- **이유**: System 페이지 `_DEFAULT_TARGETS` + `_DEFAULT_DAEMONS` 와 Logs 의
  `_SERVICES` 가 별도로 관리돼 있어 동기 누락
- **영향**: 운영 중 가장 모니터링하고 싶은 job-worker 의 로그를 사이드바에서
  못 봤음
- **수정**: 3개 추가

#### C) `system.py _check` — status 정규화

- **기존**: 각 서비스의 `/health` body 의 `status` 필드를 그대로 사용
- **현상**: kis-gateway / telegram-bot 이 `{"status":"ok"}` 반환 → "ok" 그대로
  노출 → control-ui 카운트가 `status === "healthy"` 만 세서 "9/11 healthy"
  표시 (실제 11/11 정상)
- **수정**: `_OK_LIKE_STATUSES = ("ok", "healthy", "alive", "up")` 화이트리스트
  로 정규화. UI 측 카운트 로직 손대지 않고 backend 만 변경 (control-ui 는
  별도 리포라 이번 PR 에 미포함)

### 3) Test/conftest 업데이트

- `tests/dashboard/conftest.py`: `daily_asset_snapshots` SQLite 스키마 추가
  (BIGINT / DATE PK / position_count / total_profit_loss / realized_profit_loss)
- `tests/dashboard/routers/test_portfolio.py` +2:
  - `test_history_from_daily_asset_snapshots` — 3행 inserted 후 `/history?days=30`
    가 시간순 3건 반환
  - `test_history_empty_when_no_rows` — 빈 테이블이면 `[]`
- `tests/dashboard/routers/test_system.py` +1:
  - `test_health_normalizes_ok_to_healthy` — `{"status":"ok"}` 와 `{"status":"alive"}`
    응답이 모두 `status="healthy"` 로 매핑

`tests/dashboard` 39 passed (이전 36). 전체 907 passed (이전 904 + 3).

### 4) 운영 검증 — daily_asset_snapshot ConnectError 회복

**진단**: `asset_snapshot.py` 코드는 이미 stale-keepalive 회피 + 3회 retry
적용. 5일 연속 fail 의 원인은 외부 (KIS Gateway 응답 타이밍 / docker network
일시 격리) 로 추정.

**즉시 확인** (deploy 직후, 21:35 KST):
- `docker compose exec job-worker python` 으로 `httpx.AsyncClient().get(KIS_GATEWAY_URL/api/balance)`
  → status 200, balance 정상 응답
- `daily_asset_snapshot()` 함수 직접 호출 → "OK" + DB 에 04-25 row 추가
  (total_asset 196,853,498 / unrealized -1,510만)

**결론**: 코드 변경 없음. cron schedule 자체는 변동 없음 — 04-27 월 15:45 KST
정상 회복 예상.

### 5) 배포 + 라이브 재점검

- worktree 단일 commit `6caae68`
- main 머지 `d199c33` (no-ff) + push
- ghcr.yml: success (1m23s) → deploy.yml: success (47s)
- MS-01 컨테이너 9개 자동 재시작, HEAD `d199c33`

**라이브 검증** (Chrome MCP):
- Portfolio · Asset History → 03-26~04-25 차트 정상 (1.6억~2.2억)
- Logs 사이드바 → 11개 모두 노출, job-worker 클릭 시 실 로그 흐름 (`scheduler started: owner=job_worker jobs=24`)
- Overview System Health → "healthy 11/11"
- System 페이지 → "11/11 services healthy", API 응답 11개 모두 `status="healthy"`

**부수 확인**: job-worker scheduler `jobs=24` — 0005 의 analytics 잡 2개 제거가
운영에 반영된 정확한 숫자.

## 현재 상태 (2026-04-25 토 22시대, 본 세션 종료 시점)

### 코드
- runtime HEAD (이 commit 직후): `<handoff commit>` on `claude/unruffled-sutherland-f6518f`
- main HEAD: `d199c33` (이번 세션 fix) + 본 핸드오프 commit + no-ff merge
- 907 → **907 tests** (handoff 자체는 테스트 변동 없음)
- ruff format + check (변경 파일) 양쪽 모두 clean

### 운영 (MS-01)
- HEAD `d199c33`, 9개 컨테이너 재시작 완료, BOK_ECOS_API_KEY 주입 유지
- 04-25 daily_asset_snapshot row 직접 호출로 추가됨 (운영 가시성 회복)
- 04-27 월 첫 cron 부터 자연 동작 검증 가능:
  - 07:40 macro_collect_global → USD/KRW 실 값
  - 08:30 scout_daily → 새 `candidates_count = len(raw_candidates)`
  - 15:45 daily_asset_snapshot → cron 으로도 정상
  - news_pipeline 다음 추출부터 새 impact 프롬프트 효과

### 글로벌 메모리
본 세션 변경 없음.

## 다음 단계 (TODO)

### 즉시 (다음 평일 첫 가동, 2026-04-27 월)

0005 의 즉시 항목 + 본 세션 fix 의 누적 검증:

- [ ] 07:40 KST `macro_collect_global` 후 Redis `macro:data:snapshot:2026-04-27`
      의 `usd_krw` 채워짐 확인. Macro Run Detail 의 USD/KRW 가 1350 placeholder
      가 아닌 실 환율 값으로 표시되는지
- [ ] 08:00 KST briefing macro 섹션에 USD/KRW 라인 복원 확인
- [ ] 08:30 KST `slow_loop.scout_daily` 후 scout_runs 의 `candidates_count` 가
      실제 raw_candidates 길이로 들쭉날쭉 채워지는지
- [ ] 08:30~14:30 KST 매시 30분 7회 scout 진행 중 escalate 발생 시 Telegram
      풀 메시지 도착 확인 (scout_run_id + code_hash 시퀀스 포함)
- [ ] 15:45 KST `daily_asset_snapshot` cron 자동 실행 → success (이번 세션
      수동 호출 성공했으므로 cron 도 정상이어야 함). 만약 fail 재발 시 추가
      디버그 (KIS Gateway 응답 타이밍 / docker network)
- [ ] News 페이지 24h 통계의 high impact 비율이 점진 하락하는지 (이번 세션
      프롬프트 튜닝 효과)

### 짧은 후속 (0005 이월 + 새 항목)

- [ ] **새 32-fixture impact eval 재측정** — 60% → 80%+ 검증
      ```bash
      uv run python scripts/news_event_eval.py \
          --fixture tests/fixtures/news_eval/baseline.json \
          --model hosted_vllm/jart25/Qwen3-30B-A3B-Instruct-2507-Autoround-Int-4bit-gptq \
          --api-base http://127.0.0.1:8001/v1 \
          --json-out reports/eval-$(date +%Y%m%d-%H%M).json
      ```
- [ ] **control-ui Scout 페이지 attempts 노출** — 별도 리포
      (`prime-jennie-control-ui`)
- [ ] **scheduler 가 dashboard `/dags/{dag_id}/trigger` 의 'requested' 행을 픽업하도록 확장**
      — 현재 `airflow.py:152` 가 status='requested' 만 기록하고 실 실행은 cron
      도래 시. 본 세션에서 daily_asset_snapshot 을 직접 함수 호출로 검증해야
      했던 이유. 운영 편의를 위해 scheduler reload 시 `requested` 픽업 +
      handler_key 디스패치 + 결과 기록까지

### Phase 3 후속 (0001~0005 이월 그대로)

`stage1-prep` 5 외부 의존 + Stage 0 첫 실 evaluation (~05-05 후 candidates 누적).

## 이번 세션 커밋 요약

### prime-jennie-runtime

| 해시 | 요약 |
|---|---|
| `6caae68` | fix(dashboard): control-ui 점검에서 발견된 3건 mismatch 수정 |
| `d199c33` | merge: dashboard mismatch fixes — portfolio /history + logs services + system status (no-ff) |
| (handoff) | docs(handoff): session-2026-04-25-0006 |
| (handoff merge) | merge handoff to main |

### claude-global-memory

본 세션 변경 없음.

## 중요 참고사항

### control-ui 별도 리포 — 본 세션에서 안 건드린 항목

- Asset History UI 측 차트 컴포넌트 재검토 — backend 가 `realized_profit_loss`
  를 추가로 내려주지만 UI 가 활용하는지 확인. 만약 UI 차트가 `total_asset`,
  `cash_balance`, `stock_eval_amount` 만 쓴다면 그대로 호환. 안 그렇다면 별도
  PR.
- "9/11 healthy" 카운트 로직 자체 — 이번 세션에서 backend 정규화로 우회.
  control-ui 측 카운트 로직도 동시에 손볼 거면 별도 PR.

### scheduled-jobs 'requested' 픽업 미구현

`airflow.py:152` 의 `trigger_dag` 가 'requested' 만 기록하고 실제 실행은 안
함. 코드 안에 명시적으로 "v3 Phase 2.9 slice2 기준 runner 는 cron 외 수동
실행을 따로 지원하지 않아, 본 엔드포인트는 요청을 기록하고 향후
`scheduler.trigger:{owner}:{job}` 채널로 확장할 자리" 주석. 이번 세션에서
daily_asset_snapshot 검증을 위해 docker exec 으로 직접 함수 호출했음 — 향후
운영 편의를 위해 이 미구현을 채우는 게 좋음.

### Communication Style 4 원칙 (글로벌 메모리)

정직 / 돌려말 금지 / 코드 용어 금지 / 평문 위주.
`~/.claude/global-memory-youngs7596/communication-style.md`

## 참조

- 직전 세션: `.ai/sessions/session-2026-04-25-0005.md` (이월 잔여 단기 4건 +
  main 머지 + deploy 검증)
- 글로벌 메모리:
  - `~/.claude/global-memory-youngs7596/prime-jennie-family.md`
  - `~/.claude/global-memory-youngs7596/communication-style.md`
  - `~/.claude/global-memory-youngs7596/trading-domain.md`
  - `~/.claude/global-memory-youngs7596/minyoung-mah.md`
- 본 세션 신규/수정 파일:
  - `prime_jennie_runtime/dashboard/routers/portfolio.py` (`/history` SELECT FROM daily_asset_snapshots)
  - `prime_jennie_runtime/dashboard/routers/logs.py` (`_SERVICES` 8 → 11)
  - `prime_jennie_runtime/dashboard/routers/system.py` (`_check` status 정규화 + `_OK_LIKE_STATUSES`)
  - `tests/dashboard/conftest.py` (daily_asset_snapshots schema)
  - `tests/dashboard/routers/test_portfolio.py` (+2 history tests)
  - `tests/dashboard/routers/test_system.py` (+1 normalize test)

---

**세션 종료**. control-ui 점검 → 4건 발견 → 3건 코드 fix + 1건 운영 검증
→ main 머지 + deploy + 라이브 재점검까지 한 세션 안에서 마무리. 04-27 월 첫
cron 부터 0005 의 4건 + 0006 의 3건 누적 효과가 자연 검증될 예정.
