# Session Handoff - 2026-04-17-11:59

## 작업 요약 (What was done)

### 1. Phase 1 Track B 완료 — 느린 루프 (Slow Loop)
커밋 `5f54834`. v2 코드는 직접 포팅하지 않았고(Track B는 v3 신규 영역), minyoung-mah `SubAgentRole`의 structured fast path로 Scout/Macro 단일 LLM 호출, Strategy Engine은 결정론.

- **scout/**: ScoutOutput/ScreeningCandidate schemas, ScoutRole(`tool_allowlist=[]` + `max_iterations=1` + `output_schema`), system/user 프롬프트, code_hasher (S13 재현성), validators (S06/S07/S08), ScreeningToolAdapterStub (Track D 자리 표시자), context builder + 4종 feeder protocol/stub
- **macro/**: MacroGateOutput/MarketSnapshot schemas, MacroGateRole, prompts, discretize (MG15~MG22 half-open), closed_conditions (MG02 fx/vol/sector), continuity (MG07), post_processor (auto_override + 10종 `pj.macro.*` 이벤트), MacroStateStore (MG08 24h stale)
- **strategy/**: strategy_policy.yaml (4 tag × base_pct/max_notional_krw/default exit rules), policy loader, sheet_id generator, RiskThrottleSnapshot Protocol + NoOpRiskThrottle, StrategyEngine.build_sheet (결정론), PositionSheetPublisher (DLQ)
- **pipeline.py**: `SlowLoopComponents` + `run_slow_loop()` 래퍼. Macro→post_proc→state_store→Scout→screening stub→validate→engine→publish. 3회 retry + 조건부 분기는 local workaround (minyoung-mah PR 후보)
- **테스트**: 113 신규 — 9종 exit rule, MG01~MG09/MG15~MG22, S06/S07/S08/S13, POSITION_SHEET T01~T10, E2E 4 시나리오 (normal/closed/hallucination/no_candidates)

### 2. Phase 1 Track C 완료 — 빠른 루프 + v2 포팅 (Fast Loop + KIS Gateway + Telegram + Risk Throttle)
커밋 `7906785`. 병렬 에이전트 전략으로 진행 — 4개 독립 영역을 동시 실행, 메인은 교차 의존 부분 직접.

#### 병렬 실행 전략
- **Stage 0 (직접)**: config 확장(KISConfig/TelegramConfig), Stream 상수 (`STREAM_PRICES`, `STREAM_NOTIFICATIONS`, `STREAM_EXECUTIONS`), pyproject 의존성 (`websockets`, `pybreaker`, `slowapi`, `respx`, `freezegun`), fast_loop/domain.py (PositionState/ExitDecision/TickData/FillEvent), fast_loop/indicators.py (v2 monitor/indicators.py 그대로 포팅), fast_loop/schemas.py (알림 envelope)
- **Stage 1/2/3/6 (4 에이전트 병렬)**:
  - Agent A: KIS Gateway 전체 (`kis_gateway/` 9 파일, 45 tests) — v2 `services/gateway/` 포팅
  - Agent B1: Risk Throttle (`risk_throttle.py` + `risk_updater.py`, 38 tests) — v2 `services/jobs/app.py` L1196-1452 포팅
  - Agent B2: Exit Evaluator 9종 first_match (`exit_evaluator.py`, 36 tests) — v3 신규 (v2 `monitor/exit_rules.py`는 구조 불일치, 수식만 참고)
  - Agent C: Telegram Bot 단방향 발신 (`telegram_bot/` 6 파일, 15 tests) — v2 `services/telegram/` 포팅
- **Stage 4+5 (직접)**: kis_client (v2 `infra/kis/client.py` async 포팅), entry_executor (호가 단위 정렬 + limit 타임아웃 취소), exit_executor (scale_out 부분 청산 + breakeven SL 상향), position_tracker (Redis Hash `position_state:{sheet_id}` + 재시작 복구), consumer (`v3:position_sheets` → entry + subscribe), tick_loop (`kis:prices` → ticker별 active sheet 평가), notifier — 18 tests
- **Stage 7 (직접)**: E2E 통합 (sheet→entry→tick→exit→notification) + IntradayRiskThrottle이 RiskThrottleSnapshot Protocol 만족 + StrategyEngine 회귀 — 3 tests
- **Stage 8 (직접)**: `Dockerfile.kis_gateway/fast_loop/telegram_bot`, `docker-compose.yml`에 3 서비스 추가 (kis_token 볼륨 마운트, v3_kis_token.json으로 v2와 분리)

#### 포팅 준수 사항
- v2 검증 코드는 그대로 복사 후 `httpx.Client`→`httpx.AsyncClient`, `threading.Lock`→`asyncio.Lock`, `websocket-client`→`websockets`, `pybreaker`→자체 async 래퍼, `slowapi`→자체 `AsyncRateLimiter`만 치환
- Fast loop은 **시트 = 이미 승인된 주문**으로 취급. v2 buyer가 하던 portfolio guard/correlation/cooldown은 Slow loop가 이미 처리 → Fast loop는 실행 가능성(장 시간, emergency_stop, 시트 expiry)만 체크
- LLM import 금지 (`litellm`, `anthropic`, `openai`)

### 변경 파일 총계
- Track B 커밋 `5f54834`: 54 files, +4297 lines
- Track C 커밋 `7906785`: 60 files, +7972 lines

## 현재 상태 (Current State)

- **prime-jennie-runtime**: Phase 1 Track A/B/C 완료, **297 tests 통과, ruff clean**
- **git**: `main` branch, `origin/main` 대비 2 commits ahead (push 대기 중)
- **prime-jennie (v2)**: 변경 없음, `trading_flags:stop=1, pause=emergency_stop` 유지
- **minyoung-mah**: 변경 없음, 0.1.0, editable install (`pip install -e ../minyoung-mah`)
- **global-memory**: `~/.claude/global-memory-youngs7596/` (remote `youngs7596/claude-global-memory` 아직 미생성 또는 404)

## 테스트 분포 (297 total)

| 영역 | 카운트 | 주요 커버리지 |
|---|---|---|
| Track A 기반 | 24 | infra (config/db/redis/observer), position_sheet v1.1 |
| Track B slow_loop/scout | 17 | schemas, code_hasher, role fast path, validators (S06/S07/S08), context builder, screening stub |
| Track B slow_loop/macro | 37 | discretize (MG15~MG22), closed_conditions (MG02), continuity (MG07), state_store (MG08), post_processor, role, context builder |
| Track B slow_loop/strategy | 25 | policy YAML, sheet_id, engine (macro closed/MIN_PCT/dedupe/deprecated), publisher (DLQ) |
| Track B E2E pipeline | 4 | normal / macro_closed / hallucination / no_candidates |
| Track C kis_gateway | 45 | market_hours, token_manager, rate_limiter, circuit_breaker, kis_api (respx), streamer (FakeWS), poller, server (TestClient) |
| Track C fast_loop/risk_throttle | 38 | 5단계 전이 × 13, SOX × 7, 회복 지연, min() 역전 방지 |
| Track C fast_loop/exit_evaluator | 36 | 9종 × 18 + T15~T20 조합 6 + state 갱신 |
| Track C fast_loop 통합 | 18 | kis_client (respx), entry/exit executor, position_tracker, consumer, tick_loop |
| Track C fast_loop E2E | 3 | sheet→entry→tick→exit, IntradayRiskThrottle Protocol 회귀 |
| Track C telegram_bot | 15 | bot (respx), formatters (3종), consumer (fakeredis) |

## 다음 할 일 (Next Steps)

### Phase 1 잔여 (우선순위 순)
1. **Track D — Screening Executor** (가장 블로커)
   - `screening_executor/` Docker 격리 컨테이너 (`Dockerfile.screening` 존재, 내용 없음)
   - AST 기반 import 화이트리스트 (`pandas`, `numpy`, `scipy.stats`, `talib`, `sklearn.{cluster,linear_model,preprocessing,metrics}`, `math`, `statistics`, `datetime`)
   - `eval`/`exec`/`compile`/`__import__` 검출
   - seccomp + network_mode: none + read_only + cpus 2 + mem 4g
   - 악의 코드 테스트 M01~M12 (SCOUT_CODE_GENERATION §6.6)
   - 현재 stub: `prime_jennie_runtime/slow_loop/scout/screening_stub.py` — 시그니처 고정된 상태, Track D가 real adapter로 swap
2. **Track E — 데이터 + News Pipeline**
   - v2 `news_pipeline_kor/` 포팅 (MariaDB → Postgres)
   - Slow loop의 stub feeders (`StubNewsScoreFeeder`, `StubMarketSnapshotFeeder`, `StubMarketSummaryFeeder`, `StubSectorMomentumFeeder`, `StubWsjDigestFeeder`, `StubKorMacroNewsFeeder`, `StubUniverseFeeder`)를 real feeder로 교체
   - v2 `legacy_*` 테이블의 일봉/호가 마이그레이션

### Phase 2 (Track C 미완)
- Telegram 양방향 명령 (`/stop`, `/pause`, `/liquidate`, `/dryrun`, `/status`) — v2 `telegram/handler.py` 744줄 포팅
- Control UI `control.commands` pub/sub consumer
- KIS Gateway DB 폴백 (일봉 부재 시 Postgres `legacy_daily_prices` 조회) — Track E 완료 후
- 실계좌 shakedown + shadow 운영 스프린트
- Grafana 메트릭 exporter

### 운영 준비
- `docker compose --profile full up` smoke (현재 `docker compose config`만 parse 검증)
- KIS paper account 토큰 발급 + `/app/data/kis_token/v3_kis_token.json` 수동 부팅
- Telegram bot token 실계좌 설정 전까지 `TELEGRAM_DRY_RUN=true`

## Context for Next Session

세션 시작 시 먼저 읽을 것:
- `AGENTS.md` (루트) — Track 경계, Memory Sync
- `prime_jennie_runtime/AGENTS.md` — 패키지 지도
- 해당 Track의 서브폴더 `AGENTS.md` (`slow_loop/`, `fast_loop/`, `kis_gateway/`, `telegram_bot/`)
- `docs/prime_jennie_v3_phase0_design.md` — §8 Phase 1 로드맵
- `~/.claude/global-memory-youngs7596/prime-jennie-family.md` — 장기 기억
- 이 세션 파일 (`.ai/sessions/session-2026-04-17-11-59.md`)

Track D 착수 전 참고:
- `/home/youngs75/projects/prime-jennie/prime_jennie/services/scout/` — v2 Scout가 코드 생성이 아닌 자연어이라 재참고는 거의 없음
- `docs/SCOUT_CODE_GENERATION.md` §6 (샌드박스 컨테이너 구성), §11 (M01~M12)
- `prime_jennie_runtime/slow_loop/scout/screening_stub.py` — 교체할 시그니처

Track E 착수 전 참고:
- `/home/youngs75/projects/prime-jennie/prime_jennie/services/news/` — v2 뉴스 파이프라인
- `/home/youngs75/projects/prime-jennie/prime_jennie/infra/database/` — 스키마
- `migrations/001_v3_schema.sql` — v3 Postgres 테이블

## 핵심 결정사항 (Key Decisions)

### Track B
- **Scout 코드는 Track D로 위임, 현재 stub**: `ScreeningToolAdapterStub`이 고정 candidates 반환. Track D가 완성되면 real `ScreeningToolAdapter`로 swap, 나머지 코드 변경 없음 (시그니처 고정).
- **Strategy는 파이프라인 밖 결정론 후처리**: minyoung-mah `ExecuteToolsStep`에 Strategy를 태우면 LLM이 tool_call을 내야 해서 부자연스러움. `run_slow_loop()` 래퍼 함수에서 직접 호출.
- **minyoung-mah 수정 없음**: role-level retry 3회, 조건부 분기 (`if macro_closed: skip Scout`), 결정론 후처리 단계 모두 local workaround. 2회 이상 반복되면 harness PR 후보.
- **strategy_policy.yaml**: 4 tag (GAP_UP_REBOUND, SECTOR_MOMENTUM, EARNINGS_DRIFT, MEAN_REVERT_RSI)의 base_pct/max_notional_krw/default exit rules 수동 관리. RSI_REBOUND는 폐기됨.

### Track C
- **병렬 에이전트 전략**: 독립 디렉토리 4개를 동시 실행. 메인은 Stage 0 기반 먼저 깔고, Stage 4+5 교차 의존 부분 직접. 통합 + lint + 커밋은 메인이 전담.
- **v3 토큰 파일 경로 분리**: `v3_kis_token.json`으로 v2 `kis_token.json`과 분리. v2/v3 공존 시 KIS 토큰 발급 충돌 방지. Phase 1 초기는 paper account만.
- **Fast loop = 시트 실행만**: v2 buyer의 portfolio guard/correlation/cooldown은 Slow loop가 이미 시트 발행 시 처리. Fast loop는 장 시간/emergency_stop/expiry만 추가 확인.
- **IntradayRiskThrottle `current_multiplier()` Protocol**: Slow loop가 시트 발행 시 읽고, Fast loop가 exit 평가 시 읽음. 단일 메서드 노출, 내부 복잡성 은닉.
- **Position state Redis Hash 통합**: v2는 `watermark:*`, `scale_out:*`, `rsi_sold:*` 등 분산 키. v3는 `position_state:{sheet_id}` 하나로 묶어 재시작 복구 단순화.
- **9종 exit rule은 v3 신규**: v2 `monitor/exit_rules.py`는 전역 config 기반 12-rule이라 v3 시트별 9종과 구조 불일치. 수식/임계값(Breakeven +3%→+0.3%, Profit Floor +15%→+10%)만 베이스라인 유지.
- **DB 폴백 Phase 2로 연기**: v2 gateway는 KIS 실패 시 Postgres `legacy_daily_prices`로 폴백. Phase 1은 Track E 미완성이라 502만 반환.

## 주의사항 (Warnings)

### GitHub 인증
- 현재 `gh auth` active: **youngs75**. `prime-jennie-runtime` push 시 `gh auth switch -u youngs7596` 전환 필요.
- Push 절차: `gh auth switch -u youngs7596 && git push origin main`
- 복원: 세션 끝에 `gh auth switch -u youngs75` (minyoung-mah 작업 대비)

### Global Memory Repo
- `~/.claude/global-memory-youngs7596/`의 remote `youngs7596/claude-global-memory` 가 아직 생성 안 됐거나 private 접근 권한 문제. `git pull` 시 404.
- 로컬 파일은 정상 (`prime-jennie-family.md`, `trading-domain.md`, `minyoung-mah.md`).
- **이 세션에서 prime-jennie-family.md를 Track B/C 완료로 업데이트했지만 remote push는 실패할 가능성 있음**. 사용자가 repo 생성 여부 확인 필요.

### 운영
- v2 `trading_flags:stop=1, pause=emergency_stop` 유지 중 (이란 전쟁 리스크)
- v3는 아직 실계좌 접속 금지. KIS paper account만.
- 장중(09:00~15:30 KST) v3 KIS Gateway 재시작 금지 (토큰 캐시 소실 시 rate limit 403). 단 Fast loop만 재시작 가능.

### 테스트 환경
- 병렬 에이전트들이 `pytest` 실행을 샌드박스 제한으로 직접 돌리지 못했음. 메인이 로컬에서 실행해 전체 297 tests 녹색 + ruff clean 확인.
- 초기 세션 `gh auth`는 `youngs75`. push 전 확인.

### minyoung-mah 의존성
- `StaticPipeline` 조건부 분기, role-level retry with error context, 결정론 후처리 step — 3가지 모두 Track B에서 local workaround. 추후 패턴이 굳어지면 harness PR 후보 (Phase 0 design §3.3).
