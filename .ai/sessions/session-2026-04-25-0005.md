# Session Handoff — 2026-04-25-0005

## 프로젝트 개요

**prime-jennie-runtime** — v3 실행 엔진. 2026-04-25 (토) 다섯 번째 세션.

`session-2026-04-25-0004` (3 cleanup + main 머지 + 04-27 deploy 활성화) 종료 직후
같은 날 토요일 늦은 저녁 **이월 잔여 단기 4건** 처리 세션. 0001~0004 가 매번
"다음에" 로 미뤄왔던 4건을 한 묶음으로 정리하고 main 으로 머지, MS-01 호스트
.env 갱신과 함께 04-27 월 cron 부터 4건 + USD/KRW 복원 동시 활성화.

## 최근 작업 내역

### 1) `scout_runs.candidates_count` 의미 수정

**문제** (0001~0004 이월): `candidates_count` 컬럼이 LLM 예측치
(`scout_out.expected_candidates`) 였다 — 모니터링 시 실제 후보 수와 어긋나
"속음" 발생.

**변경**:
- `slow_loop/persistence.py:343`: fallback 제거. `candidates_count` 인자가 None
  이면 NULL 저장, 값이 있으면 그 값을 그대로 저장.
- `slow_loop/pipeline.py:567`: `candidates_count=len(raw_candidates)` 명시 전달.
- 테스트 +2 (`test_persistence_candidates.py`): 실제 값 사용 / None → NULL.

**결과**: 04-27 월부터 `scout_runs.candidates_count` = sandbox 가 실행한
screening_code 의 실제 raw_candidates 길이. control-ui Scout 페이지 attempts
탭(별도 리포)에서 LLM 예측 vs 실제 후보 수의 다이버전스를 추적 가능.

### 2) Legacy 테이블 drop (migration 016) + 의존 코드 정리

**문제** (0001~0004 이월, "의존성 grep 선행 필수" 명시됨): 6 dead/legacy
테이블이 schema 에 남아 있어 백업 비용·혼란만 유발.

**migration 016** (`migrations/016_drop_legacy_tables.sql`):
- DROP: `daily_macro_insights`, `global_macro_snapshots` (이미 dead — 0024-0001
  에서 확인), `legacy_signal_logs`, `legacy_trade_logs`, `legacy_quant_scores`,
  `legacy_news_sentiments`
- DELETE FROM scheduled_jobs: `analyze_ai_performance`, `analyst_feedback`
  (legacy_trade_logs 가 없으면 의미 없음)

> **번호 노트**: 핸드오프 0001~0004 가 모두 "마이그레이션 013" 이라 적었지만
> 실제로 013/014/015 가 이미 점유돼 있어 **016** 으로 진행. 다음 핸드오프부터
> 016 으로 정정 참조 필요.

**의존 코드 정리** (legacy_trade_logs / legacy_quant_scores 가 살아있는
경로였음):
- `prime_jennie_runtime/jobs/analytics.py`: 본문 전부 비움 (analyze_ai_performance
  / analyst_feedback 제거). v3 PnL 산출은 outcomes 기반이므로 v2 trade_logs
  reason×regime×score_bin 집계는 의미 없음. docstring 만 보존.
- `prime_jennie_runtime/jobs/app.py`: 두 핸들러 등록 + import 제거.
- `scripts/seed_scheduled_jobs.py`: 두 cron 시드 제거.
- `prime_jennie_runtime/briefing/context_builder.py:_collect_watchlist`:
  `legacy_quant_scores` SELECT 제거, 무조건 `[]` 반환. v3 워치리스트 의미는
  scout_runs / screening_candidates 가 흡수 — 별도 워치리스트 잡 불필요.
- `tests/jobs/test_analytics.py`: 삭제 (테스트 대상 함수 제거됨).
- `tests/migrations/test_apply_rollback.py`: EXPECTED_TABLES_AFTER_APPLY 에서
  6 테이블 제거.
- `tests/briefing/test_context_builder.py:test_collect_briefing_data_watchlist_*`:
  "legacy_quant_scores 실패 시 빈 리스트" → "워치리스트는 무조건 빈 리스트" 로
  의미 변경.
- AGENTS.md (briefing, jobs) 업데이트.

### 3) BOK_ECOS_API_KEY 문서화 + 사용자가 MS-01 호스트 .env 갱신

**문제** (0001~0004 이월): 코드 wiring 은 완비됐지만 (docker-compose.yml:430,
jobs/app.py:120-131, council_macro.py:188+) 호스트 .env 에 키가 없어 briefing
USD/KRW 가 비어있음.

**이번 세션 변경**:
- `.env.example` 에 placeholder + 발급 URL (https://ecos.bok.or.kr/api/) 주석.
- 사용자가 직접 ssh 로 **MS-01 호스트 `~/projects/prime-jennie-runtime/.env`**
  에 `BOK_ECOS_API_KEY=<발급키>` 추가 (Claude 가 한 작업 아님).

**이번 main push 가 트리거하는 것**: `ghcr.yml` → 이미지 재빌드 → `deploy.yml`
→ MS-01 컨테이너 재기동. 이 재기동 시 호스트 .env 가 docker-compose 의
`${BOK_ECOS_API_KEY:-}` 변수 참조로 자동 주입됨 — **별도 ssh restart 불필요**.

### 4) Impact 프롬프트 튜닝 + eval fixture 15 → 32

**문제** (0024-0002 진단): EXAONE 4.0 / Qwen3-30B 결과 — `impact_acc 60%`,
실패 7건 중 6건이 **medium 기대를 high 로 과대평가**. 프롬프트의 impact_level
정의가 "주가 영향 크기" 한 줄로 너무 추상적.

**프롬프트 튜닝** (`news_pipeline_kor/adapters/exaone_extractor.py`):
- `_PROMPT_TEMPLATE` 의 impact_level 정의 확장:
  - **high**: 당일 ±5% 이상 가격 변동 예상 — 어닝쇼크/서프라이즈, 회생, 6.4조
    M&A 본계약, FDA 승인, 8% 이상 유증, 영업제한 규제, 호르무즈/대만급 지정학.
  - **medium**: 당일 ±1~5% — 목표가 변경, 부분파업, 신상품 출시, 임원 인사,
    중소형 수주, 1심 판결, 행정지도.
  - **low**: 사실 기록 — 정기 IR 일정, 정기 인사, 일반 시황 코멘트, 신규
    커버리지 개시, 컨센 부합, 분기보고서 제출.
  - **판정 가이드**: 충격성 표현 부재 + 규모(액수/비중/시한) 미명시 시 medium
    이하로 강등.
- 예시 4 (medium — 목표가 7만→7만5천 소폭 상향) + 예시 5 (low — 컨퍼런스콜
  일정 공지) 추가.

**eval fixture 확장** (`tests/fixtures/news_eval/baseline.json`):
- 15 → **32 건**.
- impact 분포: 11/4/0 → **14/14/4** (medium-high 경계 검증 강화).
- 신규 medium 8건: 컨센 부합 실적, 목표가 소폭 상향, 800억 수주, 신상품 출시,
  소규모 매각, 1심 패소, 단계적 부분파업 검토, 자율규제 가이드라인.
- 신규 low 4건: 컨퍼런스콜 일정, 정기 인사, 신규 커버리지 보류, 시황 통계.
- 신규 high 5건: LG에너지솔루션 어닝쇼크, 두산밥캣 6.4조 M&A, 쌍방울 회생,
  거래소 영업정지, 트럼프 수출통제 75품목.
- 프롬프트 예시와 fixture 의 종목/제목 중복 회피 (data leakage 방지).

**주의**: 이 변경은 04-27 월 cron 의 `news_pipeline_kor` 가 EXAONE 4.0 또는
대체 모델로 다음 추출 호출 시점부터 활성화. 첫 운영 결과는 새 32-fixture eval
로 측정 가능 (임시 수동 실행 권장).

## 현재 상태 (2026-04-25 토 늦은 저녁, 본 세션 종료 시점)

### 코드
- runtime HEAD (이 commit 직후): `<handoff commit>` on `claude/unruffled-sutherland-f6518f`
- main HEAD (머지 직후): worktree 와 동일한 SHA + no-ff merge commit
- **904 tests passed**, 1 skipped (pandas 미설치 1건은 이번 세션 무관, 사전부터)
- ruff format + check (변경 파일) 양쪽 모두 clean

### 운영 (MS-01)
- 호스트 .env 에 `BOK_ECOS_API_KEY=<key>` 추가 완료 (사용자 작업, 본 세션 외부)
- main push 직후 ghcr.yml + deploy.yml 자동 chain → 약 2~5분 후 재기동 완료 예상
- trading_flags:stop=1 유지 (실 매매 차단)
- **04-27 월 08:30 KST 첫 cron 부터 동시 활성**:
  - scout candidates_count = 실 후보 수 (LLM 예측치 아님)
  - migration 016 적용 → 6 legacy 테이블 + 2 cron 제거
  - analytics 잡 제거됨 (07/18 KST 의 analyze_ai_performance / analyst_feedback
    cron 안 돔)
  - briefing watchlist 빈 리스트 (legacy_quant_scores 의존 없음)
  - macro_collect_global (07:40 KST) 가 BOK ECOS API 로 USD/KRW 수집 → briefing
    macro 섹션 USD/KRW 복원
  - news_pipeline 다음 추출부터 새 impact 프롬프트 활성

### 글로벌 메모리
- 본 세션은 family.md / trading-domain.md / minyoung-mah.md 변경 사항 없음.

## 다음 단계 (TODO)

### 즉시 (다음 평일 첫 가동, 2026-04-27 월)

- [ ] **08:30 KST scout 결과** Loki 모니터링 — `pj.scout.code_attempt` /
      `pj.scout.code_repeated` / `pj.scout.escalate` 검색 (0004 이월)
- [ ] **scout_runs 테이블** 의 `candidates_count` 가 LLM 예측치 같은 깔끔한
      숫자(0/15/20)가 아니라 실제 raw_candidates 길이로 들쭉날쭉하게 채워지는지
      확인 — 의미 수정 검증
- [ ] **07:40 KST macro_collect_global** 후 Redis
      `macro:data:snapshot:2026-04-27` 의 `usd_krw` 필드가 채워졌는지 확인:
      ```bash
      docker compose exec redis redis-cli -a "$REDIS_PASSWORD" \
          GET "macro:data:snapshot:2026-04-27" | jq .usd_krw
      ```
- [ ] **08:00 KST briefing** 의 macro 섹션 HTML 에 USD/KRW 라인 복원됐는지
      육안 확인
- [ ] **Escalate 발생 시 Telegram 알림** scout_run_id + code_hash 시퀀스 포함
      풀 메시지 도착 확인 (0004 이월)
- [ ] **migration 016 자동 적용** 확인 — deploy.yml 체인이 마이그레이션 러너를
      호출하는 구조인지 점검. 만약 수동 적용 필요한 구조면 ssh 로 한 번 적용:
      ```bash
      docker compose exec job-worker python -m migrations.apply 016
      # 또는 적용 방식 확인 후 동등 명령
      ```
      (apply 방식 모르면 0001~0015 의 적용 흔적을 ssh 로 보면 됨)

### 짧은 후속

- [ ] **새 32-fixture impact eval 재측정** — 0024-0002 의 60% 가 80%+ 로 올라가는지.
      프롬프트 튜닝 효과 검증.
      ```bash
      uv run python scripts/news_event_eval.py \
          --fixture tests/fixtures/news_eval/baseline.json \
          --model hosted_vllm/jart25/Qwen3-30B-A3B-Instruct-2507-Autoround-Int-4bit-gptq \
          --api-base http://127.0.0.1:8001/v1 \
          --json-out reports/eval-$(date +%Y%m%d-%H%M).json
      ```
- [ ] **control-ui Scout 페이지 attempts 노출** — 별도 리포 작업
      (`prime-jennie-control-ui`). dashboard router + UI 시도별 상세 (이번 세션
      의 `candidates_count` 수정 효과를 시각적으로 보려면 이게 필요)

### Phase 3 후속 (Stage 0 → Stage 1 promotion 준비, 0001~0004 이월 그대로)

`stage1-prep` 5 외부 의존:
- [ ] GitHub merge webhook 수신부 (혹은 GHA workflow_dispatch + RateLimiter
      .record_merged 호출 lambda)
- [ ] RevokeWatcher cron (시간당 1회 GHA 또는 k8s job)
- [ ] RuntimeCheckout prewarm vs lazy 결정 — first-run latency 정책
- [ ] baseline 자동 측정 — `pre_merge_baseline_json` 자동 산출 윈도우/지표
- [ ] `_fetch_original_title` 실 PR title — `GitHubClient.get_pull_request(url)` 추가
- [ ] **Stage 0 첫 실 evaluation** — candidates 2-3주 누적 (~05-05 충분) 후 첫
      EvalReport 생성 + 첫 draft PR 한 번 열어보기

## 이번 세션 커밋 요약

### prime-jennie-runtime

| 해시 | 요약 |
|---|---|
| (단일 commit) | feat: 이월 잔여 단기 4건 — candidates_count 정확도 + migration 016 legacy drop + impact prompt 튜닝 + eval fixture 32 + .env.example BOK_ECOS_API_KEY 문서화 + handoff |
| (merge) | merge claude/unruffled-sutherland-f6518f → main (no-ff) |

### claude-global-memory

본 세션 변경 없음.

## 중요 참고사항

### Migration 016 의 자동 적용 여부

deploy.yml 체인이 마이그레이션 러너를 자동 호출하는지는 deploy.yml workflow
실 동작 확인이 필요. 만약 컨테이너 시작 시 자동 적용 (initdb.d / entrypoint
스크립트) 이거나 별도 migration job 이 트리거되는 구조면 04-27 월 자동 적용.
아니면 ssh 로 **04-27 월 cron 직전에 수동 적용 한 번** 필요.

> 0004 핸드오프에서도 동일한 구조 가정 했지만 그때는 schema 변경 migration
> 없었음 (0003 의 마지막 migration 015 까지 이미 운영 적용). 이번 016 이 첫
> "main push 와 동시에 적용해야 하는" 새 마이그레이션이라 자동/수동 여부
> 검증해야 한다.

### legacy_trade_logs 살아있던 잡 제거의 부수 영향

v2 dashboard 의 `ai-performance` 위젯이 Redis `ai:performance:latest` 키를
읽었는데 이번 변경으로 그 키가 더 이상 갱신되지 않음. 만약 v2 dashboard 가
아직 살아 있고 그 키를 보고 있다면 stale 표시될 것. v3 control-ui 는 해당
위젯 미사용으로 영향 없음 — v2 dashboard 가 죽은 상태인지 확인 권장.

### .env.example 와 호스트 .env 의 분리

`.env.example` 은 단순 문서 (placeholder). 실 키는 MS-01 호스트
`~/projects/prime-jennie-runtime/.env` 에 사용자가 직접 입력 — 코드 저장소엔
들어가지 않음. 새 머신/배포 환경 셋업 시 `.env.example` 를 보고 어떤 변수가
필요한지 알 수 있도록 해 둔 것.

### Communication Style 4 원칙 (글로벌 메모리)

정직 / 돌려말 금지 / 코드 용어 금지 / 평문 위주.
`~/.claude/global-memory-youngs7596/communication-style.md`

## 참조

- 직전 세션: `.ai/sessions/session-2026-04-25-0004.md` (3 cleanup + main 머지
  + 04-27 deploy 활성화)
- 글로벌 메모리:
  - `~/.claude/global-memory-youngs7596/prime-jennie-family.md`
  - `~/.claude/global-memory-youngs7596/communication-style.md`
  - `~/.claude/global-memory-youngs7596/trading-domain.md`
  - `~/.claude/global-memory-youngs7596/minyoung-mah.md`
- 본 세션 신규/수정 파일:
  - `migrations/016_drop_legacy_tables.sql`
  - `prime_jennie_runtime/slow_loop/persistence.py` (candidates_count fallback 제거)
  - `prime_jennie_runtime/slow_loop/pipeline.py` (candidates_count=len(raw_candidates))
  - `prime_jennie_runtime/jobs/analytics.py` (본문 비움)
  - `prime_jennie_runtime/jobs/app.py` (두 핸들러 등록 해제)
  - `prime_jennie_runtime/briefing/context_builder.py` (_collect_watchlist → [])
  - `prime_jennie_runtime/news_pipeline_kor/adapters/exaone_extractor.py` (impact 프롬프트 + 예시 4-5)
  - `scripts/seed_scheduled_jobs.py` (두 cron 시드 제거)
  - `.env.example` (BOK_ECOS_API_KEY placeholder)
  - `tests/fixtures/news_eval/baseline.json` (15 → 32)
  - `tests/slow_loop/test_persistence_candidates.py` (+2)
  - `tests/migrations/test_apply_rollback.py` (EXPECTED_TABLES 갱신)
  - `tests/briefing/test_context_builder.py` (watchlist 의미 변경)
  - `tests/jobs/test_analytics.py` (삭제)
  - `prime_jennie_runtime/briefing/AGENTS.md`, `prime_jennie_runtime/jobs/AGENTS.md`

---

**세션 종료**. 0001~0004 가 4세션 연속 미뤘던 단기 이월 4건을 한 묶음으로 처리
+ main 머지 + deploy 트리거. 04-27 월 첫 cron 부터 candidates_count 정확도 +
migration 016 + analytics 잡 제거 + briefing watchlist 빈 리스트 + USD/KRW
복원 + 새 impact 프롬프트 동시 활성화 예정.
