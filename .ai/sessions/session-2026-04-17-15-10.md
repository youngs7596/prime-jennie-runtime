# Session Handoff — 2026-04-17-15:10

## 작업 요약

### 1. 세션 초기 3-Task (bundle 정리)
커밋: `662bd47` `5cf8641` `d84bdd0` + bug fix `cc8ab25`.

- **Slow loop real feeder/adapter swap E2E**: `ScreeningInvoker` Protocol 추가 + `screening_context` JSON-safe 변환 (date.isoformat, model_dump mode=json). `NewsPipelineScoutFeeder` + `ScreeningToolAdapter(subprocess)` drop-in 확인. +3 tests.
- **v2 → v3 migrations skeleton**: `002_legacy_namespace.sql` + `_common.py` (Protocol 기반 `copy_rows` + `row_hash`/`table_hash`) + 3 migrate CLI + `verify_migration.py`. 실 DB 접속은 영석 직접, in-memory fake 로 로직 단위 검증 (+34 tests).
- **Phase 2 착수 계획 초안**: `docs/PHASE2_PLAN.md` (7 워크스트림 + D1~D5 결정 필요 항목).
- **fix**: `test_tick_loop_no_match_persists_state` — 날짜 드리프트(2026-04-16 eod 지남 → time_stop 우발 트리거) 패치.

### 2. Phase 2 전체 완료 (D1~D5 결정 + 8 workstream)

**결정 (영석 위임 → 자체 판단)**:
- **D1**: Qdrant 컬렉션 분리 — `v3_news_sentiments` 전용
- **D2**: v3 자체 `daily_prices`/`minute_prices` 테이블 (v2 read-only 마운트 의존 제거)
- **D3**: `legacy_daily_prices` 마이그 스크립트 **없음** (§13.1 일관)
- **D4**: `TELEGRAM_ALLOWED_CHAT_IDS` 빈 리스트면 기동 거부
- **D5**: KIS paper 토큰은 영석 직접, 코드는 paper 가정 + smoke 스크립트

**구현 (커밋 8개)**:

| # | Phase | 커밋 | 내용 | 테스트 |
|---|-------|------|------|--------|
| 1 | 2.1 | `1bde797` | Telegram handler — 6 control cmd + /help + long_poll + allowlist fail-safe + 5s rate limit | +30 |
| 2 | 2.2 | `c672f56` | ControlCommandConsumer + SystemState reader (`control.state:*` 키) | +20 |
| 3 | 2.3 | `f0a05fd` | KIS Gateway DB fallback + PriceRepo(asyncpg) + migrations/003 + FallbackPriceService | +13 |
| 4 | 2.4 | `478f0ad` | Naver async crawler — v2 포팅, table.type5 구조 유지, 인코딩 자동 감지 | +12 |
| 5 | 2.5 | `770dcee` | EXAONE sentiment (LiteLLM) — v2 0~100 → v3 -1~+1 매핑 + robust JSON 파서 | +16 |
| 6 | 2.6 | `e41edf3` | kure-v1 embedder + Qdrant store — `:memory:` 클라이언트 E2E | +17 |
| 7 | 2.7 | `f354ee9` | PostgresSentimentRepo + migrations/004 (news_sentiments + news_articles) | +8 |
| 8 | 2.8 | `c576689` | `scripts/kis_paper_smoke.py` — 영석용 토큰 발급 후 smoke 검증 | +7 |

## 현재 상태

- **prime-jennie-runtime**: **Phase 2 전체 완료** (Phase 2.1~2.8). **546 tests 통과, ruff clean**.
- **git**: `main` @ `c576689`. `origin/main` 미push (영석 승인 필요).
- **Phase 1 테스트 잔량**: 423 → 546 (+123 tests).

## 테스트 분포 (546 total)

| 영역 | 카운트 (증분) |
|---|---|
| Phase 1 핵심 | 386 |
| Phase 1 closeout: migrations skeleton | +34 |
| Phase 1 closeout: slow_loop real E2E | +3 |
| Phase 2.1 telegram handler + long_poll | +30 |
| Phase 2.2 control consumer + state + roundtrip | +20 |
| Phase 2.3 KIS price_repo + db_fallback | +13 |
| Phase 2.4 Naver crawler | +12 |
| Phase 2.5 EXAONE sentiment | +16 |
| Phase 2.6 kure-v1 + Qdrant | +17 |
| Phase 2.7 PostgresSentimentRepo | +8 |
| Phase 2.8 KIS paper smoke | +7 |

## 핵심 설계 결정 (Phase 2)

### Protocol-first drop-in
- 모든 Phase 2 adapter 는 Phase 1 Stub 의 Protocol 을 그대로 구현 → 파이프라인 변경 0.
- Stub/Real 스왑은 생성자 주입 지점 1 곳 수정.

### fail-safe 기본값
- TelegramConfig.allowed_chat_ids 빈 리스트 → `CommandHandler.is_allowed` False → bot 기동/명령 수신 거부.
- FallbackPriceService: repo 비어있고 KIS 실패 시 원 예외 재발생 → server.py 502/503 유지.
- LiteLLM completion 실패 → neutral(score=0) 반환, 파이프라인 계속.

### Control flow 분리
- Telegram handler = **pure producer** (Redis 만 의존). KIS/DB 미접근.
- Consumer (`control/consumer.py`) 가 `control.state:*` Redis 키 쓰기 담당.
- fast/slow loop 는 `SystemState.snapshot()` 으로 read-only.

### 인코딩/견고성 (Naver/EXAONE)
- BeautifulSoup UnicodeDammit 로 euc-kr/utf-8 자동 감지.
- EXAONE 응답은 code fence + 중간 JSON regex 로 느슨히 파싱.
- 점수 변환: v2 0~100 → v3 -1.0~+1.0 (50=0), 클램프.

## 다음 할 일

### Phase 2 운영 전환 (영석 환경 필요)
- [ ] Telegram bot token 실계좌 설정 + `TELEGRAM_ALLOWED_CHAT_IDS` = `<영석 chat_id>`
- [ ] KIS paper account 토큰 발급 → `python -m scripts.kis_paper_smoke --ticker 005930`
- [ ] `--order` 옵션으로 1주 왕복 smoke (paper)
- [ ] Ollama/EXAONE 로컬 서버 기동 → LiteLLMSentimentAnalyzer 실 호출 smoke
- [ ] Qdrant 로컬 기동 + `v3_news_sentiments` 컬렉션 초기화
- [ ] kure-v1 vLLM 서버 기동 smoke

### Phase 2 통합 E2E (코드 잔여)
- Slow loop E2E 를 **완전 real stack** (NewsPipelineScoutFeeder 입력을 NaverNewsCrawler + LiteLLMSentimentAnalyzer + PostgresSentimentRepo 로 채워서) 로 돌리는 통합 테스트 추가 — 운영 준비 완료 판단 기준.

### Phase 3 착수 전
- Control UI (`prime-jennie-control-ui`) repo 생성 (SystemState 이미 읽기 전용 API 로 활용 가능)
- meta Eval 학습 데이터로 `legacy_*` 테이블 소비 (migrations/005 등)

## 주의사항

### 커밋 / push
- 현재 origin/main 미push (이 세션 12 커밋 로컬에 정체). 푸시는 영석 명시 승인 후.

### 외부 의존
- `beautifulsoup4` 는 pyproject `news` extra 에만 있음 → `uv pip install beautifulsoup4` 선행 (또는 `uv sync --extra news`).
- `qdrant-client` 도 `news` extra. 이번 세션에서 둘 다 설치해 테스트.
- `litellm` 은 core deps — 실 호출 없이 completion_fn 주입으로 테스트.

### migrations 순서
- `001_v3_schema.sql` → `002_legacy_namespace.sql` → `003_price_tables.sql` → `004_news_sentiments.sql`.
- Phase 2 서비스 투입 전 001 + 003 + 004 를 운영 postgres 에 적용 필요.

### 인코딩 이슈 (Naver)
- BeautifulSoup auto-detect 는 replacement char 5% 이상이면 utf-8 재파싱으로 전환. 미래 네이버가 인코딩 바꾸면 이 임계값 재검토.

## Context for Next Session

세션 시작 시:
- `AGENTS.md` — Track 경계, Memory Sync (auth switch 선행)
- `docs/PHASE2_PLAN.md` — 계획 + D1~D5 결정 본문 (완료 상태 반영)
- 이 세션 파일 (`.ai/sessions/session-2026-04-17-15-10.md`)
- `~/.claude/global-memory-youngs7596/prime-jennie-family.md` — **Phase 2 완료 반영 필요** (sync 예정)

v2 원본 경로 (참조):
- `prime-jennie/prime_jennie/services/news/collector.py, analyzer.py, archiver.py` — Phase 2.4/2.5/2.6 원본
- `prime-jennie/prime_jennie/services/telegram/handler.py` — Phase 2.1 원본 (744줄 중 제어 서브셋만 포팅)
