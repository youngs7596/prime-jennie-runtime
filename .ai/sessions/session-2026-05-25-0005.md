# 세션 정리 — 2026-05-25-0005

이번 세션은 직전 0004 분석 (종목 선별 회귀의 1순위가 supply_demand 가중치 손질) 을
코드에 박는 짧은 실행 세션이었다. quant.py 의 수급 점수 max 를 20 에서 10 으로
절반화하고 5-26 화 첫 cycle 부터 효과 측정에 들어간다. push + deploy 완료.

## 1. 프로젝트 개요

prime-jennie-runtime + prime-jennie-control-ui. 5-22 결정론 코어 전환 이후 매매
진행 중. 오늘 (5-25 월) 은 대체 공휴일 휴장이라 push 자체는 안전했다. 5-26 화
08:30 macro/scout 첫 cycle 부터 새 가중치로 종목 선별.

## 2. 이번 세션 작업

### 2.1 1순위 결정 (a) 갈래 실행

직전 0004 에서 (a) `V2_WEIGHTS["supply_demand"]: 20 → 10` 한 줄 수정 vs (b) 절대
금액 임계값을 거래대금 증가율 기반으로 재설계 중 (a) 채택. 사용자가 한 글자
응답으로 합의.

### 2.2 한 줄 수정이 아니라 본문 절반화

작업 착수 직후 한 가지 짚어야 했다. `V2_WEIGHTS` dict 는 quant.py 내부에서
실제 점수 계산에 곱해지지 않고 deterministic_scout.py 의 `factor_weights`
메타데이터로만 쓰인다. `_supply_demand_score` 함수 본문이 0-20 점을 직접
만들어내는 구조라 dict 만 바꾸면 메타데이터 표시만 달라지고 선정 결과는 그대로다.
SQL 시뮬레이션 (supply_demand × 0.5) 효과를 코드에 박으려면 함수 본문의 외인/기관/
외인비율 sub-bucket 과 V2_NEUTRAL 도 같이 절반으로 줄여야 했다. 5~6 군데 수정.

### 2.3 quant.py 수정

- 모듈 docstring 의 가중치 합 110/100 미스매치 → 100 으로 일치 + 5-25 변경 근거
  주석
- QuantScore.supply_demand_score 주석 0-20 → 0-10
- V2_WEIGHTS["supply_demand"] 20 → 10 (메타데이터 정합)
- V2_NEUTRAL["supply_demand"] 10 → 5
- `_supply_demand_score` 본문 절반화:
  - 외인 순매수 0-6 → 0-3 (베이스 3 → 1.5, 계수 동일 절반)
  - 기관 순매수 0-8 → 0-4 (베이스 4 → 2, 계수 동일 절반)
  - 외인 보유비율 0-6 → 0-3 (단계별 6/4.5/3/1.5/0 → 3/2.25/1.5/0.75/0)
  - 최종 cap `min(20, score)` → `min(10, score)`

### 2.4 의존 영역 확인

- `deterministic_scout.py` 의 `quant.supply_demand_score` 직접 인용 (line 177,
  306) 과 daily_quant_scores INSERT/UPDATE (line 275-291) 은 새 값 자동 반영. DB
  컬럼이 DOUBLE PRECISION 이라 0-10 범위도 그대로 들어간다.
- `jobs/factor_analysis.py` 의 `_HIGH_THRESHOLD = 70` 은 supply_demand_score
  (0-20) 에 대해 이전부터 항상 False 였던 dead 코드. 새 변경으로 0-10 이 되면서
  dead 가 더 명확해졌을 뿐. 별건 정리 대상.

### 2.5 테스트 갱신

- `tests/slow_loop/scout/test_quant.py`: `_supply_demand_score >= 15.0` →
  `>= 7.5`, `< 8.0` → `< 4.0`. V2_NEUTRAL 직접 비교는 dict 자체가 바뀌어서 자동
  통과.
- `tests/slow_loop/scout/test_deterministic_scout.py`: `_quant` fixture default
  total 55.0 → 50.0, supply_demand_score 10.0 → 5.0. `test_factors_carry_subscores`
  의 명시 호출 `_quant(total=55.0)` → `_quant(total=50.0)`. QuantScore validator
  의 |total - subscore_sum| <= 1.5 제약 때문.

### 2.6 린트 + 테스트 + push + deploy

ruff format / check 통과 (test_quant.py 한 줄 길이 정리만). pytest 58/58 통과,
회귀 없음. pre-existing 실패 한 건 (`test_prompt_version_bumped_to_v0_6`,
SCOUT_PROMPT_VERSION 가 v0.8 인데 가정은 v0.6) 은 stash 검증으로 main 에 이미
깨져있던 것 확인.

commit `9c1f1b3` push → ghcr 빌드 (run 26402673083) → Deploy workflow
(26402736516) → MS-01 컨테이너 recreate. slow-loop / fast-loop / job-worker /
news-pipeline / dashboard / monitor / kis-gateway / price-scheduler /
telegram-bot / coordinator-listener / cloudflared 11종 모두 "Up 5 seconds"
새 이미지로 교체. 13:24 UTC 시점 (KST 22:24).

## 3. 현재 상태

작업 트리 clean. 새 supply_demand 가중치가 5-26 화 08:30 첫 cycle 부터 적용된다.
시뮬레이션상 진입 예상 종목은 대한항공 (universe 순위 18 → 7), SK텔레콤 (20 →
16), 한국타이어 (17 → 20), 현대모비스 (23 → 25), HD건설기계 (28 → 27) 등 v2
winners 일부 + 코웨이·LG유플러스·SK바이오팜·기업은행 같은 신규 진입 후보. 반도체/
IT 대형주 (삼성전자/SK하이닉스/LG디스플레이/LG전자) 일부가 빠진다.

기존 daily_quant_scores 의 supply_demand_score 컬럼은 5-25 까지 0-20, 5-26 부터
0-10 으로 시기별 범위가 다르게 섞인다. 향후 분석할 때 `score_date` 로 시기 분리해
서 봐야 한다.

## 4. 다음 단계

일주일 정도 (5-26 화 ~ 6-02 월) 돌려본 뒤 outcomes 점검. baseline 두 개:

- v2 시기 245 건: 평균 +2.50% / 건, 승률 64%, 평균 보유 2.94일, +3,019 만원
- v3 LLM 시기 132 건: 평균 -2.24% / 건, 승률 27%

새 가중치가 v2 baseline 쪽으로 회복시키는지, v3 LLM 시기에 머무는지로 (a) 의
효과를 판단한다. 5-22 ~ 5-25 결정론 코어 시기는 29 건 (평균 conviction 0.612) 으로
표본 부족이라 같이 본다.

회복 신호가 보이면 (b) `_supply_demand_score` 의 절대 금액 임계값 (5e9) 을
거래대금 증가율 기반으로 재설계로 넘어간다. v2 winners 의 supply_demand 평균이
universe 평균과 거의 같았다는 점은 거래대금 절대값이 변별력 없다는 강한 단서다.

회복 신호가 약하면 2순위 (5월 시장 국면 — 자동차·항공·통신·산업재 강세, 금융주
약세) 가 점수 구조 외 다른 곳에 박혀있는지 확인. 코웨이·LG유플러스·SK바이오팜·기업
은행 같은 신규 진입 종목이 진짜 시기 적합인지 노이즈인지 분리하는 작업이 같이
필요하다.

3순위는 v2 signal_logs 40만 건 + llm_decision_ledger 16,768 건. 진입 시그널
패턴을 추적해서 결정론 코어에 옮길 수 있는 신호가 있는지 확인. 이건 이전 세션
0004 에서 시간상 못 본 분기.

## 5. 중요 참고사항

`V2_WEIGHTS` dict 의 documentation 성격을 이번 세션에서 한 번 확인했다. 점수 계
산은 sub-score 함수 본문이 직접 max 를 만들어내고, dict 는 `factor_weights`
메타데이터로만 들어간다. 다음에 다른 팩터 가중치를 손볼 때도 함수 본문 절반화 +
V2_NEUTRAL 반영 + dict 갱신 세 군데를 동시에 가야 한다.

테스트의 `_quant` fixture default total 을 50.0 으로 낮춰서 V2_NEUTRAL 합과
같아졌다. 다음에 누가 fixture override 의도로 `_quant(total=...)` 호출하려면
서브점수 합과 1.5 이내로 맞춰야 한다. QuantScore validator 의 제약.

오늘은 휴장이라 push 가 안전했지만, 5-26 화부터는 다시 09:00~15:30 KST 평일
deploy 금지 규칙이 살아난다. 같은 영역 후속 손질이 필요하면 장 마감 후 (15:30
이후) 또는 장전 (08:30 전) 에 push.

`factor_analysis._HIGH_THRESHOLD = 70` 은 별건 정리 후보로 메모. 0-20 범위에서도
이미 dead 였고 0-10 이 되면서 더 dead. 의미 있는 임계값으로 갱신하든 (예: 7) 분
기 자체를 제거하든 둘 중 하나.

## 6. 참조

- 직전 세션: `.ai/sessions/session-2026-05-25-0004.md` (종목 선별 분석)
- 이번 commit: `9c1f1b3` feat(scout): supply_demand 점수 max 20→10
- 변경 파일 3개:
  - `prime_jennie_runtime/slow_loop/scout/quant.py`
  - `tests/slow_loop/scout/test_quant.py`
  - `tests/slow_loop/scout/test_deterministic_scout.py`
- 분석 메모리: `memory/project_2026_05_25_selection_analysis.md` (가설 + 시뮬레이
  션 + 두 baseline 수치)
- 글로벌 메모리 갱신 후보 (다음 sync memory 지시 시):
  - `trading-domain.md` 의 "7팩터 점수 구조 한계 — supply_demand 과대 평가"
    섹션에 "5-25 commit `9c1f1b3` 로 max 20→10 절반화 적용 완료, 5-26 부터 측정
    시작" 한 줄 추가
