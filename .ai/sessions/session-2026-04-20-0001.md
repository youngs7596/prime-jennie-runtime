# Session Handoff — 2026-04-20-0001

## 프로젝트 개요

**prime-jennie-runtime** — v3 실행 엔진. 월요일 2026-04-20 저녁 기준 KIS real
mode + stop 이중 차단 유지. Scout primary=Opus / shadow=DeepSeek, Macro primary
=Opus / shadow=DeepSeek 병렬 축적 중. 본 세션에서 오전 정기 실행 검증부터
새 기능(WSJ Gmail), 인프라 대개조(ghcr.io), 하드웨어 소음 근본 해결까지 수행.

## 최근 작업 내역

### 1) Resume + 정기 운영 점검 (2026-04-20 월요일)

이전 세션(session-2026-04-19-0002) 종료 후 월요일 첫 정기 scout_daily 관찰 목적.
- **scout_daily 7/7 성공** (08:30~14:30 KST). primary cand 6~8 / shadow 4~11.
  100% 승격률, 총 69 sheet. 일 비용 Opus $1.70 + Macro $0.42 = ~$2.12
- **Macro 7/7 open**, size 0.5~0.75, auto_override=false. `MACRO_AUTO_OVERRIDE_DISABLED=1`
  정상 동작. Opus reasoning 품질 실증 (12:30 run 이 스스로 WTI snapshot vs 뉴스
  방향 불일치 감지)
- **shadow 비교**: gate 7/7 일치, size 5/7 일치 (Opus 가 10:30/11:30 에 0.75 로 상향,
  DeepSeek 0.5 고정). shadow_cost ~$0.001/run (primary 60× 저렴)
- **스케줄 잡 29개 점검**: 1건 `daily_asset_snapshot` ConnectError transient,
  kis-gateway restart=0 로 네트워크 튐 단발

### 2) 사용자 보고 이슈 4건 일괄 해결

**Cloudflared 502 Bad Gateway** — 사용자 수동 수정
- tunnel ingress `http://127.0.0.1:80` → cloudflared 컨테이너가 bridge 네트워크
  에 있어 자기 자신 찌르는 상태
- 사용자가 Cloudflare 대시보드에서 `http://control-ui:80` 으로 변경 → 해결

**daily_briefing Telegram 미수신** — 커밋 `ff4d1ce`
- `source=fallback html_len=365 telegram_sent=False` 원인 2건:
  1. job-worker 컨테이너에 `TELEGRAM_*` env 누락 (telegram-bot 서비스엔 있고 job-worker 엔 없음)
  2. `jobs/app.py:175 h_daily_briefing_report` 가 `llm_caller=None` 하드코딩
- fix: docker-compose.yml job-worker 에 TELEGRAM_* + ANTHROPIC_* env 추가,
  `_build_briefing_llm_caller` 헬퍼 신설 (langchain_anthropic), Dockerfile.job_worker
  extras 에 `slow_loop` 추가
- 수동 smoke: `source=llm html_len=1288 telegram_sent=True` 확인

**WSJ Gmail 4종 뉴스레터 미포팅** — 커밋 `57c6031`
- v2 `prime_jennie/infra/crawlers/wsj_gmail.py` 코드 + OAuth 자격증명 그대로 재사용
- v2 credentials.json/token.json → `infra/secrets/gmail/` 복사 (git ignore)
- 신규 handler `wsj_gmail_ingest`:
  1. Gmail API fetch (`access@interactive.wsj.com` newer_than:2d)
  2. `global_macro_news_articles.source='wsj_newsletter'` upsert → 기존 07:30
     `global_news_digest` 가 자동 흡수 → `RealWsjDigestFeeder` 경로로 Macro 반영
  3. Claude Opus 한국어 요약 → Telegram (Redis dedup `wsj:summary:sent:{type}:{date}` TTL 36h)
- 신규 extras `gmail`: google-api-python-client, google-auth-oauthlib, google-auth-httplib2
- 스케줄: `20 7 * * 1-5` (global_news_digest 07:30 직전)
- 실 smoke: 3종 수신 (Markets A.M./10-Point/What's News), 사용자 Telegram 수신 확인.
  Markets P.M. 은 ~05:00 KST 도착이라 smoke 시점 기준 2일 window 에 포함 안됐음

**eGPU + MS-01 본체 팬 굉음 ("비행기 소리")** — 대규모 진단

진단 순서:
1. 첫 관측: GPU fan 98% spike 확인 → 3090 전력캡 `nvidia-smi -pl 200` 시도. peak 277W→200W,
   fan 98%→72% (일부 효과)
2. news_pipeline 에 asyncio.Semaphore(8) 도입 시도 → **역효과**. vLLM 은 단일 요청만으로
   GPU 풀가동이라 peak 동일하고 지속시간만 8초→64초로 늘어남. 즉시 revert
3. **실제 범인 발견**: GPU 가 idle 인데도 "비행기 소리" 지속 → MS-01 본체 팬이
   fan1 4856 RPM / fan2 4299 RPM 로 max 근처 가동 중. CPU 는 40~45°C 정상
4. 원인: `nct6798` 드라이버가 **SYSTIN/AUXTIN 센서를 가짜 116°C 로 보고** (MS-01 보드의 실제
   센서 매핑을 Linux 커널이 모르고 default layout 사용 → 연결 안 된 핀의 낮은 전압이
   고온으로 해석됨). BIOS SmartFan IV 가 이 가짜 값을 참조해 pwm=249 (거의 max) 적용
5. PWM 방향성 테스트: pwm=20 → fan1 569 RPM, pwm=255 → 4909 RPM (정상 반응).
   Linux override 완전 먹힘
6. pwm=40 manual override 적용 → **fan1 1328 / fan2 1147 RPM** (73% 감소), 잔소리 방어선 확보
7. 추가 발견: **매 git push 마다 MS-01 에서 `docker compose up -d --build` 로 10개 이미지
   전체 빌드** → CPU 풀가동 → 팬 굉음 이중 원인. 오늘 밤 push 6회 이상으로 반복 트리거됨

영구화 조치:
- `infra/systemd/nvidia-power-limit.service` — 재부팅 자동 `nvidia-smi -pl 278` (75%
  기본값). 초기 200W 에서 MS-01 팬 잡힌 뒤 278W 로 복귀 (사용자 지시)
- `infra/systemd/mobo-fan-override.service` — 재부팅 자동 hwmon 중 name=nct6798
  찾아서 pwm1/2 manual=40 적용
- 둘 다 systemctl enable + 활성화 확인

### 3) RedisDeduplicator 전환 — 커밋 `698be35`

news-pipeline 컨테이너 재시작 시 `InMemoryDeduplicator` 가 리셋 → 크롤러 window 내
기사 전부가 "신규" 처리되며 vLLM 에 258건 burst → 팬 굉음 주기적 트리거. v2 Redis SET+3일
TTL 스킴 (이미 `RedisDeduplicator` 클래스로 구현돼 있었음) 으로 교체. sync redis 만
지원하므로 news-pipeline 에 별도 sync 클라이언트 추가.

### 4) ghcr.io 빌드 파이프라인 이전 — 커밋 `e60ded7`, `4e0ad35`

v2 패턴 포팅. MS-01 CPU/팬 부하의 가장 큰 원인(매 push 마다 전체 빌드) 제거.

- `.github/workflows/ghcr.yml` 신설: GitHub-hosted runner 에서 10개 서비스 matrix 빌드
  → `ghcr.io/youngs7596/prime-jennie-runtime/<service>:{latest, sha-*}` push.
  cache type=gha, scope 서비스별
- `.github/workflows/deploy.yml` 재작성: `on: workflow_run` 체인으로 ghcr 완료 후
  발동. MS-01 은 git pull + `docker login ghcr` + `compose pull` + `up -d --no-build` 만
- `docker-compose.yml` 10개 서비스에 `image: ghcr.io/...` 추가. `build:` 블록은 유지
  (로컬 개발용)
- `screening_executor/adapter.py` DEFAULT_IMAGE → ghcr 태그
- `control-ui` 는 별 repo 로컬 빌드라 `pull_policy: never` (TODO: 후속에 이전)
- 실증: 첫 ghcr 빌드 11분 → 두번째 1분 (cache 적중), deploy 1분. 총 2~3분 내 배포 완료

## 현재 상태

### 컨테이너 16/16 running
- 9개 custom: ghcr.io 이미지 (39초 전 재시작)
- control-ui: 로컬 이미지 유지 (37시간)
- 공유: grafana/loki/promtail/postgres/redis/cloudflared

### 스케줄 잡 30개 (+1 WSJ)
- 기존 29 + `job_worker.wsj_gmail_ingest` 07:20 평일
- 오늘 scout_daily 7/7 · macro_quick 84/84 · minute_chart 84/84 · news cycle 128+
- daily_briefing 17:00 success (수동 smoke 로 실 도착 검증 완료)

### 하드웨어 (MS-01)
- **GPU**: 278W 캡 (systemd). idle 29W / 48°C / fan 0%
- **본체 팬**: pwm=40 manual. fan1 ~1330 / fan2 ~1150 RPM. CPU 부하 상승해도
  자동 RPM 상승 없음 (정적 설정)
- **CPU**: idle 40~45°C, 빌드 부하 올때 70°C 확인

### 배포 경로 (새로)
- push → GHCR (1~4분) → workflow_run → Deploy (1분)
- MS-01 docker 빌드 0건

## 다음 단계 (2026-04-21 화요일 이후)

- [ ] **07:20 첫 WSJ 정기 크롤** 관찰 — 4종 전부 잡히는지 (Markets P.M. 포함)
- [ ] **07:30 global_news_digest** 가 WSJ 4종을 본문에 포함시키는지 확인
- [ ] **08:30 첫 Macro run** 이 WSJ 내용 기반 reasoning 을 생성하는지
- [ ] **17:00 daily_briefing** 정식 cron Telegram 도착 확인
- [ ] **Scout/Macro shadow 2~3 주 누적** 후 Opus 전환 근거 정량화
- [ ] **engine_error 재측정** (FK fix + real 전환 이후 감소 여부)
- [ ] (옵션) `fancontrol` daemon 설정 — 현 mobo-fan-override 는 정적 pwm=40 이라
  CPU 장시간 고부하(대형 빌드 등) 시 자동 상승 없음
- [ ] (옵션) `control-ui` 이미지도 ghcr.io 로 이전 → pull_policy: never 제거
- [ ] (옵션) MS-01 BIOS 에서 팬 프로파일을 "Silent" 로 변경 → systemd override 불필요해질 수도
- [ ] F6 (backtest stub) 재평가 — outcome 축적 수준

## 중요 참고사항

### MS-01 팬 제어 (새로 확립)
- `/etc/systemd/system/mobo-fan-override.service` 재부팅 자동. hwmon 중 name=nct6798
  찾아 pwm1/2 manual=40 적용
- **CPU 고부하 시 팬 자동 상승 없음** — 주의. 지속 고부하 계획 있으면 fancontrol 로 전환
- 복원 (BIOS 제어로 돌리기): `sudo systemctl disable --now mobo-fan-override.service &&
  sudo sh -c 'for h in /sys/class/hwmon/hwmon*; do [ "$(cat $h/name 2>/dev/null)" = nct6798 ]
  && echo 5 > $h/pwm1_enable && echo 5 > $h/pwm2_enable; done'`

### GPU 전력제한
- `/etc/systemd/system/nvidia-power-limit.service` 재부팅 자동 `nvidia-smi -pl 278`
- 기본값(370W)의 75%. 200W 로 더 조이면 peak fan 72%→60% 까지 내릴 수 있으나 MS-01
  본체 팬이 잡힌 이상 불필요

### 배포 파이프라인 (중요 변경)
- **MS-01 에서 Docker 빌드 하지 않음**. ghcr.io 에서 pull 만
- push → GHCR Publish (GitHub-hosted, 10 matrix) → workflow_run → Deploy (self-hosted,
  pull+up)
- control-ui 는 여전히 로컬 빌드 (별 repo). compose 의 pull_policy: never 로 우회

### WSJ Gmail 파이프 세부
- credentials/token: `infra/secrets/gmail/{credentials,token}.json` (git ignore, MS-01 에 scp)
- 저장 경로: `global_macro_news_articles.source='wsj_newsletter'` — 기존 digest 가 자동
  흡수 (별도 테이블/feeder 수정 없음)
- Telegram 요약: Claude Opus + `wsj:summary:sent:{type}:{date}` dedup TTL 36h
- cron 07:20 평일 → global_news_digest 07:30 → Macro 08:30 로 이어지는 체인

### LLM tier 변경 체크리스트 (유지)
1. `slow_loop/app.py::_try_build_tiered_router` — primary/shadow 모델 인스턴스
2. `slow_loop/persistence.py::_tier_model` — service → model name 반환
3. `dashboard/routers/llm_stats.py::_SERVICES + _FEATURE_MAP + _service_model` — UI 집계/표시

### FK 순서 교훈 (재확인)
- `persist_scout_run` 먼저 → `persist_screening_candidates` 나중 (screening_candidates.scout_run_id FK)
- 오늘 관측: 7/7 cycle 모두 screening_candidates 정상 저장 — FK regression 재발 없음

## 이번 세션 커밋 (main)

| 해시 | 요약 |
|---|---|
| `ff4d1ce` | fix(job-worker): daily_briefing Telegram/LLM 배선 |
| `57c6031` | feat(jobs): WSJ Gmail 4종 뉴스레터 수집 + Telegram 요약 |
| `594220c` | perf(news-pipeline): semaphore 시도 (→revert) |
| `dd8a788` | Revert semaphore |
| `698be35` | perf(news-pipeline): RedisDeduplicator + systemd GPU 전력캡 |
| `73f5352` | chore(systemd): MS-01 본체 팬 PWM override unit |
| `e60ded7` | build(ci): ghcr.io 파이프라인 도입 |
| `4e0ad35` | fix(compose,systemd): control-ui pull 회피 + GPU 278W 복귀 |

### MS-01 영구 반영 (git 대상 아님)
- `/etc/systemd/system/nvidia-power-limit.service` (enabled, 278W)
- `/etc/systemd/system/mobo-fan-override.service` (enabled, pwm=40)
- `~/projects/prime-jennie-runtime/infra/secrets/gmail/{credentials,token}.json` (scp'd)

## 참조

- 이전 세션: `.ai/sessions/session-2026-04-19-0002.md`
- 글로벌 메모리: `~/.claude/global-memory-youngs7596/prime-jennie-family.md`
- Real mode gates: `docs/REAL_MODE_MIGRATION_CHECKLIST.md`

---
**세션 종료**. gh 활성 계정 youngs7596 유지.
