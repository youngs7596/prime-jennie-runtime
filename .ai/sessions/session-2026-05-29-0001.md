# 세션 정리 — 2026-05-29-0001

사용자가 작성한 paper 모드 전환 설계·작업 지시서 두 문서를 받아 P0~P2 를 끝낸 세션. v3 의 정체성을 "paper 기반 alpha 탐색 실험실" 로 코드와 문서 양쪽에서 단일화했다. 5 commit, 전체 테스트 1450 통과·0 실패. 매매 영향 0.

## 1. 출발점

프로젝트 루트에 두 문서가 새로 들어와 있었다 — `prime-jennie-v3-paper-mode-design.md` (정체성·경계 설계, "확정/ground truth"), `prime-jennie-v3-worklist-2026-05-29.md` (P0~P4 작업 지시). 핵심은 v3 가 실계좌 자산 운용에서 손을 떼고 paper 로 alpha 를 증명하는 실험실이 된다는 것. 실계좌는 운영자가 KODEX 200 + 직관으로 직접 굴리고, LLM 은 매매 결정 라인 밖(상류 데이터 생성 + 운영자 코파일럿)으로 못박혔다.

근거는 자산 정체다. v3 가 깎으면 운영자가 멈추고 직접 복구하는 사이클로 순 기여가 0 이었고, LLM 시기 132건은 -2.24%/건·승률 27%. 실계좌론 alpha 증명 전에 돈이 깎여 검증이 매번 끊겼다. paper 면 "측정 → 가설 → 코드 수정 → 재측정" 루프가 안 끊긴다.

## 2. 한 일 — 5 commit

- **P0** (`13cf300`, 문서): 설계 문서를 `.ai/designs/2026-05-29-paper-mode-alpha-discovery.md` 로 영속. README 첫 단락을 "자가진화 트레이딩 엔진" → "paper 기반 alpha 탐색 실험실" 로, Mode 배지 real → paper. AGENTS.md 에 리포 경계 정정 + "폐기된 비전" / "결정 변경 룰" 섹션 신규 + 공유 스펙에서 SCOUT_CODE_GENERATION 제거.

- **P1** (`026b34c`, 문서): LLM-at-core 시절 문서 5종(SCOUT_CODE_GENERATION, prime_jennie_v3_phase0_design, PHASE2_PLAN, PHASE_2_10, PHASE_2_13)을 `docs/archive/` 로, 무효화된 설계 2종(g2-overextension-validator, g6-thesis-aware-exit — 헤더에 이미 "archive 확정" 자기표기 있던 것)을 `.ai/archive/` 로 이동. MACRO_GATE_SPEC·agent-coordinator 헤더 정합, README/prompts 링크 갱신. 5-12 vision-review 와 g2-thresholds 분석은 아직 살아있는 Phase 0 결정 근거라 유지.

- **P2** (`cfedae0` + `4267018`, 코드): scout LLM codegen 잔재 정리. 지시서는 6파일 삭제라 했지만 전수 확인 결과 그중 둘(validators.py, code_hasher.py)이 결정론 파이프라인의 라이브 의존이었다 — 멈추고 보고 후 사용자 결정으로 "결정론 모듈로 이전 후 삭제" 경로 채택. 먼저 검증 로직을 `candidate_validation.py`, 해시 유틸을 `deterministic_scout.py` 로 이전(동작 보존, `cfedae0`). 그다음 prompts/code_loop/role/screening_stub/pipeline_helper + observers/(escalate) + scripts/scout_replay 삭제, pipeline.py 의 죽은 _scout_pipeline/_run_pipeline_with_retry/screening 필드 제거, app.py 의 ScoutRole 등록·scout shadow·escalate 배선 제거(`4267018`, 2697줄 삭제).

- **추가** (`a6b53d6`, 테스트): 기존 stale 2건 수정. bar engine 의 intraday_cum_volume 과 portfolio history 테스트 둘 다 고정 과거 날짜(2026-05-11 / 2026-04-22)를 써서 실행일이 그 날짜를 지나며 깨진 시간 의존 테스트였다. 구현은 정상 — 날짜를 오늘 기준 상대값으로 바꿔 결정론화. worklist 가 이걸 P2 stale 정리로 묶었는데 실제론 codegen 과 무관한 별개 이슈였다.

## 3. 현재 상태

매매 STOP 5-22 부터 그대로. paper outcomes 잡 활성 — 오늘(5-29)부터 본격 측정, 6-1(월) 첫 정량 분석. KIS 잔고 두 종목(069500 KODEX200 1598주, 241560 두산밥캣 41주)은 ps_20260521_* 시트로 v3 trailing 관리 그대로.

scout 디렉토리엔 이제 결정론 모듈만 남았다(candidate_validation, context_builder, deterministic_scout, enrichment, feeders, market_data_loader, quant, schemas, selection). screening_executor 는 안 건드렸다(docstring 한 줄 정정 외) — 그쪽 63 테스트 통과, Track D 격리 샌드박스는 dead-path지만 ScreeningResult 스키마는 deterministic_scout 가 import 하는 라이브 의존이라 유지.

전체 테스트 1450 통과·0 실패. 이 세션 전 기존 stale 2건은 해소됐다.

## 4. 다음 단계

worklist 의 P3·P4 는 6-1 첫 데이터 일정에 맞추는 작업이라 지금 급하지 않다.

1. **P3 — paper outcomes v0→v1**: 1분봉 어댑터 + overextension_exit 활성화 + scale_out weighted exit_price. `jobs/paper_outcomes.py` 헤더의 "v1 계획" 주석에 상세. 6-1 첫 cycle 에서 top30/워치리스트가 coverage=full 로 측정되는지 확인.
2. **P4 — benchmark dashboard**: paper 누적 PnL vs KODEX200(069500) BUY & HOLD. expectancy/승률/손익비/MDD + 인덱스 대비 초과분. dashboard 라우터 신규 또는 portfolio.py 확장.
3. **범위 밖(6-1 데이터 후 결정)**: Coordinator cooldown/dedup policy(같은 종목 손절 후 N일 재진입 차단 — 엘앤에프 8회 전패 류 결함의 근본 해법), macro LLM → 결정론 룰 게이트 전환 검토, supply_demand 정의 재설계(절대 거래대금 → 증가율).
4. **별건**: 5-26 잔여 FNGUIDE parser fix (universe forward_roe 동일값 7종 사고 직접 원인, 분량 작음), silent failure 가드 재개.

## 5. 참조

- 정체성 ground truth: `.ai/designs/2026-05-29-paper-mode-alpha-discovery.md`
- 작업 지시서: `prime-jennie-v3-worklist-2026-05-29.md` (P 작업 다 끝나면 archive 정리 — P3/P4 대기 중이라 루트 유지)
- 직전 세션: `.ai/sessions/session-2026-05-27-0004.md`
- 로컬 메모리: `project_paper_mode_transition_2026_05_29.md` 신규
- 글로벌 메모리: `prime-jennie-family.md` 2026-05-29 섹션 + repo 상태/설계문서 위치 갱신 (commit b3a68c9)
- 이 세션 commit: `13cf300` `026b34c` `cfedae0` `4267018` `a6b53d6`
