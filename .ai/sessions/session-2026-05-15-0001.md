# Session Handoff — 2026-05-15-0001

> 5-15 KST 00:10 직전 세션(5-14-0002) handoff 후 약 20분 만에 이어서 시작.
> 본 세션은 단일 목표(listener daemon wire-up)만 다룬 짧고 깔끔한 deploy 세션.
> KST 00:10 ~ 00:35 약 25분.

## 프로젝트 개요

**prime-jennie-runtime** — v3 실행 엔진. 5-14-0002 의 1순위 TODO 였던
**Coordinator listener daemon wire-up** 을 별도 compose service (옵션 2) 로 해소.
Stage 1 의 마지막 누락 부품. 이제 hook 발행 → daemon 자동 archive 흐름이 무인 동작.

## 최근 작업 내역

### 1) Resume + repo/global memory sync (KST 00:10)

`gh auth switch -u youngs7596` → global memory pull → repo fetch (clean).
직전 세션 `session-2026-05-14-0002.md` 전체 읽고 브리핑. 1순위는 **listener daemon
wire-up**, 권장은 옵션 2 (별도 service `coordinator-listener`).

### 2) listener daemon entry point + 인프라 작성 (KST 00:15)

| 파일 | 변경 | 메모 |
|---|---|---|
| `prime_jennie_runtime/coordinator/app.py` (신규 66줄) | + | AppConfig + redis + asyncpg pool(1~2) + HeartbeatPublisher + SIGTERM/SIGINT → stop_event → `run_listener`. fast_loop/slow_loop app.py 패턴 답습 |
| `infra/docker/Dockerfile.coordinator_listener` (신규 16줄) | + | core deps only (extras 없음). slow_loop Dockerfile 미러 |
| `docker-compose.yml` | +21 | `coordinator-listener` service, profiles=full, postgres+redis healthcheck, `COORDINATOR_CONSUMER_NAME` 옵셔널 env |
| `.github/workflows/ghcr.yml` | +2 | matrix 에 `coordinator-listener` 빌드 추가 |
| `tests/coordinator/test_app_wiring.py` (신규) | + | import + SERVICE_NAME + run() coroutine smoke |

deploy.yml 은 변경 불필요 — `--profile full` 이 자동 포함.

### 3) Commit + push + deploy 검증 (KST 00:18 ~ 00:25)

```
commit fd58d5a  feat(coordinator): listener daemon entrypoint + 전용 compose service
```

- GHCR Publish 25868318651: success (11 image, coordinator-listener 신규 포함, 약 2.5분)
- Deploy 25868403892: success (MS-01 docker compose pull + up -d --no-build)
- 컨테이너 `prime-jennie-runtime-coordinator-listener-1` Up 정상

### 4) Round-trip 검증 (KST 00:25 ~ 00:30)

- **heartbeat**: `heartbeat:coordinator-listener` key 30초 갱신, pid=1
- **publish smoke**: slow-loop 컨테이너에서 `ExternalEvent(daemon-smoke-2026-05-15-00-22)` publish → `1778772085983-0`
- **자동 archive**: event_log INSERT 확인 (수동 `run_listener` 호출 없이 daemon 이 처리)
- **stream backlog**: `XPENDING=0`, `XLEN=3` (이전 smoke 2 + 이번 1 모두 ACK)
- **error 로그**: 0건

### 5) Cleanup (KST 00:35)

```
DELETE FROM event_log WHERE correlation_id LIKE 'smoke-test%'
  OR correlation_id LIKE 'listener-smoke%'
  OR correlation_id LIKE 'daemon-smoke%';
-- DELETE 3
XTRIM coordinator:events MAXLEN 0
-- XLEN 0
```

이제 event_log 총 0 row, stream 도 빈 상태. 다음 거래일 09:15 KST 시초 자연
hook 발화가 fresh slate 에서 들어옴.

## 오늘 commit 요약

| commit | 요약 |
|---|---|
| `fd58d5a` | feat(coordinator): listener daemon entrypoint + 전용 compose service |
| (이번) | docs(handoff): session-2026-05-15-0001 — listener wire-up 완료 + cleanup |

## 현재 상태 (KST 5-15 00:40, UTC 15:40)

### 코드
- runtime main HEAD `fd58d5a` → 본 handoff commit 으로 advance
- origin/main push 완료
- ruff format/check OK, pytest **15 passed + 1 skipped** (기존 14 + app wiring 1)

### 운영 (MS-01)
- `coordinator-listener-1` Up 20분+, crash 0, error log 0
- heartbeat 정상 publish (30초 주기)
- event_log 0 row, decision_log 0 row, `coordinator:events` XLEN 0
- 다른 컨테이너 모두 정상

### 검증된 path
- daemon startup → consumer group 보장 → XREADGROUP 폴링 ✓
- publish_event → daemon 자동 ACK → event_log INSERT ✓
- heartbeat (HeartbeatPublisher) ✓
- SIGTERM/SIGINT graceful shutdown (코드상 — 실제 trigger 미테스트)

### 미검증 path
- 자연 hook 자연 발화 (다음 거래일 시초 — 본 세션 시점 장 마감)
- DLQ 분기 (real load 에서 times_delivered ≥ 5)
- xpending_range 의 redis-py 버전 호환성 (운영 컨테이너에서 실제 호출은 1분 주기로 도는 중 — 1시간 이상 무사 운행 시 silent 통과)

## 다음 단계 (TODO)

### 다음 거래일 (2026-05-15 09:15 KST)
- [ ] **자연 hook 발화 검증** — sheet_published / entry_queued / entry_filled / exit_filled 가 event_log 에 row 로 들어오는지 확인. 정상 path 와 1:1 매칭되는지.
- [ ] decision_log 는 Stage 1 시점에는 비어있음 (Stage 2 가 채울 예정)

### Stage 1 마감 후 (Stage 2 진입)
- [ ] **Stage 2 design 확장** — `.ai/designs/2026-05-14-agent-coordinator.md` 의 Decision Authority 정책 정의. Advisory 단계 (관측 + 권고만, 매매 영향 0) 부터.
- [ ] 정책 후보 (audit_2026_05_14 에서 발견): B2 duplicate_today / A1 max_rejected / F6 macro re-check / D1 외부 매수 감지
- [ ] Phase 0 #1 분석 실제 실행 — `docker compose exec job-worker uv run python -m scripts.analyze_conviction_outcome --since 2026-04-01`

### 별개 트랙 (Coordinator 와 병행)
- [ ] Phase 0 #2 손절 % 진단 — 5-14 baseline + 6주 데이터 누적
- [ ] Phase 0 #3 Macro gate calibration — 5-12 KOSPI -2.29% 사례 알고리즘 표현

## 중요 참고사항

### 본 세션의 architectural 학습

1. **외톨이 패턴 방지의 실천** — listener 를 fast/slow loop 안에 task 로 띄우는
   대신 별도 컨테이너로 분리. audit_2026_05_14 의 외톨이 패턴 분석을 design 결정에
   직접 반영. Stage 4 supervisor 가 자연스럽게 들어올 위치.

2. **소형 entrypoint 패턴 정착** — 66 줄짜리 app.py. AsyncExitStack +
   HeartbeatPublisher + SIGTERM 핸들러 + daemon 함수 호출 4종 셋. 향후 다른
   격리 daemon (Stage 4 supervisor, Stage 2 policy applier 등) 의 boilerplate.

3. **검증 흐름 = publish → daemon 자동 archive** — 수동 `run_listener` 호출이
   더 이상 필요 없음. publish 한 직후 event_log 에 row 가 INSERT 되어야 정상.
   이게 Stage 1 의 의미 — "관찰자가 무인으로 켜져 있다".

### Coordinator 작업 진행 시 주의 (이전 세션에서 이월, 여전히 유효)

- **slow/fast loop 의 timing 절대 변경 X** — Coordinator 는 정보 흐름만 보강
- **Stage 1 은 read-only** — 본 세션 deploy 후에도 매매 path 변화 0
- **agents 끼리 직접 소통 금지** — Coordinator 거침
- **prompt 결정론 제약 충동 → Coordinator policy 로 옮길 것** (Stage 2 영역)

### 글로벌 메모리 sync 권장

- `prime-jennie-family.md` 에 "Stage 1 listener daemon wire-up 완료 (5-15-0001)"
  항목 추가 권장 — 이제 진짜 Stage 1 완료 (직전 세션은 hook + listener 코드는
  있었지만 daemon 미가동 상태)
- `trading-domain.md` 본 세션 변경 없음

## 참조

- 직전 세션: `.ai/sessions/session-2026-05-14-0002.md`
- 본 세션 commits:
  - `fd58d5a` listener daemon entry + Dockerfile + compose service + ghcr matrix + wiring smoke
  - (본 commit) handoff
- 본 세션 신규 메모리: 없음
- 본 세션 cleanup: event_log 3 row + `coordinator:events` XLEN 3 → 0

---

**세션 종료**. 다음 세션은 **자연 hook 발화 검증** (다음 거래일 09:15 KST 직후)
또는 **Stage 2 design 확장**. listener daemon 은 무인으로 계속 동작.
