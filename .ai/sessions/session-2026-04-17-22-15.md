# Session Handoff — 2026-04-17-22:15

## 작업 요약 (Phase 2.9 slice2 — v3 runner 전면 구현 + WSL2 검증)

핸드오프 2026-04-17-20:26 이후 이어진 세션. "**v3 가 v2 대비 빠진 runner 를 전부 채우자**" 가 목표. slow/fast/price/news 4 runner entrypoint + scheduler 인프라 + KIS Gateway streamer 배선까지 **commit 2회 (3119d17, 다음 커밋)** 로 완결.

### 1. 사전 정정
지난 세션 문서가 `fast_loop / slow_loop / price-monitor / scout-job` 을 "없다" 로 표현한 건 부정확. **로직 모듈은 완비**되어 있었고 **long-running entrypoint (app.py) 와 KIS streamer 기동 훅** 만 비어 있었다. 영석님 지적으로 재조사 후 정확한 상태 재인식.

### 2. 커밋 3119d17 — 4 runner 첫 구현 + scheduler 인프라
- `migrations/005_scheduled_jobs.sql` — apscheduler 내장 pickle jobstore 대신 SQL CRUD 가능한 `scheduled_jobs` + `scheduled_job_runs` 테이블 + pj_ui UPDATE 권한
- `infra/scheduler.py` — `SchedulerRunner` + `SchedulerStore` Protocol + `PostgresSchedulerStore`. owner 별 apscheduler 래퍼, 30s polling + Redis pub/sub `scheduler.reload:{owner}` 지원
- `news_pipeline_kor/app.py` — 네이버 크롤 → EXAONE 감성 → kure 임베딩 → Qdrant/PG
- `slow_loop/app.py` — apscheduler → `run_slow_loop()`
- `fast_loop/app.py` — `PositionSheetConsumer` + `TickLoop` 상시 루프 + `BalanceAwareSizer` (SystemState 로 stop/pause 시 qty=0) + `PostgresSheetFetcher`
- `kis_gateway/price_scheduler_app.py` — KIS Gateway REST → minute_prices/daily_prices upsert (v2 collect_minute_chart 대체)
- `kis_gateway/server.py` — lifespan 에서 `KISWebSocketStreamer`/`KISRestPoller` 자동 생성 (기존 None → subscribe API 503 문제 해소)
- `Dockerfile.{news_pipeline,slow_loop,price_scheduler}` + docker-compose 서비스 (profiles=full, host.docker.internal 로 v2 vLLM/Qdrant 재활용)
- `scripts/seed_scheduled_jobs.py` — 4 cron 시드 (news_pipeline */10, price_scheduler */5 + 16시, slow_loop 30 8-14)
- 테스트 +25 (scheduler 13, seed 4, news/slow/fast smoke 8)

### 3. 다음 커밋 — LLM tier 라우팅 + smoke 성공
slow_loop/app.py 첫 구현은 `SingleModelRouter(ChatOpenAI→EXAONE)` 로 잘못 조립 → Scout/Macro 둘 다 EXAONE 호출. 영석님 지적으로 v2 설계 복구.

| Role | tier | 모델 | 라이브러리 |
|---|---|---|---|
| Scout | strong | DeepSeek chat | `langchain-openai` + DeepSeek base_url |
| Macro Gate | reasoning | Claude Opus 4.7 | `langchain-anthropic` |
| News sentiment (별도) | - | EXAONE 4.0 32B AWQ | vLLM OpenAI 호환 |

조정 포인트:
- `TieredModelRouter({"strong": deepseek, "reasoning": claude, "default": deepseek})`
- `_DeepSeekChatOpenAI` subclass — DeepSeek 은 `response_format=json_schema` 미지원 → `with_structured_output(method="function_calling")` 강제
- Claude Opus 4.7 은 `temperature` 파라미터 미지원 (API 400) → 인자 제거
- `pyproject.toml` slow_loop extras 에 `langchain-anthropic>=0.2` 추가

### 4. WSL2 전체 검증 성공

v2 실운영 서버 (MS-01) 를 건드리지 않고 WSL2 에서 먼저 검증한 순서 (영석님 제안):

#### 인프라 기동
- Docker address pool 고갈 → `docker-compose.override.yml` 로 `172.80.0.0/24` 명시 (커밋 제외)
- `docker compose up -d postgres redis` 성공
- migrations 002~005 적용 + pj_runtime/meta/ui 비밀번호 `ALTER ROLE` 동기화
- `scripts.seed_scheduled_jobs` 로 4 cron 시드 확인

#### 4 runner 개별 기동 검증
| Runner | 결과 |
|---|---|
| news_pipeline | `scheduler started: jobs=1` ✅ Qdrant MS-01 공유 OK |
| slow_loop | `jobs=1` — 실 LLM 호출 (Claude Macro 200 OK, DeepSeek Scout 200 OK + 3 retry), "섹터 모멘텀 가설" 한국어 응답 생성. sheet 3개 생성됐지만 장외 시간이라 `valid_until <= 15:30 KST` validator 가 차단 (설계대로) |
| price_scheduler | `jobs=2` (collect_minute + collect_daily) ✅ |
| fast_loop | tracker 복원 0 + sheet_consumer + tick_loop 상시 루프 ✅ |

#### Docker compose build
`news-pipeline / slow-loop / price-scheduler / fast-loop / kis-gateway` 모두 빌드 성공 (exit 0). minyoung-mah 는 **PyPI 0.1.1** 에서 자동 설치되므로 로컬 COPY 불필요 확인.

### 5. 기타
- minyoung-mah 0.1.1: `format_for_llm()` 이 INCOMPLETE + output 없을 때 성공한 tool_results 를 fallback 으로 surface. prime-jennie 에는 무영향 (fast path 는 tool_results 없음)
- `.env` 에 `POSTGRES_PASSWORD` 와 `PJ_RUNTIME_PASSWORD` 를 같은 값으로 통일 (host 에서 uv run 시 pydantic-settings 가 POSTGRES_PASSWORD 로 읽기 때문)

### 6. 최종 테스트
- **571 pass / 0 fail** (slice2 smoke 25개 추가)
- ruff clean

## 현재 배포 상태

### WSL2 (로컬 개발)
```
prime-jennie-runtime-postgres-1  Up (healthy)   5432
prime-jennie-runtime-redis-1     Up (healthy)   6379
```
- scheduled_jobs 에 4 cron 시드 완료
- docker images 빌드 완료 (tag 없이 compose cached layer)
- 4 runner uv run 검증 완료 — 다음 트리거는 월요일 09:00 이후 (장중 한정)

### MS-01
- migrations/005 적용됨 (이 세션에서 선행)
- seed / build / up 은 **미적용** — 월요일 장중 검증 후 배포 예정

## 다음 세션 ToDo

### 🔴 P0: MS-01 실배포 (월요일 장중)
1. `ssh prime-jennie "cd ~/projects/prime-jennie-runtime && git pull"`
2. `.env` 에 DEEPSEEK_API_KEY + ANTHROPIC_API_KEY + PJ_RUNTIME_PASSWORD 동기화
3. `uv run python -m scripts.seed_scheduled_jobs`
4. `docker compose --profile full build news-pipeline slow-loop price-scheduler fast-loop kis-gateway`
5. `docker compose --profile full up -d kis-gateway` 먼저, 로그 확인
6. 나머지 runner up
7. 장중 실제 job 트리거 관측 (09:00 scout_job, 09:05 collect_minute, 09:10 news_pipeline)
8. 24h parallel 성공 시 v2 news-pipeline / price-monitor / scout-job / buy-scanner / sell-executor 5 컨테이너 stop

### 🟡 P1: TurboQuant + vLLM 최적화 (별도 인프라 세션)
영석님 요청 — EXAONE 4.0 AWQ 유지 + **TurboQuant 조합**으로 v2 vLLM 재기동.
- TurboQuant PR #38479 (2-bit KV cache, 4x 용량). 머지 안정화 확인 후 적용
- 대안/선행: `--kv-cache-dtype fp8` 로 FP8 KV 만이라도 활성 (75% 메모리 절감)
- EXAONE 4.5-33B-FP8 은 RTX 3090 VRAM 24GB 에 안 맞음 (33GB) → 4.0 AWQ 유지 결정

### 🟡 P1: slow_loop Scout retry 튜닝
DeepSeek Scout 호출이 3회 retry + 각 1분 대기 → 총 3분 소요.
- 원인 의심: function_calling 응답 schema 파싱 실패 → minyoung-mah `_run_pipeline_with_retry` 가 재시도
- 해결 시 response time 1/3 로 단축

### 🟢 P2: Scout/Macro feeder Stub → 실구현 (Phase 2.9 slice3)
AGENTS.md §위임 경계 명시. 현재 Stub:
- `StubNewsScoreFeeder` → news_pipeline_kor 결과 조회 어댑터
- `StubMarketSnapshotFeeder` → KIS + v3 `daily_prices` 조회
- `StubUniverseFeeder` → `stock_masters` 또는 v2 legacy 조회
- `StubWsjDigestFeeder` → 별도 wsj digest 파이프라인 (v2 에 있으면 포팅)
- `NoOpRiskThrottle` → Track C 실제 throttle 구현

### 🟢 P2: Async RedisDeduplicator
현재 news_pipeline 은 `InMemoryDeduplicator` 사용 (container 재시작 시 24h 내 중복 기사 재분석 가능). v2 와 동일한 Redis 키 스킴으로 async 어댑터 추가.

## 중요 참고사항

- **v3 는 아직 "데이터 공백 위험 없는 parallel 운영" 단계**. fast_loop 는 orders 경로가 SystemState stop/pause 로 차단되어 있지만, v2 `trading_flags:stop=1` 도 유지할 것
- **DB 비밀번호 체계**: docker compose 내부는 `${PJ_RUNTIME_PASSWORD}` 치환, host 에서 `uv run` 할 때는 `.env` 의 `POSTGRES_PASSWORD` 를 그대로 읽음 → 두 값 동일 유지 필수
- **docker-compose.override.yml** 은 로컬 subnet 전용, 커밋 금지 (.gitignore 에 추가)

## Context for Next Session

세션 시작 시:
- `AGENTS.md` — Track 경계, Memory Sync (auth switch 선행)
- 이 세션 파일
- `~/.claude/global-memory-youngs7596/prime-jennie-family.md` — Phase 2.9 slice2 반영 필요 (다음 메모리 sync 시)
- `scripts/smoke_slow_loop_once.py` — Claude+DeepSeek tier 검증 명령

v2 대응 매핑 (slice2 완료 후):
| v2 컨테이너 | v3 대응 | 상태 |
|---|---|---|
| news-pipeline | news-pipeline (Dockerfile.news_pipeline) | ✅ |
| price-monitor | price-scheduler (Dockerfile.price_scheduler) | ✅ |
| scout-job | slow-loop (Dockerfile.slow_loop) | ✅ Scout/Macro feeder 는 아직 Stub |
| buy-scanner/buy-executor/sell-executor | fast-loop (Dockerfile.fast_loop) | ✅ trading_flags:stop 으로 주문 차단 |
| kis-gateway | kis-gateway (streamer 자동 기동) | ✅ |
| job-worker + airflow utility DAG 21개 | 미포팅 | Phase 2.9 범위 밖, v2 병행 유지 |
