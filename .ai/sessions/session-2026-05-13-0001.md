# Session Handoff — 2026-05-13-0001

## 프로젝트 개요

**prime-jennie-runtime** — v3 실행 엔진. 2026-05-13 (수) 단일 세션, 약 2시간 30분 (18:30~21:00 KST).

사용자 한 줄 트리거: "kis gateway가 거의 하루종일 오류를 뿜어내고 있다" — STOP 해제 후 시작된 KIS throttle 폭주 사이클 추적 → root cause 4단 분석 → stale state 즉시 차단 → fix A/B/C/D 4종 구현 + deploy.

## 최근 작업 내역

### 1) Resume + KIS Gateway 오류 진단 (18:30~19:00 KST)

5-12-0001 핸드오프 읽고 brief. 사용자가 어제 STOP → 오늘 오전 resume 했더니 거래 폭주 + KIS gateway 오류가 하루종일.

MS-01 ssh 점검:
- kis-gateway 로그에 `KIS POST .../order-cash -> 500 EGW00201 throttle` 반복
- Circuit breaker `CLOSED → OPEN (fail_count=20) → HALF_OPEN → OPEN` 사이클
- balance / sell 모두 503 Service Unavailable
- fast-loop "Up About an hour" — 다른 컨테이너 20시간 (반복 crash 흔적)

fast_loop 로그: 같은 sheet `ps_20260506_028050_b46f` (profit_floor), `ps_20260506_241560_75c6` (breakeven) 가 100ms 간격 무한 매도 시도.

### 2) Root cause 4단계 확정 (19:00~19:30)

| 단계 | 원인 |
|---|---|
| 1. stale state | 028050/241560 가 5-7~5-12 사이 외부 청산 (사용자 수동 매도, HTS) 됐지만 fast_loop position_tracker 가 모름. Redis `position_state:*` 에 quantity=45/61 남음. executions 테이블 매도 기록 0건 확인 |
| 2. 미체결 → state 미정정 | `exit_executor.execute()` 의 `sell_not_filled` 경로 (exit_executor.py:196-202) 가 `state.quantity` 감소도 `tracker.close()` 도 안 부름. 다음 tick 에 또 trigger → 무한 retry |
| 3. tick burst × trade_limiter sliding window | `xreadgroup(count=10)` + `AsyncRateLimiter` sliding window 가 윈도우 경계 burst 허용 → KIS 본 서버에 5건/0.2초 도달 → `EGW00201` throttle |
| 4. circuit breaker open → task die → restart | `fail_max=20` 누적 후 OPEN → 모든 호출 503. fast_loop 가 503 받으면 `asyncio.wait(FIRST_COMPLETED)` 에서 task die → restart → stale state 재load → 루프 |

### 3) 즉시 차단 (19:30~19:35)

- Redis `DEL position_state:ps_20260506_028050_b46f` + `..._241560_75c6` (8 → 6)
- `docker restart prime-jennie-runtime-fast-loop-1` → in-memory 도 6 states 로 정리
- KIS broker 잔고 확인: 음수 포지션 없음, 총자산 208.6M 보존
- 5-13 executions: sell 4건 (017670/003230/005935/028300) + buy 1건 (000250). NAVER 3중첩 sheet 도 분할 매수로 정상 (17+22+2=41주)

### 4) Fix A/B/C/D 플랜 작성 → 4종 모두 구현 + 테스트 + commit + push (19:35~21:00)

**`.ai/plans/2026-05-13-kis-throttle-fix.md`** 작성 후 사용자 승인 → 구현.

**Fix A — exit_executor sell_not_filled 누적 후 sheet auto-close**:
- `PositionState.exit_fail_count: int = 0` 필드 추가 (domain.py)
- `ExitExecutor.__init__(max_exit_failures=3)` 인자
- `_record_exit_failure()` 헬퍼: fail_count++ + persist. threshold 도달 시 `tracker.close()` + `GenericAlertNotification(severity=critical)` 발행
- 성공 매도 시 `state.exit_fail_count = 0` 리셋

**Fix B — fast_loop 부팅 시 KIS-state mismatch 알림**:
- 신규 `prime_jennie_runtime/fast_loop/position_sync_check.py` (`check_state_kis_mismatch`)
- only_in_state (KIS 잔고에 없는 ticker) → critical alert
- only_in_kis (state 추적 없는 종목) → warning alert
- 자동 정리 X (사용자 메모리 `feedback_sync_positions_manual.md` 준수)
- `fast_loop/app.py` startup 단계 (load_from_redis 직후) 에서 호출

**Fix C — tick_loop exception 격리**:
- `_evaluate_exits` / `_evaluate_pending_entries` / `_evaluate_forced_liquidation` 의 executor 호출을 try/except 로 감싸 503/CircuitBreakerError 등이 task die 로 번지지 않게

**Fix D — rate_limiter sliding window → token bucket**:
- `kis_gateway/rate_limiter.py` AsyncRateLimiter 재구현
- capacity=rate, refill_per_sec = rate/window. 윈도우 경계 burst 차단
- 기존 인터페이스 호환 (acquire, __aenter__/__aexit__, rate)

### 5) 테스트 + lint + commit + push (20:50~21:00)

- 신규 회귀 테스트 **8건** 추가:
  - test_exit_executor: auto_close_after_consecutive_failures, fail_count_reset_on_success + 기존 sell_not_filled 보강
  - test_position_sync_check: 4 테스트 (match silent, state-only critical, kis-only warning, balance failure)
  - test_consumer_and_tick: isolates_exit_executor_exception
  - test_rate_limiter: token_bucket_blocks_burst_at_window_boundary, steady_state_average_rate
- changed scope **246/246 통과**, lint clean
- Commit `47b95df` "fix(fast_loop): stale state KIS throttle storm 차단 — A/B/C/D 4종"
- Push 완료, GHCR Publish 자동 트리거 → MS-01 deploy (어제 기준 ~20분 소요)

## 오늘 commit 요약 (1개 + 본 세션 handoff 예정)

| commit | 요약 |
|---|---|
| `47b95df` | fix(fast_loop): stale state KIS throttle storm 차단 — A/B/C/D 4종 |

## 현재 상태 (2026-05-13 21:00 KST)

### 코드
- runtime main HEAD `47b95df`
- 11 files changed, +663 / -22
- 신규: `fast_loop/position_sync_check.py`, 테스트 `test_position_sync_check.py`, plan 문서

### 운영 (MS-01)
- 19 컨테이너 정상 Up
- STOP 해제됨 (사용자 오전 수동)
- fast_loop position_tracker = 6 states (035420 ×3 + 032830 + 326030 + 000250)
- KIS 4종목 보유, 현금 195.8M, 총자산 208.6M

### Deploy
- GHCR Publish queued at 19:28 KST
- 예상 deploy 완료: ~19:50 KST (어제 기준)
- 다음 거래일 09:00 KST 까지 12시간 여유

## 다음 단계 (TODO)

### 다음 거래일 (5-14) 자연 검증
- [ ] 09:00 시작 시 fast_loop 부팅 → `check_state_kis_mismatch` 결과 텔레그램 확인 (정상이면 silent)
- [ ] 028050/241560 같은 stale state 재발 없는지 확인 (이번 사고가 한번 더 일어나려면 외부 청산 동일 시나리오 필요)
- [ ] trade_limiter 변경 후 정상 매도 처리량 유지 확인

### Phase 0 선결과제 (5-12 핸드오프 이월)
- [ ] **#1 Scout conviction-outcome correlation 분석** — 최근 6주 retrospective. r > 0.3 이면 sizing 반영
- [ ] **#2 매도 96% -5% 손절 원인 분리** — uniform fixed_sl 5% vs entry quality
- [ ] **#3 Macro gate calibration** — 5-12 KOSPI -2.29% 에 open 유지 = 미작동

### 기존 사전 이슈 (이번 fix 무관, 별도)
- [ ] `tests/fast_loop/test_bar_engine.py::test_indicator_provider_adapter` 실패 — main HEAD 이전부터 존재. `intraday_cum_volume` 어댑터 누락. 다음 세션에 fix
- [ ] `ruff check .` 8 errors (UP017 datetime.timezone → datetime.UTC) — 내 fix 외 파일. 일괄 정리 PR 분리 가능

### Fix 후속 검토 (Phase 0 이후)
- [ ] sync_positions 자동화 정책 재검토 — Fix B 알림이 충분한지, 자동 close 까지 갈지 (사용자 결정)
- [ ] exit_fail_count threshold 3 의 적정성 — 장 마감 임박 미체결 등으로 false-positive 발생 시 조정

## 중요 참고사항

### 오늘 학습

1. **사용자 직감의 가치**: "kis gateway가 거의 하루종일 오류를 뿜고 있다" 한 줄이 진짜 사고. 어제의 매수 0 사건과 다른 결의 사고였고 또 즉시 짚어냄. 운영 직감의 우선순위는 audit 보다 빠름 (4-17 이후 5번째 사례).

2. **외부 청산 sync 누락의 위험성**: STOP 해제 트리거가 없었다면 stale state 가 계속 잠재. STOP/resume 흐름이 잠복 버그의 trigger 가 됨. Fix B (startup mismatch 알림) 가 첫 방어선.

3. **단일 sheet 실패가 시스템 전체로 전파되는 구조의 위험**: `asyncio.wait(FIRST_COMPLETED)` 가 한 task die → 전체 shutdown. Fix C 의 try/except 격리가 정석. 다른 path (entry_executor, control_consumer 등) 도 비슷한 검토 가치.

4. **rate limiter 의 sliding window vs token bucket 차이**: 평균 rate 가 같아도 burst 패턴이 다르면 KIS 측 throttle 결과 다름. 외부 한도 트립 회피엔 token bucket 이 안전.

### Plan 문서 보존
`.ai/plans/2026-05-13-kis-throttle-fix.md` — 향후 비슷한 패턴 (외부 sync 누락, 무한 retry) 사고 시 참고

## 참조

- 직전 세션: `.ai/sessions/session-2026-05-12-0001.md` (5-12 매수 0 + 비전 폐기)
- 본 세션 신규 (코드):
  - `prime_jennie_runtime/fast_loop/position_sync_check.py` (신규)
  - `prime_jennie_runtime/fast_loop/domain.py` (exit_fail_count 필드)
  - `prime_jennie_runtime/fast_loop/exit_executor.py` (fail count + auto-close)
  - `prime_jennie_runtime/fast_loop/tick_loop.py` (exception 격리)
  - `prime_jennie_runtime/fast_loop/app.py` (sync check 호출)
  - `prime_jennie_runtime/kis_gateway/rate_limiter.py` (token bucket)
- 본 세션 신규 (테스트):
  - `tests/fast_loop/test_position_sync_check.py` (신규)
  - `tests/fast_loop/test_exit_executor.py` (auto-close 회귀)
  - `tests/fast_loop/test_consumer_and_tick.py` (exception 격리 회귀)
  - `tests/kis_gateway/test_rate_limiter.py` (token bucket 회귀)
- 본 세션 신규 (문서):
  - `.ai/plans/2026-05-13-kis-throttle-fix.md`
- 본 세션 메모리 업데이트:
  - 로컬: `feedback_honorifics_required.md`, `feedback_korean_typo_check.md` + MEMORY.md 인덱스
  - 글로벌 (youngs7596): `prime-jennie-family.md` 에 "5-13 KIS throttle 사고 + fix A/B/C/D" 섹션 (이번 세션 종료 시 sync)

---

**세션 종료**. GHCR build → MS-01 deploy 자동 진행. 다음 거래일 09:00 자연 검증 + Fix B 알림으로 부팅 시 mismatch 즉시 확인.
