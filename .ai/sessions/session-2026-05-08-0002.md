# Session Handoff — 2026-05-08-0002

## 프로젝트 개요

**prime-jennie-runtime** — v3 실행 엔진. 5-08 두 번째 세션. 첫 세션
(`session-2026-05-08-0001`) 직후 동일일자 이어진 작업.

본 세션 흐름:
1. **Trade notification 강화** — 메시지에 종목명/거래액/손익/KST 추가
2. **운영 헬스체크 + KIS circuit 사고 정리** — drift 누적 발견 + 원인 추적
3. **KIS throttle 근본 원인** — `scheduled_jobs` 시드 시점의 잡 중복 + 운영 정리
4. **news_sentiments stale 조사** — 정상 deprecation 확인
5. **운영 도구 2개 + seed 정리 commit + 핸드오프**

## 최근 작업 내역

### 1) Trade notification 강화 (commit `868ac0d`)

이전엔 `code (매수) 수량/가격` + UTC 시각만 와서 텔레그램 메시지로
어떤 종목/얼마짜리/손익/현지시각 파악 불가. fix:

- `TradeNotification` 스키마에 `stock_name`, `entry_avg_price`,
  `pnl_amount`, `pnl_pct` 추가
- `PostgresStockNameResolver` 신규 (stock_masters lookup + 메모리 캐시)
- `ExitExecutor` 가 `state.entry_price` 기반 PnL 계산
  (DRY_RUN/price=0 이면 None 반환해 라인 생략)
- formatter: 종목명 헤더, 거래액 라인, 손익/평단 라인, 시각
  `Asia/Seoul` KST 변환 (`YYYY-MM-DD HH:MM:SS KST`)
- 테스트 9건 추가 (formatter 7 + exit_executor PnL 1)

예시 (청산 손실):
```
🛑 [청산 체결]
삼성전자 (005930) (매도)
수량: 10주 | 단가: 66,500
거래액: 665,000원
손익: -35,000원 (-5.00%)
평단: 70,000
사유: fixed_sl
시각: 2026-05-08 09:23:41 KST
```

### 2) 운영 헬스체크 + 5-08 사고 정리

오전 점검 중 발견:
- **KIS gateway circuit OPEN** (10:40 KST `fail_count=20`) —
  `inquire-time-itemchartprice` throttle 누적
- **PositionTracker drift**: Redis `position_state:*` 72개 vs KIS 잔고
  29 종목. fully_closed 잔재 11개가 여전히 evaluator 호출되며 매 tick
  KIS sell 시도 → circuit 자가 유발

**Phase A (즉시 정리)**:
- price-scheduler 정지 → fast-loop 자동 매도 호출은 계속됨 → 미해결
- STOP 키 SET → exit path 도 차단 (5-08 첫 세션 fix `01d6049` 활용)
  → fast-loop 매도 호출 중단 → circuit ~30초 후 자연 회복
- `/api/balance` 으로 KIS ground truth 확보 (29 종목, 총 매입가
  125,781,213 원, 미실현 -585,613 원)

**Phase B (drift 정리)** — commit `6f9a749` (운영 도구):
- retrofit dry-run: PG executions 누락 0건 확인 (KIS 200 OK 였지만
  application-level reject 로 실 체결 0)
- PG `positions` 29 vs KIS 29 일치 (±433 원 반올림)
- Redis stale 11개 close → 72→61. 운영 도구 2개 신규:
  - `scripts/inspect_position_tracker.py` — sheets 분류 (zero_qty /
    not_in_kis / kis_aligned / kis_drift / orphan)
  - `scripts/cleanup_stale_position_state.py` — KIS 에 ticker 없는
    sheet 만 close (dry-run / `--apply`)

**Phase C (운영 복귀)**: price-scheduler 재시작 +
`/api/control/resume` POST → ControlConsumer 가 STOP/PAUSE DEL →
정상 운영 복귀.

### 3) KIS throttle 근본 원인 추적 + 정리

throttle 27건/30분 추적 → caller IP `172.18.0.3` = job-worker (52
calls/5min). 단순 price-scheduler 가 아닌 다른 호출자.

**근본 원인 — `scheduled_jobs` 시드 시점의 잡 중복**:
- `price_scheduler.collect_minute` (Phase 2.9 Track C 시드) —
  kwargs `universe=[5종목 sample]`, placeholder
- `job_worker.collect_minute_chart` (Phase 2.10 Track B 시드) —
  kwargs `top_n=30` + watchlist 자동 발견

같은 cron `*/5 9-15` 에 양쪽 fire. 5종목 모두 top30 안에 있어
100% 중복. 사용자 원래 의도는 `collect_minute_chart` 단일 수집
이었는데 시드 시점 두 트랙 혼재로 잔재.

**fix** (DB UPDATE + commit 미정 → 본 세션에서 commit):
```sql
UPDATE scheduled_jobs SET enabled = false
WHERE id = 'price_scheduler.collect_minute';
```
+ `PUBLISH scheduler.reload:price_scheduler ""` 즉시 반영.

추가로 코드 정리:
- `scripts/seed_scheduled_jobs.py` 에서 `price_scheduler.collect_minute`
  SeedJob 제거 (재시드 시 부활 방지) + 위 주석 정리
- `scripts/phase_2_10_full_smoke.py` expected set 갱신
  (collect_minute → collect_minute_chart)
- `README.md` cron 표 갱신
- `prime_jennie_runtime/jobs/AGENTS.md` 분리 설명 갱신
- `prime_jennie_runtime/jobs/minute_chart.py` 주석 갱신

**잔존 throttle**: 4건/5min (backoff 1초 수준, circuit OPEN 임계
미만). 옵션 D — 수용. 다음 사고로 임계 도달 시
`_REQ_PER_SEC = 18` → 12 로 낮추기 (`minute_chart.py:29`).

### 4) news_sentiments stale = 정상 deprecation

`news_sentiments` 마지막 update 2026-04-21 발견 → 조사:

`prime_jennie_runtime/news_pipeline_kor/storage.py` docstring 명시:
> "SentimentRepo: 과거 호환 — news_sentiments 저장. **2026-04-21 이후
> write 중단**. EventRepo 신규 — news_events 테이블 upsert.
> Qdrant/VectorStore 계열은 2026-04-21 제거 (메타데이터 RDB 전환)."

검증: `news_events` h24=11,635 / h3=2,727 / latest=12:42 KST (2초 전
update) → EXAONE 정상 작동. 마이그레이션 완료. 액션 없음.

이월: 모니터링/대시보드/스크립트 중 `news_sentiments` 참조 잔재가
있으면 `news_events` 로 교체 — 별도 audit.

### 5) Macro auto_override "왜 걸어뒀나" 정정

신규 매수 0 원인 추적 → 모든 macro_run
`gate=closed, auto_override=true (high_volatility)`. 사용자 질문에
git history 추적:

- 2026-04-16 `MACRO_GATE_SPEC v0.1/0.2` §2.3/§5.2 에 정의된 결정론적
  dual-check (LLM 판단 100% 신뢰 안 함). closed_conditions:
  fx_shock / high_volatility / sector_contagion
- 2026-04-17 `5f54834` Track B 구현 시 spec 그대로 코드화
- 2026-04-18 `64d0d6d` 에서 사용자가 직접 `MACRO_AUTO_OVERRIDE_DISABLED
  env bypass` 추가 (paper 검증 단계용 임시 우회)
- 2026-04-25 phase 2.13 real mode 전환 시 우회 env 제거 → spec 본래
  동작 복귀

→ 사용자가 새로 막은 게 아니라 spec 정책 정상 작동. `high_volatility`
트리거는 KOSPI 20d vol >= 35% 측정값. 진짜로 35% 이상이면 spec 대로
막혀야 맞고, 데이터 stale 이면 데이터 fix 우선 — env 로 끄거나
임계 풀면 안 됨 (5-08 2차 세션 결정).

## 현재 상태 (2026-05-08 13:00 KST 직전)

### 코드
- runtime main HEAD: 본 세션 commit 후 갱신 예정
- 본 세션 신규 commit:
  - `868ac0d feat(telegram_bot,fast_loop): enrich trade notifications`
  - `6f9a749 feat(scripts): position_state diagnostic + cleanup utilities`
  - 본 세션 마지막 commit: seed cleanup (예정)
  - 본 세션 핸드오프 doc (예정)
- 테스트: 843 passing (formatter / executor / job 영향 범위 전부)

### 운영 (MS-01)
- 17 컨테이너 healthy. 9 runtime 컨테이너 deploy 후 운영 중
- KIS Gateway circuit=closed, streamer_running=true (43 종목 WS 구독)
- ControlCommandConsumer running, IntradayRiskThrottle CAUTION (5분
  cycle 정상)
- LongPollLoop / DeepSeek intent router 작동
- STOP/PAUSE/DRYRUN/liquidate_armed 모두 empty (정상 운영 모드)
- 28 scheduled_jobs 중 26 enabled, 마지막 cycle 모두 success

### 데이터
- KIS 잔고 29 종목 ↔ PG positions 29 일치 ↔ Redis position_state 61
  (정리 완료)
- 5-08 매도 26건 (사고 6 + STOP 해제 직후 누적 + 자연 매매)
- 신규 매수 0건 (`high_volatility` macro auto_override 로 자연 차단)
- v3:position_sheets 1119건 누적, fast_loop consumer lag=0
- minute_prices: latest 12:35 KST, news_articles latest 12:30 KST,
  news_events latest 12:42 KST — 모두 신선

### 운영 변화점
- `price_scheduler.collect_minute` enabled=false (DB UPDATE) + seed
  코드 제거. 분봉 단일 수집자 = `job_worker.collect_minute_chart`
- 운영 진단 도구 표준: `scripts/inspect_position_tracker.py` +
  `scripts/cleanup_stale_position_state.py` 사용. job-worker 컨테이너에
  docker cp + python 실행 (uv 미장착 환경 대응)

## 다음 단계 (TODO)

### 즉시 (없음)
- 운영 정상 — STOP 해제, KIS circuit closed, drift 정리, 잡 중복 해소

### 단기
- KIS throttle 모니터링 — circuit OPEN 재발 시
  `prime_jennie_runtime/jobs/minute_chart.py:29` `_REQ_PER_SEC = 18` →
  12 로 낮추기
- `news_sentiments` 참조 잔재 audit (대시보드/모니터/스크립트 grep)
- `high_volatility` 트리거 풀려면 KOSPI 20d vol 측정값 검증 선행
  (실 35% 이상이면 spec 대로 차단 정상). env 로 끄거나 임계
  완화는 금지

### 짧은 후속 (5-08 첫 세션 이월 + 본 세션 추가)
- LLM structured output schema 일괄 audit (`Field(max_length)` 패턴) —
  5-07 이월 그대로
- ControlConsumer `_resume` 핸들러가 `trading_flags:stop` 도 함께 DEL
- `fast_loop/app.py:5-8` docstring 정정 (trading_flags 언급 제거)
- tick_loop pending entry STOP 시 단락 회로
- volume_over_ma20 활성화 / scout entry_hint conditions 예시 / strict
  시그널 정책 (5-07 이월)

### 중기
- outcomes 누적 → prime-jennie-meta 첫 실 evaluation

## 이번 세션 커밋 요약

### prime-jennie-runtime
| 해시 | 요약 |
|---|---|
| `868ac0d` | feat(telegram_bot,fast_loop): enrich trade notifications (name/amount/PnL/KST) |
| `6f9a749` | feat(scripts): position_state diagnostic + cleanup utilities |
| (예정) | chore(scripts): remove obsolete price_scheduler.collect_minute seed |
| (예정) | docs(handoff): session-2026-05-08-0002 — notification 강화 + KIS throttle 정리 |

### claude-global-memory (youngs7596)
- `prime-jennie-family.md` audit 패턴 5-layer 확장 (잡 중복 + 3-way
  ledger cross-check 추가)
- Phase 2.19 섹션 신규 (5-08 2차 세션)

## 중요 참고사항

### 잡 중복 감지 패턴 (audit layer 4 신설)

같은 cron 시각에 동일 v2 source job 이 v3 시드 두 트랙으로 중복
등록되는 케이스가 production drift 로 이어짐. 다음 작업 시
`scheduled_jobs` 전체에 cron 충돌 매트릭스 + handler_key 동일성
점검 권장. seed 시점 placeholder 가 obsolete 됐는지 주기적 확인.

### 3-way ledger cross-check (audit layer 5 신설)

KIS 잔고 (ground truth) ↔ PG `positions` ↔ Redis `position_state:*`
3축 정합성. `positions` 일치하더라도 Redis 가 stale 잔재 누적되면
TickLoop 가 매 tick 죽은 시트 evaluator 호출 → 의미 없는 KIS sell
시도 → throttle 자가 유발. 운영 도구 2개 (inspect/cleanup) 활용.

### macro auto_override 의 진짜 의미

사용자가 임의로 막은 가드 아님. spec §2.3 dual-check (LLM 판단 +
코드 결정론 cross-check). 4-18 paper 검증 시 임시 우회한 env 를
4-25 real mode 시 끈 결과. closed_conditions 트리거 (fx_shock /
high_volatility / sector_contagion) 가 신규 매수 자연 차단. 풀려면
데이터 stale 검증 선행 — env 우회 금지 (5-08 2차 세션 결정).

### KIS throttle 수용 결정

잔존 throttle 4건/5min 은 backoff 1초 수준이라 circuit OPEN 임계
미만. 옵션 D — 수용. 다음 사고로 임계 도달 시
`_REQ_PER_SEC = 18` → 12 로 낮추기. 운영 데이터 누적 후 재평가.

### 본 세션 신규/수정 파일 (주요)

- `prime_jennie_runtime/fast_loop/schemas.py` (TradeNotification 확장)
- `prime_jennie_runtime/fast_loop/persistence.py` (StockNameResolver 신규)
- `prime_jennie_runtime/fast_loop/entry_executor.py` /
  `exit_executor.py` (stock_resolver 주입 + PnL 계산)
- `prime_jennie_runtime/fast_loop/app.py` (와이어링)
- `prime_jennie_runtime/telegram_bot/formatters.py` (KST + 종목명 +
  거래액 + 손익)
- `scripts/inspect_position_tracker.py` (신규)
- `scripts/cleanup_stale_position_state.py` (신규)
- `scripts/seed_scheduled_jobs.py` (collect_minute SeedJob 제거)
- `scripts/phase_2_10_full_smoke.py` (expected set 갱신)
- `README.md` / `prime_jennie_runtime/jobs/AGENTS.md` /
  `prime_jennie_runtime/jobs/minute_chart.py` (cron 표/주석 갱신)
- 그 외 tests/ 다수

## 참조

- 직전 세션: `.ai/sessions/session-2026-05-08-0001.md` (5-08 09:00 STOP
  무력 사고 + 미연결 audit)
- 글로벌 메모리: `~/.claude/global-memory-youngs7596/prime-jennie-family.md`
  Phase 2.19 섹션

---

**세션 종료**. Trade notification 강화 production 반영, KIS circuit
사고 정리 + drift cleanup, 잡 중복 잔재 제거, news_sentiments
deprecation 확인, macro auto_override 정책 정상 검증 완료. 운영
정상 모드. 다음 거래일 자연 매매 + 신규 trade notification 포맷
검증 가능.
