# 세션 정리 — 2026-06-12-0004

같은 날 네 번째 기록. 0003 핸드오프와 같은 push 로 나간 시나리오 B 배포의
사후 검증, 그리고 직후 영석님이 물어본 KODEX200 경고의 진단·수리까지.

## 1. 시나리오 B 배포 검증 (0003 의 후속)

- push 한 번으로 빌드 1분 20초 + MS-01 배포 3분 1초 성공 (13:15 KST, 장중 —
  사용자 지시, 0002 전례).
- 검증 결과: 전 컨테이너 정상 재기동, fast-loop 이 추적 1건 (두산밥캣) 복원 +
  시세 구독 20종목 + control consumer 기동, 텔레그램 봇은 영속된 offset 으로
  재시작해 과거 명령 재전달 없음. PAUSE (exit_only_human_approved_v1) 유지,
  STOP 꺼짐.

## 2. KODEX200 대조 경고 — 진단

질문: "[WARNING] runtime state-KIS mismatch only_in_kis (1) 069500 이 계속
온다". 정체는 5분 주기 reconcile 잡 (읽기 전용, 같은 내용 30분 debounce) 이
KIS 실잔고와 v3 추적 장부를 비교해 보내는 경고. 069500 은 KODEX200 1,568주 —
0002 의 "계속 보유하되 v3 관리 제외" 결정으로 추적이 해제된 순간부터 "추적
없는 보유" 로 보고된 것이다. 시스템 이상도 배포 영향도 아니고, 부팅 검사
(startup state-KIS mismatch) 도 재기동마다 같은 종목을 경고하고 있었다.

## 3. 수리 — 인지된 비추적 보유 무시 목록 (commit `180cf1e`)

- env `RECONCILE_IGNORE_KIS_ONLY` (쉼표 구분 종목 목록). 대조기 두 곳 (5분
  잡 + 부팅 검사) 모두 적용, 공유 헬퍼는 infra/acknowledged_holdings.py.
- **"KIS 에만 있음" 방향 전용** — 반대 방향 (v3 만 보유로 착각, 무한 매도
  위험) 과 수량 어긋남은 무시 목록 종목이라도 여전히 경고한다. 제외할 때마다
  로그를 남겨 조용히 묻히지 않는다.
- compose 의 fast-loop·job-worker 양쪽에 069500 등록 (코드/compose 양면 —
  env 누락 사고 학습 준수). 테스트 1,567 통과 (신규 5).
- 배포 (13:39, 같은 날 세 번째 장중 배포) 후 검증: env 가 두 컨테이너에 도달,
  부팅 검사가 "069500 제외 → check OK: 1 tickers matched", 13:40 reconcile
  실행이 "state-KIS aligned" — 경고 발행 0. 30분마다 오던 경고 종결.

## 4. 현재 상태

- 0003 §3 와 동일: 운영 PAUSE, 추적 장부 두산밥캣 1건 (손절선 65,623원),
  KODEX200 은 비추적 보유 — 이제 무경고.
- 이 세션 파일은 로컬 commit 만 해 둔다 — docs 변경도 push 하면 전체 재배포가
  돌아서, push 는 장 마감 (15:30) 후.

## 5. 다음 단계

1. **이 파일 push (장 마감 후).** 다른 머신의 resume 이 0004 를 보려면 필요.
2. 6-15 (월) 08:30 첫 추천 발송 → dryrun 수락 1건 → 실 1주 (시나리오 B 라이브
   검증, 설계 Pre-flight 순서).
3. 패닉 통과 표본 합류 후 위기장 비교 분석 — 적기 6-15 저녁 이후.
4. /buy 확인 전환 (보호 시트 자동 발행 포함) 별 슬라이스.
5. 이월: minyoung-mah response model 전파 PR, CI pytest 게이트, auto_override
   결정 박제. 후보: 보유-보호용 /adopt 어휘.

## 6. 중요 참고사항

- **무시 목록 운영법**: 인지된 비추적 보유가 또 생기면 compose 두 서비스
  (fast-loop·job-worker) env 에 쉼표로 추가하고 배포. 그 종목을 다시 v3 추적에
  넣는 날엔 목록에서 제거 — 안 빼도 추적되는 순간 어긋남 자체가 사라져 실해는
  없다.
- KODEX200 을 외부에서 팔아도 v3 는 모른다 (추적 없음). 무시 목록은 보유
  여부와 무관하게 "KIS 에만 있음" 만 거르므로, 전량 매도하면 자연히 조용해진다.
- 오늘 하루 장중 배포 3회 (전부 사용자 지시) — STOP/PAUSE 가 진입을 막고 있고
  추적 상태가 Redis 영속이라 재기동 공백 위험이 작았던 맥락. 평시 기본은
  여전히 장중 배포 금지.
