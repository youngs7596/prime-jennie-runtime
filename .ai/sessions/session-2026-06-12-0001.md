# 세션 정리 — 2026-06-12-0001

금요일 아침 resume 으로 시작해 저녁까지 간 큰 세션. 이월 확인 두 건을 닫고,
전수조사 즉시 ToDo 두 건을 배포했고, 무엇보다 **v3 의 정체성을 한 줄 더 고쳐
박은 날이다** — v3 는 추천하고 측정하고, 실계좌 매매의 방아쇠는 항상 사람,
인터페이스는 텔레그램 자연어. 그 결정의 구현 4단계 중 3단계를 같은 날 끝냈다.

## 1. 이월 확인 두 건 (오전, 읽기 전용)

- **FnGuide 건 완전 종결.** 6-11 (목) 06:00 정기 컨센서스 수집이 정상 — success
  228초, 186행 적재 (FNGUIDE 182 / NAVER 4), 목표주가·추정기관수 108종목,
  forward_roe 동일값(8.83) 재발 없음 (최다 중복 2건). 수리 전 6-08 은 목표주가
  0건이었다. 5-26 발견 → 6-7·6-9 수리 → 6-10 수동 검증 → 6-11 정기 검증으로 닫힘.
- **패닉 통과 표본은 아직 0건 — 결함 아니라 설계.** 측정 잡은 보유 윈도우가 완전히
  닫힌 뒤에만 행을 영속한다 (진입·청산을 한 행에). 합류 일정 확정: **오늘 6-12 밤
  18:00 에 첫 3건** (6-4 발행 SECTOR_MOMENTUM — LG·SK스퀘어·SK), 6-15(월) 3건,
  6-19(금)·6-22(월) 각 1건 — 코호트 총 8건. 6-3 발행 2건은 휴장일 잔재로 영구
  제외 (정상). **위기장 비교 분석 적기는 6-15 저녁 이후** (질문 3개는 6-10 기준선
  doc 결론 참조).

## 2. 텔레그램 안전 2건 구현·배포·라이브 검증 (오전, runtime `57c1e3b`)

전수조사 즉시 ToDo 1·2. 사용자 지시로 장중(09:02) 배포 — STOP 상태라 매매 영향 없음.

- **update_id Redis 영속** — long_poll offset 을 `telegram:long_poll:offset` 에
  영속, 기동 시 복원. 봇 재시작 시 텔레그램 재전달로 과거 명령이 재실행되는
  경로(G11) 차단. ACK 는 처리 시작 시점 (유실이 재실행보다 안전).
- **/resume 확인 단계** — /stop /sellall 과 대칭. 영석님이 실제로 /status, bare
  /resume 을 보내 라이브 검증 — offset 키 생성 확인, bare /resume 후에도 stop=1
  유지 (안내만 회신, 발행 없음).

## 3. 정체성 결정 — 사람-승인 매매 + 자연어 인터페이스 (runtime `080a5ac`)

영석님 결정: "v3 는 paper 로 알파 탐색을 지속하고 종목 추천까지, 매수/매도는 내가
직접 지시 또는 수락, 방법은 텔레그램 자연어". 5월 전환 (5-12 → 5-22 → 5-29) 의
종착점. ground truth: `.ai/designs/2026-06-12-human-approved-trading-nl-interface.md`.

- 5-29 "실계좌에서 손을 뗀다"에 **사용자 트리거 매매 예외를 정식화** — 자동매매
  폐기는 그대로. AGENTS.md 경계 문구 + 5-29 doc §1.1 수정 주석 동시 commit.
- LLM 위치 원칙에 (c) 사용자 의도 통역 추가 — 통역만, 장중 감시·판단 금지.
  확인 단어는 직접 친 슬래시 명령으로만 유효.
- **추가 결정: 자동 매매 기능은 코드로 유지** (영석님 명시) — 끄는 건 운영 상태
  (PAUSE + emit_stream=False) 뿐. paper 증명 통과 시 상태 전환만으로 되켠다.
  dead code 정리 제안 금지.
- 전수조사 ToDo 3 (intent router 끔/보강 미결) 은 "확장"으로 종결.

## 4. 구현 ①②③ (오후, 같은 날 완료 — 전체 테스트 1,534 통과)

- **① recovery_exit 룰** (`4f1c888`) — 손익률이 회복선(-10%~0%) 이상이면 전량
  청산하는 10번째 exit 룰. 공유 스펙이라 schema + exit_evaluator +
  POSITION_SHEET_SPEC v0.3 동시 갱신, paper 시뮬레이터는 evaluate 재사용이라
  자동 반영 (테스트로 고정). 양수 임계는 fixed_tp 영역이라 스키마가 거부.
- **② /adopt 명령** (`fb20fdf`) — tracker 밖 실보유 종목 편입. `/adopt 종목
  회복선%` → KIS 잔고에서 매입가·수량 확인 → 해석 echo (발동가 + 안전장치
  -10% 손절·250거래일 시간 종료 명시) → 끝에 `확인` 붙여 등록. 등록 =
  MANUAL_ADOPT 시트 DB 영속 + adopt_position ControlKind 로 fast-loop tracker
  in-process 등록 (재전달 멱등). 이중 편입은 tracker Redis 키로 거부. hold 250 이
  측정 제외 필터(>60)에 걸려 paper 측정 오염 없음. 티커 구독은 positions 기반
  기동 구독이 커버.
- **③ intent v2** (`8bffec4`) — 조건부 매도 자연어("서진시스템이 양전하거나 -1%
  미만으로 손실이 줄어들면 매도해줘")를 `/adopt 서진시스템 -1` 로 번역. 분류
  실패·거부의 무응답 제거 (안내 회신). **NL 경로 args 의 "확인" 토큰 제거** —
  LLM 출력이 확인 단계를 통과시키는 길 차단. /adopt 는 SAFE (자체 확인 단계).
- **④ 운영 PAUSE 전환은 절차** — 배포 후 영석님이 텔레그램에서 `/resume 확인`
  → `/pause`. 진입 차단 + 청산 허용 상태가 목표.

## 5. 현재 상태

- 이 세션 끝에 commit 4개 (080a5ac 문서 + 4f1c888 + fb20fdf + 8bffec4) + 세션
  파일을 함께 push → 빌드 → MS-01 배포 (사용자 지시로 장중 배포, STOP 상태).
- control.state: stop=1 유지. 서진시스템 178320 1주 (매입 80,800) 이 첫 사용 사례
  대기 중.
- 패닉 통과 표본 첫 3건이 오늘 18:00 측정에서 합류 예정.

## 6. 다음 단계

1. **등록된 조건부 주문의 조회·취소 명령 — 다음 작업 1순위.** 설계 §4 가 같은
   슬라이스로 약속했는데 이번에 빠졌다. 현재 해제 수단은 /sell 수동 매도뿐이고,
   조회는 control-ui 에서도 안 보인다. standing order 의 가시성 공백.
2. 서진시스템 라이브 등록 검증 (배포 후 자연어 → echo → 확인 → 등록까지).
3. 위기장 비교 분석 — 6-15 저녁 이후 (코호트 6건 합류 시).
4. 이번 주 항목: minyoung-mah response model 전파 PR (gh auth youngs75), CI
   pytest 게이트, auto_override 결정 박제.
5. 시나리오 B (추천 수락 매수) — 매도 경로 검증 후. PAUSE 와 수동 매수 충돌
   정책을 그때 결정.

## 7. 중요 참고사항

- **조건부 주문의 유효기간은 "청산까지"다** — 시트 valid_until(다음날 15:30)은
  진입용 필드라 exit 추적과 무관. 등록은 ① 조건 충족 매도 ② -10% 손절 ③ 250
  거래일 시간 종료 ④ /sell 수동 매도 중 먼저 오는 것까지 살아 있고, fast-loop
  재기동도 Redis 영속으로 살아남는다. 평가는 장중 tick 에서만 일어난다.
- STOP 중에도 봇 응답·자연어 해석·/adopt 등록 전부 동작한다. 발동 시 주문만
  ExitExecutor 가 차단 — recovery_exit 는 무상태 룰이라 STOP 해제 후 조건이
  여전히 참이면 다음 tick 에 매도된다.
- ruff 저장소 전체 check 는 main 에서부터 깨져 있다 (airflow.py 등 6파일,
  이번 변경과 무관). 내 변경 파일은 깨끗.
- 글로벌 메모리 6-12 절 push 완료 (431cb86). 패닉 표본 일정은 paper outcomes
  로컬 메모리에도 박음.
