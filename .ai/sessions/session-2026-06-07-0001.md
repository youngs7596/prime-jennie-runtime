# 세션 정리 — 2026-06-07-0001

일요일 장 마감 상태에서 resume 로 시작해, 두 가지 대화(구독 토큰으로 LLM 두뇌 교체,
삼성증권 OpenAPI 유무)를 거친 뒤 본 작업으로 "죽은 수집기 셋"을 월요일 전에 손봤다.
진단 결과 셋 중 하나만 실제 코드 버그였고, 하나는 이미 살아 있었으며, 하나는 운영 설정
이중 결함이었다. 팩트체크가 메모리를 뒤집은 세션이다.

## 1. 먼저 나눈 두 대화 (결론만)

**Claude Max 구독 토큰으로 DeepSeek 대체** — `claude setup-token` 의 OAuth 토큰을
자동매매 백엔드에 꽂는 건 구독 약관 위반이고, 걸리면 계정 정지로 지금 쓰는 Claude Code
접근까지 잃는다. 하려면 별도 API 키로 토큰 과금이 전제. 대체 값어치가 있는 자리는 느린
루프의 추론 호출(매크로 게이트·브리핑·해외 뉴스 요약)과 텔레그램 분류 정도이고, 뉴스
메타데이터 추출은 고빈도라 자체 GPU 의 Qwen3 에 그대로 두는 게 맞다. 결정은 보류.
(주의: 이전에 "매크로 Opus 그림자가 돌고 있다"고 잘못 말했다 — compose 기본값
`MACRO_SHADOW_ENABLED=0` 라 실제론 꺼져 있다. Opus 실사용처는 여전히 0.)

**삼성증권 OpenAPI** — 없다. 기관 전용 유료이고 개인 개발자용 공개 API/포털이 없다.
멀티 브로커를 본다면 삼성은 빼고 DB증권·LS증권·키움이 현실적 후보.

## 2. 죽은 수집기 진단 — 셋 중 하나만 코드 버그

세 가지를 추측 없이 코드+운영 DB+라이브 크롤로 갈랐다. MS-01 SSH(`prime-jennie`)로
운영 DB·컨테이너에 직접 붙어 확인했다.

- **펀더멘털: 안 죽었다.** stock_fundamentals 91,160행, 최신 6-1. 네이버 크롤러는
  라이브 테스트에서 종목별 PER/PBR/ROE 를 정확히 반환(삼성 ROE 19.16, 하이닉스 61.16).
  5-26 메모리의 "dead/53% 빈칸"은 그 사이 회복돼 stale. 로컬 메모리만 정정, 코드 무수정.
- **DART: 죽어 있었다(4-17 동결). 이중 버그, 둘 다 라이브 수리.** ①운영 `.env` 에
  DART 키 라인이 없어 빈 키 → 핸들러가 즉시 skip → 매일 39ms "성공"(컨센서스는 4분).
  v2 리포의 유효한 40자 키를 v3 `.env` 에 넣고 job-worker `--force-recreate`. ②키를
  고치자 첫 insert 부터 pkey(id) 충돌 — id 시퀀스가 last_value=1, max(id)=156337 로
  desync(마이그 때 시퀀스 미갱신). `setval` 로 재동기화. days=30 백필로 2,727건 적재,
  stock_disclosures 6-5 까지 최신화(47,524행). **이미 라이브 동작, 매일 18:45 크론 인계.**
- **FnGuide 컨센서스: 코드 버그(확정).** 업종비교 표 헤더가 [구분, 회사, 업종평균,
  KOSPI평균] 인데 파서가 마지막 셀(KOSPI 평균)을 읽어 forward_eps·forward_roe 가 전
  종목 동일값으로 오염(운영 6-04 자 231종목 중 두 값 모두 distinct 2개). 회사 값인 첫
  셀을 읽도록 수정. forward_per 은 Financial Highlight 경로가 정상이라 유지. 라이브 검증
  +단위테스트 4개+jobs 102개 통과+린트. 커밋 `d013b77`.
  - 단서: 이 EPS·ROE 는 "최근결산" 회사 값이라 엄밀히 forward 아닌 trailing 에 가깝다.
    그래도 종목별 구분되는 진짜 값이라 시장평균 오염보다 비교 불가하게 낫다.
  - 미해결(별개·기존 공백): target_price·analyst_count 는 여전히 None. SVD_Main 에 그
    형태로 없어 보임. 이번 범위 밖.

## 3. 현재 상태

- runtime: main 에 `d013b77`(FnGuide) 커밋됨. 이 핸드오프 커밋과 함께 푸시 → MS-01 배포
  예정(코드 변경이라 fast-loop 포함 전 컨테이너 recreate, STOP/PAUSE 는 redis 보존).
- DART: MS-01 에 라이브 적용 완료(`.env` 키 + 시퀀스). 코드 변경 없음.
- 시스템은 여전히 STOP+PAUSE. 일요일+STOP 이라 장중 배포 게이트 비적용.

## 4. 다음 단계

1. FnGuide 배포 검증 — 다음 컨센서스 수집(월 06:00 KST) 후 stock_consensus 의
   forward_roe/forward_eps distinct 수가 종목별로 퍼졌는지 확인(이전엔 2개).
2. DART 운영 검증 — 다음 18:45 크론 후 stock_disclosures 가 당일까지 들어오는지,
   잡 실행시간이 39ms 가 아니라 정상(수 초)인지 확인.
3. (이월·선택) FnGuide target_price·analyst_count 복구 — 별도 컨센서스 페이지/엔드포인트
   조사 필요. 급하지 않음.
4. (이월) Claude API 두뇌 도입 여부 결정 — 매크로 그림자부터 켜서 비교 데이터 축적.
5. 결정론 코어 재분석 — 6월 중하순 표본 30+.

## 5. 중요 참고사항

- **status=success + 비정상적으로 짧은 실행시간 = skip/early-return 의심.** DART 가
  매일 39ms "성공"이던 게 빈 키 skip 이었다. 결과 테이블 max(date)+행수로 실검증.
- **마이그 후 serial PK 테이블은 시퀀스 재동기화 필수.** DART id 시퀀스 desync 학습.
- **다층 결함 주의.** 빈 키를 고치자 그 뒤에 시퀀스 버그가 드러났다. 하나 고치고 끝내지 말 것.
- STOP/PAUSE 는 진입·청산(매매)만 막고 데이터 수집 크론은 무관하게 돈다(코드 확인).
- MS-01 직접 작업: SSH alias `prime-jennie`(192.168.31.195), DB는 컨테이너
  `prime-jennie-runtime-postgres-1`(pj_admin/prime_jennie_v3).

## 6. 참조

- 직전 세션: `.ai/sessions/session-2026-06-05-0003.md`
- 변경 커밋: `d013b77`(FnGuide), 본 핸드오프 커밋
- 로컬 메모리: 신규 `project_dart_collector_fix_2026_06_07.md`, 갱신
  `project_v3_fundamentals_collection_dead_2026_05_26.md`(stale 표시)
- 글로벌 메모리: `prime-jennie-family.md` 수집기 상태 갱신
