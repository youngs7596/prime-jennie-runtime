# 세션 정리 — 2026-05-25-0001

대체 공휴일 휴장 아침에 들어와 dashboard 재설계를 한 세션에 phase 1~4 다 끝낸 흐름. 사용자가 어제 만들어 둔 의뢰서 + 핸드오프 지시서 + Claude Design 시안 11 페이지 셋을 입력으로 받아 control-ui 14 commits + runtime 6 commits + 통합 검증 fix 1 commit 으로 마무리. 매매 영향 없는 휴장이라 두 리포 main 머지·push 까지 한 흐름.

## 1. 프로젝트 개요

prime-jennie-runtime, v3 실행 엔진. 결정론 코어 + STOP/PAUSE 해제 상태 그대로. 별도 작업: prime-jennie-control-ui (React + Vite + TS + Tailwind, runtime dashboard FastAPI 를 백엔드로 소비) 의 narrative 재설계. 시안 ZIP 11 페이지는 `/home/youngs75/projects/prime-jennie-v3-dashboard-renewal/`, 의뢰서·핸드오프는 runtime 의 `.ai/designs/` 안.

오늘이 대체 공휴일 휴장이라 09:00~15:30 deploy 게이트 영향 없음. 거래 자체가 안 일어나 backend 변경 + main 머지 + ghcr 자동 빌드 + MS-01 deploy 까지 안전하게 갔다.

## 2. 이번 세션 작업

### 2.1 Phase 1 — 디자인 시스템 토큰 + 공용 컴포넌트

control-ui 의 tailwind.config.js 를 시안 시트 9 묶음 그대로 재작성했다. bg / fg / line, accent 8 색, 그리고 semantic alias 5 묶음: macro (bull/neutral/bear), throttle 5 단계 (nominal/soft/half/trickle/halted), heartbeat (healthy/attention/warn/danger), pl (profit/flat/loss), flag (off/protective/critical), service (news/macro/scout/briefing). 기존 16 파일의 옛 토큰 (`bg-bg-primary` / `text-text-secondary` / `border-border-primary` 등) 은 sed 일괄 마이그레이션, 잔여 0 건 확인. Tailwind 의 `colors.border` 가 border-width utility 와 충돌하길래 `line.DEFAULT/strong` 명명으로 회피했고, throttle key 도 `'1.0'` 같은 string 이 selector escape 문제라 nominal/soft 식 tag 명으로 갔다.

그 다음 신규 공용 컴포넌트 13 종 추가 (HeartbeatDot, SparkLine, MacroGateBar, CommandButton, CronCell, AggregateCard, EventBanner, LogRow, FilterChip, RunListRow, DiffBadge, TopRiskCard, ControlFlagCard). props + 토큰 기반 stub 만, Phase 3 페이지 배선용. 시안 픽셀 정확 일치는 안 했고 narrative 의도와 토큰만 보장.

### 2.2 Phase 2 — runtime dashboard 백엔드 보강 6 영역

핸드오프 §3 의 6 항목을 각각 별도 commit 으로 분리. 매 commit 통합 테스트 1~3 건 추가.

portfolio /summary 응답에 sector_group 필드 + ETF prefix 12 발행사 (KODEX/TIGER/ARIRANG/KBSTAR/KOSEF/HANARO/KINDEX/ACE/RISE/PLUS/SOL/KOACT) 로 식별. stock_masters 에 type 컬럼이 없어 이름 prefix 로만 식별 (메모리 메모 그대로). logs /stream 에 level (multi) + q (LogQL regex) 파라미터 — LogQL 동적 결합 + injection 차단 위한 escape. system /health 에 port (urlparse) + heartbeat_status_level (4 단계 healthy/attention/warn/danger) — 프론트가 직접 초 비교 안 하도록 백엔드 한 곳에서 계산. llm /stats 에 cost_actual + cost_shadow + vllm_amortized_daily_usd — 신설 dashboard/llm_pricing.py 모듈에 deepseek $0.27/$1.10, openai gpt-4o-mini, anthropic opus-4.7 가격표. vLLM (RTX 3090) 일일 amortized $2 보수 추정 (전력 + 5 년 감가, 휴장 idle 인 날엔 amortize 안 시킴 — 비용 해석 안 망가지도록). control flag 변경 audit hash + /control/state 응답에 last_change 메타 — consumer.py 의 모든 state handler 가 같은 record_change 호출, UI / telegram / 자동 발동 셋 다 같은 형식. system status_label="recovered" — uptime<1h + heartbeat<30s 단순 휴리스틱. 핸드오프 §3.6 의 정확한 조건 (last_24h_heartbeat_breach>0) 은 hourly breach counter 인프라가 별도라 minimal 로 시작.

### 2.3 Phase 3 — 페이지 10 개 narrative 통합

작은 페이지부터 큰 페이지 순으로 매 페이지 시안 narrative + 신규 토큰/컴포넌트 적용 + 빌드 통과 + commit. News 카테고리 16 종 색 매핑부터 시작해 LLM Stats (macro 색을 red 가 아닌 blue, 차트에서 위험 신호 오인 회피), Jobs (CronCell 한국어 변환 6 패턴 + prefix 7 그룹핑), Portfolio (Summary 6 슬롯 + Sector chip + P&L mini bar + Asset History 듀얼 axis), Trades (BUY/SELL split bar + BEAR auto-liquidate 4 채널 narrative), System (Summary 5 슬롯 + 12 번째 AGGREGATE 카드 + HeartbeatDot + ControlFlagCard + Command Center 6 버튼), Logs (3 채널 ERROR 가시화 + 헤더 3 줄 + tail pause/resume), Macro (좌우 분리 scroll + sticky day separator + TopRiskCard + DiffBadge + Reasoning 13px), Scout (좌우 분리 + Conviction tier 색 + Factor 한국어 + Context 접힘), Overview (Hero band 1 카드 + 5 카드 균등 + MacroGateBar chronological + NEXT REFRESH 카운트다운).

마지막에 Layout 사이드바도 RUNTIME / INTEL / OPS 3 묶음 그룹핑 + footer ● live 점.

### 2.4 Phase 4 — 통합 검증 + fix

핸드오프 §7 5 영역 정적 코드 검증. 색 토큰 / 운영 narrative / UX 패턴 / 데이터 정확성 4 영역 통과, cross-link 영역만 부분 통과. Trades/Scout 가 cross-link URL `/macro?date=...&run_id=...` 만들지만 Macro 가 useSearchParams 로 받는 부분 없었고, Macro detail 의 run_id 에 Scout 역방향 link 미구현, Overview Hero band 의 SYSTEM ALERT pill 이 없던 것 세 군데 한 commit (`b5ed5f4`) 으로 fix.

dev 서버 + 백엔드 wiring + 브라우저 visual 확인은 안 한 정적 검증 한계라는 점은 마지막 보고에 명시.

## 3. 현재 상태

control-ui feature/dashboard-redesign 14 commits + runtime feature/dashboard-redesign-backend 6 commits 모두 main 으로 머지·push 완료. runtime 머지로 ghcr 빌드 + MS-01 deploy 자동 트리거 (휴장이라 거래 영향 없음). control-ui 는 수동 빌드 정책이라 dashboard 시각 변경은 다음 수동 deploy 시 반영. 두 디자인 doc (의뢰서 + 핸드오프) 도 runtime 메인에 commit 됨.

핵심 검증 통과 항목: backend `_heartbeat_level` 임계 (30/60/120s) 와 frontend HeartbeatDot 의 heartbeatTier 임계 정확히 동기, ETF prefix 12 발행사 정상, vLLM 가격표 deepseek $0.27/$1.10 정확, 의미 토큰 (macro/pl/flag/heartbeat/throttle/service) 정의 + 사용처 일관.

## 4. 다음 단계

dev 서버 + 백엔드 wiring 띄워 시각 검증이 가장 먼저 와야 할 작업. dashboard FastAPI 가 MS-01 (또는 로컬 docker compose) 에서 도는 상태에서 `npm run dev` 로 control-ui 띄우고 각 페이지 narrative 가 실제로 시안과 같은 톤인지 확인. 시안 픽셀 정확 일치는 안 한 부분들 (특히 Macro Reasoning 의 syntax coloring 같은 line-level 강조, Overview System Health dot indicator 11 개 가로 정렬, Scout Conviction tier 의 상위 3 / 중위 / 하위 2 경계) 이 가시적으로 어떻게 떨어지는지가 첫 fine-tune 신호.

후속 fine-tune 항목 셋. 사이드바 badge 카운트 dynamic 화 — 시안의 Trades 28 / News 12 / Jobs 1 은 hardcode 가 아니라 useRecentTrades, useNewsStats, useJobs 의 실제 응답에서 가져와야. Layout.tsx 의 NAV_GROUPS 옆에 hook 으로 카운트 주입. Reasoning 블록 syntax coloring — 현재는 다크 배경 + mono 13px 까지만, 시안의 결론 녹강조 / step 회색 / 숫자 노랑 / 음수 빨강 / 양수 녹은 line-by-line regex 가 필요한 부분. EventBanner 의 5-18 hardcode (`BEAR_AUTO_LIQUIDATE_DAYS = new Set(["2026-05-18"])`) — runtime 쪽에 자동 청산 메타 필드 (trade row 의 auto_liquidate boolean 또는 day 단위 macro gate 닫힌 + 청산 발동 row 조회) 를 추가하고 프론트가 그걸로 동적 분기해야 정상 동작.

운영 narrative 측면에서 한 가지 더. control flag audit hash 의 triggered_by 형식 — UI 는 "ui:alice", telegram 은 "telegram:1001", 자동 발동은 "auto:slow-loop-hb-expired" prefix 권장. 현재 consumer.py 의 record_change 호출 부분에서 ControlCommand.issued_by 가 그대로 들어가니, 자동 발동 publisher (heartbeat watcher 같은 컴포넌트) 가 이 형식을 지키도록 후속에 손볼 작업.

5-25 첫 풀데이 거래일은 0004 핸드오프의 약속대로 다음 거래일 (5-26 화) 로 미뤄짐. 0003 가드 + 0004 별건 두 fix 의 실제 거래일 검증은 화요일 장 끝나고.

## 5. 중요 참고사항

핸드오프 지시서가 `.ai/designs/2026-05-25-dashboard-handoff-brief.md` 로 commit 됨. 미래 dashboard fine-tune 작업 시 §7 검증 체크리스트 다시 돌리면 narrative drift 잡힌다. 의뢰서 v2 와 핸드오프 지시서 사이의 결정 30+ 개가 이 한 문서에 보존됐기 때문에 새 페이지 추가 / 기존 페이지 변경 시 이 문서가 기준선이다.

heartbeat 임계 (30/60/120s) 변경은 control-ui HeartbeatDot 의 heartbeatTier 와 runtime dashboard/routers/system.py 의 _heartbeat_level 두 곳 동시 수정 필수. 두 함수 위에 같은 임계 같은 내용으로 주석 박혀 있지만 동기화 빠지면 UI 단계와 백엔드 status_level 이 어긋난다. 이건 두 함수 위 주석 (`핸드오프 §3.3, control-ui HeartbeatDot 와 같은 임계`) 에 명시.

vLLM amortized $2/day 는 보수 추정. 실제 전력 측정 + 감가 재산정 시 dashboard/llm_pricing.py 의 VLLM_AMORTIZED_DAILY_USD 한 곳만 조정. 가격표 (`PRICING` dict) 도 같은 모듈, 향후 hot-reload 필요해지면 config/llm_pricing.yaml 로 빼낼 여지를 주석에 남김.

세 컴포넌트 중 EventBanner / TopRiskCard / ControlFlagCard / CommandButton 은 실제 페이지 (Trades / Macro / System) 에서 props 인터페이스 다 활용 중이라 변경 시 사용처 한 번 더 확인 필요. 나머지 컴포넌트 (HeartbeatDot, SparkLine, MacroGateBar, FilterChip, RunListRow, DiffBadge, CronCell, AggregateCard, LogRow) 는 각자 한 페이지에서만 사용되는 컴포넌트라 변경 영향 작음.

## 6. 참조

- 직전 세션: `.ai/sessions/session-2026-05-24-0004.md`
- 핸드오프 지시서: `.ai/designs/2026-05-25-dashboard-handoff-brief.md`
- 의뢰서 v2: `.ai/designs/2026-05-24-dashboard-redesign-brief.md`
- 시안 ZIP 11 페이지: `/home/youngs75/projects/prime-jennie-v3-dashboard-renewal/`
- runtime commits (main 머지): `8f38a96` / `dc810ec` / `e7e3364` / `5697baa` / `30e1f9e` / `c21f905`
- control-ui commits (main 머지): `7210d14` (refactor(tokens)) 부터 `b5ed5f4` (fix(narrative)) 까지 14 개
- 글로벌 메모리 갱신: `prime-jennie-family.md` "2026-05-25 월요일 휴장 — control-ui 대시보드 재설계 4 phase 완료" 섹션
