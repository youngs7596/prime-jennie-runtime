# Session Handoff — 2026-05-22-0001

> 트리거: `AGENTS.md resume` → 직전 세션 이어받기 → 사용자가 "v3 가 v2 보다 못하다,
> 진화가 아니라 퇴보 같다" 판단 → v2/v3 정밀 비교 조사로 확장 → 하루 종일 4단계
> (v2 해부 / v3 평가 / 결함 burn-down 배포 / 선정 아키텍처 결정 + Phase 1 포팅).

## 1. 프로젝트 개요

prime-jennie-runtime = v3 실행 엔진 (KOSPI 자동매매). 계보 my-prime-jennie → v1 →
v2(prime-jennie) → **v3(현 라이브, MS-01)**. slow loop(시간당 macro+scout) +
fast loop(실시간 틱 진입/청산). 상세 `AGENTS.md`.

## 2. 이번 세션 작업 내역

### 2.1 v2 teardown (step 1) — `.ai/analyses/2026-05-22-v2-teardown-{selection,execution,data-news,orchestration}.md`
다중 에이전트로 v2 코드+데이터(v2 MariaDB 부활 — `prime-jennie-mariadb-1`, MS-01 port3307) 정밀 해부.
**결정적 발견**: "검증된 v2"는 허상 — v2 의 stream 아키텍처는 라이브 매매 ~2주
(2026-02-19~03-05)뿐, 이후 6주 STOP 후 은퇴. trade_logs 의 좋은 실적(+30.5M·승률70%)은
전부 v1-ETL. v2-native 실적은 청산 35건·합산 −34만원. → "v1(검증됨) / v2-native(2주·미검증)
/ v3" 3자 프레임.

### 2.2 v3 assessment (step 2) — `.ai/analyses/2026-05-22-v3-assessment-*.md`
v3 를 4도메인 53훅으로 냉정 평가. 4대 결함 패턴: ①이음매 결함(시간청산·death_cross
배선 누락, STOP blast radius) ②v2 안전게이트 상실 ③LLM-at-core 아키텍처 역전
④스캐폴딩 過·실물 未.

### 2.3 step-3 burn-down — **배포 완료** (commit 22f7fbd~586f741, main)
- 시간청산·death_cross 배선 복구 (fast_loop)
- STOP 게이트를 시트 발행 직전으로 이동 (STOP 중 분석·영속 유지, emit 만 차단)
- selection 안전게이트 복원 — 섹터 cap + conviction floor (engine.py)
- 뉴스 기사 본문 크롤 배선 (news_pipeline)
- scheduler retry + KIS_GATEWAY_URL fail-fast

### 2.4 KODEX 200(069500) 트레일링 검증
평단 116,139 / high_watermark 124,130 / trailing_active. 동작 확인 — 트리거 120,406원.

### 2.5 선정 아키텍처 결정 — `.ai/decisions/2026-05-22-selection-architecture-decision.md`
v3 의 LLM-at-core(매 scout 실행 screening 코드 통째 생성, 138run 전부 distinct
code_hash, 출력 Jaccard 0.317) 폐기 확정. 외부 검토 2건(Gemini·Claude Web) 수렴.
- **(a) 결정론 코어 복원 = Phase 1 (완료)**
- (b) 버전고정 codegen = 기각
- **(d) LLM 별도 측정 채널 = Phase 2 (확정, 트리거 대기)**

### 2.6 Phase 1 (a) — **배포 완료·검증됨** (commit a65db2e~3ccf097, main HEAD 3ccf097)
v2 결정론 선정 코어 포팅: `slow_loop/scout/{quant,enrichment,selection,deterministic_scout}.py`
신규(1,640줄) + `pipeline.py` 통합(−198/+84). LLM codegen·scout shadow 라이브 경로
은퇴. **선정 경로 LLM 호출 0회.** v2 per-종목 LLM(±15 clamp)은 무효용(+1.12)이라 미포팅.
신규 테스트 78, 전체 suite 1,388 pass. SQL 컬럼 7테이블 실DB 검증 완료.

### 2.7 daily_prices 픽스
`collect_full_market_data`(top300 일봉 운영 수집자)가 시드 placeholder 로 disabled
였음(승급 누락 — 사고 아님). 라이브 scheduled_jobs 활성화 + days30→45, redundant 한
`collect_daily`(5종목) disable. `seed_scheduled_jobs.py` 정합.

## 3. 현재 상태

- **main HEAD `3ccf097`**, push·배포·검증 완료. MS-01 19 컨테이너 healthy.
- v3 선정 = 결정론 quant 코어 (첫 결정론 scout run 은 다음 거래일 08:30 KST).
- daily_prices: `collect_full_market_data` **16:00 KST 첫 backfill 예약** (5-22 금 장후).
- 브랜치 `step3-burndown`·`phase1-deterministic-selection` 은 main 에 머지됨 — 삭제 가능.
- v2 MariaDB `prime-jennie-mariadb-1` (MS-01 port3307) — teardown 후 stop 됨, 필요시 재기동.

## 4. 다음 단계

**다음 세션 주제 (사용자 지정 — 2026-05-22 세션 종료 시): `dashboard` 수정.**
구체 수정 범위는 다음 세션 시작 시 사용자에게 확인할 것.

이월 항목:

1. **16:00 KST 후 daily_prices 검증** (orchestration 영역, 이번 세션 종료 후 시점) —
   `collect_full_market_data` 첫 run 결과 확인: 124 정지종목 갱신 / 04-20·04-21 포함 /
   top300 커버리지. 16:00 cron 실패 시 수동 트리거.
2. **첫 결정론 scout run 관찰** — 다음 거래일 2026-05-25(월) 08:30 KST. 풀 universe 로
   도는지, 후보 수·섹터 분산·conviction 분포 확인. (5-23 토·5-24 일 휴장.)
3. **Phase 2 (d)** — LLM 별도 측정 채널. 트리거: (a) 라이브 + 결정론 코어 outcome
   baseline 축적. 그 시점에 cap·shadow 설계 + 착수 재확인. 트리거 전 착수 금지.
4. **사전 부채 정리** (step 3 무관, 별도): stale 테스트 2건(`test_bar_engine`
   ::test_indicator_provider_adapter, `test_prompts_outcomes`::test_prompt_version_bumped_to_v0_6),
   `test_pipeline_e2e_real.py` 3 skip(은퇴 codegen-sandbox 경로 — 삭제/재작성 결정),
   dead code(`comp.screening`·shadow_orchestrator 의 scout 용도).
5. **thesis_aware_hold 로드맵 재검토** — 결정론 scout 는 `thesis_spec` 을 emit 하지
   않음(`deterministic_scout._to_screening_candidate` 가 `thesis_spec=None`). thesis
   gate(5-29 예정, `project_thesis_gate_deferred_2026_05_22`)는 LLM scout 의 thesis_spec
   반환을 전제했으므로 — 이 아키텍처 결정으로 무효화/재설계 필요.

## 5. 중요 참고사항

- **3자 프레임**: "v3 가 검증된 v2 보다 못하다"는 전제는 틀림 — v2-native 도 2주·미검증.
  어느 최근 세대도 길게 검증된 적 없음. Phase 1 의 목표 = 처음으로 "잘 설계되고+검증된"
  상태에 도달.
- **선정 경로 결정성**: 종목 채점·선정 단계 LLM 호출 0. macro regime·뉴스 감성은 상류
  LLM 산출 *데이터*로서 결정론 스코어러가 입력 소비 (v2 와 동일 구조).
- step3·phase1 모두 **장중 배포** — 사용자 명시 승인(KOSPI 횡보장). Phase 1 첫 배포는
  Docker Hub 520 으로 실패했으나 pull 단계 원자적 실패라 라이브 무손상, 재실행 통과.
- v2 포팅 충실성: `quant.py` V2_WEIGHTS 합 110/캡 100 quirk 는 faithful port 로 그대로
  이식(주석 명시) — 가중치 수정은 별도 follow-up.
- 069500(KODEX200)·241560(두산밥캣)·105560(KB금융) 3종목 v3 trailing 관리 중.
- 결정 근거 전문: `.ai/decisions/2026-05-22-selection-architecture-decision.md` (외부
  검토 응답 부록 포함), 검토 의뢰서 `.ai/reviews/2026-05-22-llm-at-core-review-request.md`.

## 6. 참조
- 직전 세션: `.ai/sessions/session-2026-05-18-0001.md`
- 분석: `.ai/analyses/2026-05-22-v2-teardown-*.md`, `2026-05-22-v3-assessment-*.md`
- 결정: `.ai/decisions/2026-05-22-selection-architecture-decision.md`
- 글로벌 메모리 갱신: `prime-jennie-family.md` (v3 선정 아키텍처 결정 반영 — 본 세션 종료 동기화)
