# Session Handoff - 2026-04-17-14:22

## 작업 요약 (What was done)

### 1. 메모리 인프라 정리 — youngs7596/youngs75 분리 정책 박제
커밋 `3861c80` (AGENTS.md), 글로벌 메모리 양쪽 push 완료.

문제 진단:
- `~/.claude/global-memory-youngs7596/`의 remote `youngs7596/claude-global-memory`가 `git pull` 시 404. 원인은 git credential helper가 active gh 토큰을 사용하는데 세션 시작 시 `youngs75`가 active였기 때문 (private repo에 youngs75 토큰으로 접근 → 404).
- 별도 issue: 사용자가 두 계정에 각각 메모리 repo를 분리해서 운영 중 (`youngs75/claude-global-memory` 존재, `youngs7596/claude-global-memory` 존재). 두 repo 내용은 명확히 분리됨 (apt vs prime-jennie). 섞이지 않음.

조치:
- 정책 명시: youngs75 = 사내 교육(apt-family/portal-infra), youngs7596 = 개인(prime-jennie family). 교차 저장 금지.
- `prime-jennie-family.md` (youngs7596 측), `apt-family.md` (youngs75 측) 양쪽에 정책 박제 (`a3b5d13`, `3a4ffba`).
- 본 repo `AGENTS.md` Memory Sync 섹션에 정책 + auth switch 선행 명시 (`3861c80`).
- 로컬 youngs75 메모리 클론 신설: `~/.claude/global-memory-youngs75/`.

### 2. Phase 1 Track D 완료 — Screening Executor
커밋 `992a0be`. SCOUT_CODE_GENERATION §6 충실 구현.

- **`prime_jennie_runtime/screening_executor/`** 4 모듈:
  - `allowlist.py`: AST 기반. ALLOWED_MODULES 정확/하위 prefix만 통과. `__import__`/`eval`/`exec`/`compile` 호출 거부. Relative import 거부. SyntaxError → `syntax_error` violation.
  - `executor.py`: in-process exec. allowlist → compile → exec → screen 함수 확인 → 호출 → 반환 검증 → ScreeningCandidate 직렬화. CANDIDATE_HARD_CAP=20에서 잘라냄. `_stdio_main`이 stdin→stdout JSON 프로토콜 (컨테이너 진입점).
  - `adapter.py`: ScreeningToolAdapter — `subprocess`/`docker` 두 backend. ScreeningToolAdapterStub과 동일한 `async def invoke(code, context)` 시그니처 → drop-in 가능. `run()`은 raw ScreeningResult 반환 (테스트/observer용). docker argv는 `--network=none --read-only --memory=4g --cpus=2 --cap-drop=ALL --security-opt=no-new-privileges:true --user=1000:1000`. timeout시 child kill.
  - `schemas.py`: ScreeningResult (ok/error/details/exec_time_ms/truncated). ErrorCode enum 8종.
- **`Dockerfile.screening`**: executor 모듈 ENTRYPOINT, slow_loop.scout.schemas만 의존하도록 최소 복사.
- **`docker-compose.yml`**: 영구 서비스 X (build-only profile). 실 실행은 ScreeningToolAdapter가 매번 `docker run --rm -i ...`로 ephemeral spawn.
- **테스트 +55**:
  - `test_allowlist` (25): M01~M07/M11/M12 + 화이트리스트 통과 12종 + syntax error
  - `test_executor` (15): S03~S09/S13 + happy/runtime/invalid_return_type 시나리오
  - `test_adapter` (15): subprocess 실 spawn happy/timeout/violation, docker argv, stdout 파싱, FileNotFoundError 케이스
- M08 timeout은 `subprocess` backend로 1건 검증. M09 OOM/M10 fork bomb은 컨테이너 격리 e2e 영역이라 Phase 1 미작성.

### 3. Phase 1 Track E 완료 — News Pipeline KOR
커밋 `efd5d4d`. v2 `prime_jennie/services/news/` 포팅. 외부 의존(Naver/EXAONE/kure-v1/Qdrant)은 Protocol + Stub로 추상화 — 실 어댑터는 Phase 2에서 v2 코드 그대로 포팅 (재작성 금지 원칙).

- **`prime_jennie_runtime/news_pipeline_kor/`** 7 모듈:
  - `models.py`: NewsArticle / SentimentScore (label = positive/neutral/negative) / NewsEmbedding / NewsScoreSnapshot (Pydantic)
  - `dedup.py`: v2 fingerprint 호환 (`md5(text.strip().lower()).hex[:12]`). 3일 윈도우. NewsDeduplicator Protocol + InMemoryDeduplicator + RedisDeduplicator (v2 키 스킴 동일).
  - `crawler.py`: NewsCrawler Protocol + StubCrawler + `make_dummy_article` 헬퍼 (테스트 fixture용 결정론적 article_id).
  - `sentiment.py`: SentimentAnalyzer Protocol + StubSentimentAnalyzer (키워드 hit count 기반 결정론, ±1 클램프, ±0.2 임계로 라벨링) + Embedder Protocol + StubEmbedder (article_id ord 합 기반 결정론 벡터, dimension 기본 8).
  - `storage.py`: SentimentRepo Protocol + InMemorySentimentRepo (ticker → article_id → SentimentScore) + VectorStore Protocol + InMemoryVectorStore (cosine top-k, ticker filter). `lookback_since(now, hours)` 헬퍼.
  - `pipeline.py`: NewsPipeline.run_cycle(universe) → CycleStats. crawl → dedup → analyze (asyncio.gather) → embed (선택) → store. crawler 예외는 stats.errors로 캡처, analyzer 부분 실패는 개별 항목만 스킵.
  - `scout_feeder.py`: NewsPipelineScoutFeeder — Track B `NewsScoreFeeder` Protocol 실 구현. SentimentRepo 기반 ticker별 최근 N시간 평균 score / 최신 timestamp / staleness_hours. 빈 ticker는 score=0, count=0, staleness=lookback_hours로 명시 표기.
- **테스트 +34**:
  - `test_dedup` (5): v2 fingerprint 호환, URL 정규화, 3일 윈도우 경계
  - `test_sentiment` (8): 키워드 라벨링, ±1 클램프, embedder 결정성
  - `test_storage` (8): upsert/lookback/top-k 코사인, ticker 필터
  - `test_pipeline` (6): happy/dedup 두 사이클/universe 필터/crawler 실패/analyzer 부분 실패/임베더 disabled
  - `test_scout_feeder` (7): Protocol 적합성 (`isinstance(feeder, NewsScoreFeeder)`), 평균/staleness/lookback/clamp/independence

### 변경 파일 총계
- 메모리 정책 (youngs7596 a3b5d13, youngs75 3a4ffba, AGENTS.md 3861c80): +18 / +10 / +4 lines
- Track D 커밋 `992a0be`: 12 files, +1048 lines
- Track E 커밋 `efd5d4d`: 14 files, +1345 lines

## 현재 상태 (Current State)

- **prime-jennie-runtime**: **Phase 1 전부 완료** (Track A/B/C/D/E). **386 tests 통과, ruff clean**.
- **git**: `main` branch, `origin/main` 동기화 완료 (`efd5d4d` HEAD).
- **prime-jennie (v2)**: 변경 없음, `trading_flags:stop=1, pause=emergency_stop` 유지.
- **minyoung-mah**: 변경 없음, 0.1.0, editable install.
- **global-memory**: 양쪽 정상 push 완료.
  - youngs7596: HEAD `a3b5d13` (정책 박제 + Track A/B/C 완료 사실)
  - youngs75: HEAD `3a4ffba` (정책 박제)
  - **주의**: youngs7596 측 prime-jennie-family.md는 아직 "Track A/B/C 완료"로 적혀 있음. Track D/E 완료 사실로 업데이트 필요 (이 세션 끝에 sync 예정).

## 테스트 분포 (386 total)

| 영역 | 카운트 | 주요 커버리지 |
|---|---|---|
| Track A 기반 | 24 | infra (config/db/redis/observer), position_sheet v1.1 |
| Track B slow_loop/scout | 17 | schemas, code_hasher, role fast path, validators (S06/S07/S08), context builder, screening stub |
| Track B slow_loop/macro | 37 | discretize (MG15~MG22), closed_conditions (MG02), continuity (MG07), state_store (MG08), post_processor, role, context builder |
| Track B slow_loop/strategy | 25 | policy YAML, sheet_id, engine, publisher (DLQ) |
| Track B E2E pipeline | 4 | normal / macro_closed / hallucination / no_candidates |
| Track C kis_gateway | 45 | market_hours, token_manager, rate_limiter, circuit_breaker, kis_api (respx), streamer, poller, server |
| Track C fast_loop/risk_throttle | 38 | 5단계 전이, SOX, 회복 지연, min() 역전 방지 |
| Track C fast_loop/exit_evaluator | 36 | 9종 × 18 + T15~T20 조합 6 + state 갱신 |
| Track C fast_loop 통합 | 18 | kis_client, entry/exit executor, position_tracker, consumer, tick_loop |
| Track C fast_loop E2E | 3 | sheet→entry→tick→exit, IntradayRiskThrottle Protocol 회귀 |
| Track C telegram_bot | 15 | bot, formatters, consumer |
| **Track D screening_executor** | **55** | **allowlist M01~M12, executor S03~S09/S13, adapter subprocess/docker** |
| **Track E news_pipeline_kor** | **34** | **dedup v2 호환, sentiment/embedder, storage, pipeline, scout_feeder Protocol** |

## 다음 할 일 (Next Steps)

### Phase 1 외 / 즉시 가능
1. **Slow loop pipeline에 real feeder 주입 시도**:
   - `StubNewsScoreFeeder` → `NewsPipelineScoutFeeder(sentiment_repo=...)` swap E2E 테스트 추가
   - `ScreeningToolAdapterStub` → `ScreeningToolAdapter(backend="subprocess")` swap E2E 테스트 추가
   - 둘 다 시그니처 호환 — 의존성 주입 지점 1개 변경으로 가능

### Phase 1 잔여 (선택)
2. **v2 데이터 마이그레이션 스크립트** (Track E 일부)
   - `migrations/002_legacy_namespace.sql`
   - `migrations/migrate_signal_logs.py` (v2 MariaDB → v3 Postgres `legacy_signal_logs`)
   - `migrations/migrate_trade_logs.py`
   - `migrations/migrate_daily_quant_scores.py`
   - `migrations/verify_migration.py` (row count + hash)
   - 외부 v2 DB 접속이 필요한 작업이라 Phase 1에서는 skeleton만, 실 실행은 영석 직접

### Phase 2 (Track C 미완 + 운영)
3. Telegram 양방향 명령 (`/stop`, `/pause`, `/liquidate`, `/dryrun`, `/status`) — v2 `telegram/handler.py` 744줄 포팅
4. Control UI `control.commands` pub/sub consumer
5. KIS Gateway DB 폴백 (Track E의 `legacy_daily_prices` 테이블 활용)
6. Naver Finance Crawler 실 구현 (v2 `infra/crawlers/naver_news.py` 포팅 → `news_pipeline_kor.crawler`)
7. EXAONE 4.0 Q8 sentiment adapter (LiteLLM 경유) → `news_pipeline_kor.sentiment` 실 구현
8. kure-v1 embedder + Qdrant adapter → `news_pipeline_kor.storage` 실 구현 (httpx)
9. Telegram bot token 실계좌 설정 + `TELEGRAM_DRY_RUN=false`
10. KIS paper account 토큰 발급 + smoke

## Context for Next Session

세션 시작 시 먼저 읽을 것:
- `AGENTS.md` (루트) — Track 경계, **Memory Sync (auth switch 선행 명시)**
- `prime_jennie_runtime/AGENTS.md` — 패키지 지도
- 해당 영역의 서브폴더 `AGENTS.md` (`screening_executor/`, `news_pipeline_kor/` 추가)
- `docs/prime_jennie_v3_phase0_design.md` — §8 Phase 1 로드맵
- `~/.claude/global-memory-youngs7596/prime-jennie-family.md` — 장기 기억 (auth switch 후 git pull)
- 이 세션 파일 (`.ai/sessions/session-2026-04-17-14-22.md`)

Phase 2 시작 시 참고:
- `/home/youngs75/projects/prime-jennie/prime_jennie/services/news/collector.py` — Naver crawler 실 구현 v2 원본
- `/home/youngs75/projects/prime-jennie/prime_jennie/services/news/analyzer.py` — EXAONE sentiment v2 원본
- `/home/youngs75/projects/prime-jennie/prime_jennie/services/news/archiver.py` — Qdrant archiver v2 원본
- `/home/youngs75/projects/prime-jennie/prime_jennie/services/telegram/handler.py` — Telegram 양방향 v2 원본

## 핵심 결정사항 (Key Decisions)

### Track D
- **executor는 in-process + subprocess + docker 3-tier 격리**: 단위 테스트는 in-process, CI/dev는 subprocess (실 timeout 동작), 운영은 docker. 모두 동일 코드 (executor.py)가 진입점.
- **Pipeline 시그니처 호환 우선**: ScreeningToolAdapter가 ToolAdapter Protocol(`call`/`arg_schema`)을 구현하지 않음. ScreeningToolAdapterStub의 `async def invoke(code, context)` 시그니처를 그대로 노출 — 기존 pipeline 변경 없이 swap 가능. ToolAdapter conformance는 Phase 2 (Orchestrator 경유 호출 시).
- **검증/dedup/cap 일부는 validator 책임**: executor는 §6.4 "21+ → 상위 20개" 까지만 처리. universe 외/중복 제거는 slow_loop.scout.validators가 담당 (이미 구현된 책임 분리 유지).
- **adapter `_build_argv`는 외부 mockable**: 테스트에서 `adapter._build_argv = lambda: ["/bin/true"]` 식 직접 패치로 silent child / nonzero exit / FileNotFoundError 시나리오 검증.

### Track E
- **외부 의존은 모두 Protocol + Stub**: Naver 크롤러, EXAONE LLM, kure-v1, Qdrant 모두 Protocol + In-memory/Stub 구현. 실 어댑터는 Phase 2에서 v2 코드 그대로 포팅 (재작성 금지). 결정론 핵심 로직과 IO 어댑터를 분리.
- **단일 사이클 함수 채택**: v2의 3 스레드(collector/analyzer/archiver) + Redis Stream 분리 모델 → v3는 `NewsPipeline.run_cycle()` 단일 함수. 스케줄러/스레드는 운영 시 cron 또는 별도 서비스가 주기 호출. 테스트가 훨씬 간결.
- **dedup v2 fingerprint 호환 유지**: `md5(text.strip().lower()).hex[:12]` 그대로. v2와 v3가 같은 키 스킴 사용 가능 (동시 운영 시 중복 분류 일관성).
- **ScoutFeeder 빈 ticker 처리**: 기사가 없는 ticker도 dict에 명시 entry로 포함 (score=0, count=0, staleness=lookback_hours). Scout가 missing보다는 명시적 0건을 더 잘 다룬다는 가정.
- **score 클램프 + NaN 방어**: `_clamp(avg, -1.0, 1.0)` + `math.isnan(x): return 0.0` — 향후 가중치 적용 시 안전망.

### 메모리 인프라
- **두 GitHub 계정 통합 불가**: GitHub는 계정 병합 미지원. 옵션 검토(Org/이메일 통합/repo 이전)했지만 사용자 결정 = 계정 분리 유지 + `gh auth switch` 수동 전환. directory-based credential helper 자동화는 Phase 2 후보.
- **메모리 repo 계정별 분리 영구**: youngs75 = 사내 교육(apt 등), youngs7596 = 개인(prime-jennie). 교차 저장 금지. 양쪽 메모리 파일에 박제, 본 repo AGENTS.md에도 명시.

## 주의사항 (Warnings)

### GitHub 인증
- 현재 `gh auth` active: **youngs7596** (Track D/E push 후 그대로). minyoung-mah 작업 전환 시 `gh auth switch -u youngs75` 필요.
- 메모리 sync 절차: pull/push 전 반드시 `gh auth switch -u <해당 계정>` (private repo는 토큰 없으면 404).

### 미완 영역 (Phase 1 핵심 외)
- **v2 데이터 마이그레이션 스크립트 미작성**. Phase 0 design §13.2 산출물 (`migrations/migrate_*.py`, `verify_migration.py`)이 비어있음. v2 DB 접속이 필요한 작업이라 영석 직접 실행 영역.
- **Track D 컨테이너 e2e 미실행**. `docker compose --profile build-only build screening-executor`로 이미지 빌드 시도해본 적 없음. M09 OOM/M10 fork bomb 격리 효과 검증은 컨테이너 환경에서 별도.
- **Track E 외부 어댑터 미구현**. Naver crawler / EXAONE / kure-v1 / Qdrant 모두 Stub. 실 운영은 Phase 2.
- **Slow loop가 real feeder/adapter swap 미적용**. 코드는 swap 가능하지만 default는 여전히 Stub. E2E 테스트 추가 권장.

### 운영
- v2 `trading_flags:stop=1, pause=emergency_stop` 유지 중 (이란 전쟁 리스크).
- v3는 아직 실계좌 접속 금지. KIS paper account만.
- 장중(09:00~15:30 KST) v3 KIS Gateway 재시작 금지 (토큰 캐시 소실 시 rate limit 403).

### 테스트 환경
- 로컬 `uv run pytest -q` 386 녹색. ruff clean.
- 외부 의존성(docker daemon, Postgres, Redis, KIS API, Telegram, Qdrant) 없이 in-memory + fakeredis + respx + asyncio subprocess만으로 모든 테스트 실행 가능.
