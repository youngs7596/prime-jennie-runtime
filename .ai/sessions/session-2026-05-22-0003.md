# Session Handoff — 2026-05-22-0003

> 트리거: `세션 종료` 요청. 같은 세션 중간 핸드오프 `0002` 이후 daily_prices 16:00
> backfill 검증 → ETN 오염 발견 → fix·배포까지 이어진 작업을 반영한 **최종본**.
> `0002` 를 대체한다 (resume 시 이 파일만 읽으면 됨).

## 1. 프로젝트 개요

prime-jennie-runtime = v3 실행 엔진 (KOSPI 자동매매). slow loop(시간당 macro gate +
결정론 quant scout) + fast loop(실시간 틱 진입/청산). v3 compose 스택은 MS-01
호스트에서 실행 (SSH alias `prime-jennie` = 192.168.31.195). 상세 `AGENTS.md`.

## 2. 이번 세션 작업 내역 (전체)

### 2.1 dashboard ↔ 결정론 scout 정합 — **배포 완료** (runtime 77efcdb·d00abd5, control-ui 003f2aa)

2026-05-22 Phase 1(LLM codegen 폐기 → 결정론 quant) 후에도 `persist_scout_run` 이
거짓 LLM 메타(`model_used=deepseek-chat`, phantom `cost_usd`, `record_llm_call`)를
기록 → dashboard 가 허위 LLM 사용량 표시.
- `persist_scout_run` 결정론 전용화 (model_used/cost_usd NULL, LLM 통계 미적재)
- `dashboard/routers/scout.py` 스키마 재설계 (code_hash/code_text/model_used/cost_usd
  폐기 → scorer_version·summary·strategy_tags·runtime_seconds·factor_weights·context)
- `dashboard/routers/llm_stats.py` scout/scout_shadow 제거
- `tests/dashboard/conftest.py` migration 012 `context_snapshot_json` 누락 보정
- `slow_loop/AGENTS.md` 결정론 scout 반영
- control-ui: api.ts 타입, Scout.tsx(Shadow 섹션 제거 + Factor Weights/Context),
  Overview.tsx — 라이브 검증 완료

### 2.2 monitor balance polling 장 시간 인식 — **배포 완료** (runtime 76aa3b2)

monitor `/balance` poll 이 24h 30초 고정 → 장 외 낭비. 평가금액만 장중 현재가
연동되고 수량·예수금은 체결 시에만 변함.
- `LivePositionsPoller` 가 `MarketCalendar` 로 장중 30s / 장 외 300s 적용
- heartbeat TTL 적응적, `MONITOR_IDLE_POLL_INTERVAL_SEC` env (default 300)
- 검증: `/status current_interval_sec` 정규장 30 확인

### 2.3 daily_prices 16:00 backfill 검증 + ETN fix — **배포 완료** (runtime e7b2ed4)

- `collect_full_market_data` 2026-05-22 16:00 KST **정상 실행** (success, 44.7s,
  에러 0). daily_prices 가 거래일당 5종목 → 169 equities 로 확장, 04-20/04-21 포함.
- **발견**: top300 중 **131개가 ETN** (CD금리/레버리지/인버스). KIS 일봉 endpoint
  `/api/market/daily-prices` 가 ETN 에 HTTP 200 + 빈 리스트 반환 → 실효 커버리지
  top-169 equities 뿐. 핸들러는 예외만 `failed` 로 세어 `failed=0` 이 실태를 가림.
- **fix (e7b2ed4)**: universe SELECT 에 `AND stock_name NOT ILIKE '%ETN%'` + 빈
  응답 `empty` 카운터. ETF 는 일봉 정상 수집되므로 유지. 배포·이미지 검증 완료.
- 메모리 `project_universe_etn_pollution.md` 기록.

### 2.4 진단 — "Monitor poll failing" 알림 (코드 변경 없음)

`monitor/poller.py` 가 `/balance` poll 3회 연속(=90s) 실패 시 critical 알림. 실제
원인 = `httpx.ConnectError` (kis-gateway TCP 불가) = 주로 **배포 시 startup race**
(kis-gateway 에 healthcheck 없어 monitor 가 준비 전 poll). 양성·자가회복.

## 3. 현재 상태

- **runtime main HEAD `e7b2ed4`** (+ 이 핸드오프 docs 커밋). 배포·검증 완료.
  MS-01 전 컨테이너 healthy.
- **control-ui main `003f2aa`**, MS-01 수동 재빌드·배포 완료.
- 오늘 배포 4회 — 장중 3회(scout/dashboard, control-ui, monitor) + 장후 1회(ETN
  fix). 전부 사용자 명시 승인 (장중분은 KOSPI 횡보 근거).
- monitor: 장중 30s / 장 외 300s polling.
- daily_prices: 현재 169 equities/거래일 (ETN 오염 상태). **2026-05-25(월) 16:00
  `collect_full_market_data` 부터 ETN 제외 top-300 equities 수집** (e7b2ed4 적용분).
- 오늘 scout: 12:30 까지 LLM(v0.8), 13:30 부터 결정론(deterministic-quant-v2-port@1).

## 4. 다음 단계

1. **2026-05-25(월) 16:00 `collect_full_market_data` 결과 검증** — 로그
   `target=300 ... empty=N` (empty≈0 기대), `daily_prices` distinct 종목 169→~300
   증가 확인. (0002 의 "16:00 backfill 검증" 이월항목은 검증+ETN fix 까지 완료 —
   이제 월요일 run 결과만 확인하면 됨.)
2. **kis-gateway healthcheck 추가** — "Monitor poll failing" 배포 race 알림 제거.
   infra(Track A), kis-gateway 에 `healthcheck` + monitor `depends_on` 을
   `condition: service_healthy` 로.
3. **다음 거래일 결정론 scout 관찰** — 2026-05-25(월) 풀 cadence·후보 분포.
4. **stock_masters instrument-type 컬럼 추가** — ETN 종목명 필터의 정식 해법
   (마이그레이션 + `seed_stock_masters`/masters 동기화 job). 현재는 휴리스틱 필터.
5. **Phase 2 (LLM 별도 측정 채널)** — 트리거: 결정론 코어 outcome baseline 축적 후.
6. **사전 부채** — stale 테스트(`test_prompts_outcomes::test_prompt_version_bumped_to_v0_6`
   등), 은퇴 LLM codegen 파일 삭제(`scout/role.py`·`prompts.py`·`code_loop.py`·
   `code_hasher.py`·`screening_stub.py`).
7. **thesis_aware_hold 재설계** — 결정론 scout 는 `thesis_spec` 을 emit 하지 않음.

## 5. 중요 참고사항

- **배포 파이프라인**: push to main → `ghcr.yml`(빌드) → `deploy.yml`(MS-01,
  pull+up). **코드 변경이면 공유 패키지 구조라 fast-loop 포함 전 컨테이너 recreate**.
  `ghcr.yml` 은 `paths` allowlist(`prime_jennie_runtime/**` 등)라 `.ai/**` 문서
  push 는 빌드 미트리거. 상세 메모리 `project_deploy_pipeline.md` (이번 세션 갱신).
- **control-ui** 는 별 repo(`youngs7596/prime-jennie-control-ui`), CI 없음 — API
  계약 변경 시 MS-01 수동 `docker build` + `compose up -d --force-recreate control-ui`.
- **ETN 오염**: `stock_masters` 에 instrument-type 컬럼 없음, `market` 전부 `KOSPI` —
  top-N-by-market_cap universe 선정 시 ETN 오염 전제. `project_universe_etn_pollution`.
- MS-01 SSH alias `prime-jennie`. postgres: user `pj_admin` / db `prime_jennie_v3`.
- `test_prompt_version_bumped_to_v0_6` 실패는 이번 변경과 무관한 기존 stale —
  전체 suite 1건 fail 은 정상.

## 6. 참조

- 같은 세션 중간 핸드오프(대체됨): `.ai/sessions/session-2026-05-22-0002.md`
- 그 이전 세션: `.ai/sessions/session-2026-05-22-0001.md`
- 커밋: runtime `77efcdb`·`d00abd5`·`76aa3b2`·`e7b2ed4`, control-ui `003f2aa`
- 메모리: `project_universe_etn_pollution.md`(신규), `project_deploy_pipeline.md`(갱신)
- 결정: `.ai/decisions/2026-05-22-selection-architecture-decision.md`
