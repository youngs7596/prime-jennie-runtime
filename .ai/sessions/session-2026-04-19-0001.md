# Session Handoff — 2026-04-19-0001

## 프로젝트 개요

**prime-jennie-runtime** — v3 실행 엔진 (느린 루프 + 빠른 루프 + v2 포팅). 운영 상태
는 KIS paper + `trading_flags:stop=1` + `control.state:stop="real-mode-transition"`
(= bypass/stop 이중 차단 유지) 상태. MS-01 호스트에서 compose `--profile full` 로 운영.

자매 리포:
- `prime-jennie-control-ui` (React SPA, dashboard consumer)
- `prime-jennie` (v2, 참조 전용)
- `minyoung-mah` (harness 라이브러리, youngs75 계정)

Global memory: `~/.claude/global-memory-youngs7596/` (prime-jennie family 전용).

## 최근 작업 내역 (이번 세션)

### 1) 리포 메타 정비
- `docs/SESSION_HANDOFF_2026-04-18*.md` 7개 → `.ai/sessions/session-2026-04-18-000{1..7}.md`
  시간순 이관 + TIMELINE/README/REAL_MODE_CHECKLIST 링크 갱신 (`7cebd00`).
- `prime-jennie-control-ui/AGENTS.md` 신설 + `.ai/sessions/.gitkeep` (`c1707b5`).
- apt-family global memory (`youngs75/claude-global-memory`) 를 `~/.claude/global-memory-youngs75/`
  로 pull/정리 (세션 내 로컬 정돈).

### 2) 민지 리뷰 기반 조사 + 수정
민지의 `INVESTIGATION_TASKS_2026-04-19.md` (코드 기반 리뷰) 에 대해 DB + loki +
smoke 실측으로 교차 검증하고 필요한 수정만 적용. 상세는 `docs/INVESTIGATION_2026-04-19_REPORT.md`.

| 커밋 | 작업 |
|---|---|
| `32e6aeb` | F5 — allowlist 에 `ast.Attribute` + `FORBIDDEN_NAMES` + pickle 패턴 차단. M13~M17 테스트 10건 |
| `07791bc` | F3 — REAL_MODE_MIGRATION_CHECKLIST 에 Blocking Pre-check Gates G1~G4 |
| `2fec603` | F4 — Moby v24.0.7 default seccomp 포함 + `SCREENING_SECCOMP_PROFILE` env 기반 조건부 argv |
| `5f73a6f` | Scout primary: DeepSeek → **Claude Opus 4.7**. DeepSeek 은 shadow 로 전환 (Macro 와 동일 패턴). per-ticker 비용 없는 코드 생성자라 품질 우선 |
| `0be41c0` | Publisher FK race fix — `publish()` 내 `position_sheets` upsert 를 stream 발행 전 동기 실행 |
| `e803c83` | INVESTIGATION_2026-04-19_REPORT + G5 (Scout Opus 검증 게이트) |

### 3) 검증 smoke
- `scripts/smoke_slow_loop_once.py` 를 slow-loop 컨테이너에서 실행 (`docker cp` + `exec`)
- 결과: Opus/DeepSeek 4회 API 호출, position_sheets 10건 upsert, screening_candidates
  promoted 100%, 전략 다양성 (SECTOR_MOMENTUM/GAP_UP_REBOUND/EARNINGS_DRIFT), Opus Scout
  가설이 **이란전쟁·WTI·고변동성 장세를 즉시 인지해 수비적 필터 자동 적용**.

## 현재 상태

- **git head**: `e803c83` (MS-01 반영 완료, 컨테이너 재배포됨)
- **Stop flags**: `trading_flags:stop=1`, `control.state:stop="real-mode-transition"` — 둘 다
  truthy 로 판정되어 fast_loop 주문 경로 차단 유지
- **Env (slow-loop)**:
  - `MACRO_AUTO_OVERRIDE_DISABLED=1` (고변동성 장세에서 auto_override bypass, stop 과 세트)
  - `SCOUT_SHADOW_ENABLED=1` / `MACRO_SHADOW_ENABLED=1` (DeepSeek 거울 축적)
  - `ANTHROPIC_MODEL=claude-opus-4-7` (primary)
  - `SCREENING_SECCOMP_PROFILE=/home/youngs75/projects/prime-jennie-runtime/infra/screening/seccomp.json`
    (F4 seccomp 활성)
- **Test**: slow_loop + screening_executor + strategy 227/162/34 건 전부 green
- **KOSPI 시장**: 이란전쟁 뉴스로 ±5~8%/일 변동성. `kospi_20d_vol = 58%` 는 실측 정상값

## 다음 단계 (월요일 2026-04-20 이후)

- [ ] **08:30 KST 첫 정기 `slow_loop.scout_daily` 실행 관찰**
  - success 상태로 종료
  - `scout_runs.model_used = 'claude-opus-4-7'`, `metadata_json->shadow` 에 DeepSeek 기록
  - `macro_runs.auto_override = false` 기록 확인 (bypass 실작동 증거 = G2 만족)
  - `engine_error` 감소 여부 — FK fix 효과
- [ ] **Scout 가설 품질 관찰** — 장세 인지 여부 (WTI/변동성 언급, 수비 필터 유무)
- [ ] **hallucination rate 측정** (V6 재시도) — 14일치 쌓인 후 off_universe 비율
- [ ] **F6 (backtest stub) 재평가** — 정기 rejection_reason 분포 안정화되면
- [ ] **Scout/Macro shadow 비교 평가** — 1~2주 축적 후 gate 일치율, hypothesis 다양성,
  code diff 로 Opus vs DeepSeek 실 성능 차이 정량화 (LLMStats dashboard 활용)

## 중요 참고사항

### stop 해제 전 반드시
`docs/REAL_MODE_MIGRATION_CHECKLIST.md` **§0.1 Blocking Pre-check Gates** G1~G5
모두 green 확인. 특히:
- **G1**: `MACRO_AUTO_OVERRIDE_DISABLED` 제거/0 — 고변동성 장세에서 bypass 켠 채
  stop 풀면 자동 closed 방어 무력화됨 (실측: 58% vol 장세에서 bypass ON 상태로 10건 sheet
  발행 가능. stop=1 만이 유일한 방어)
- **G3**: seccomp 호스트 절대경로 env 주입 (이미 완료). `.env` 수정 시 반드시
  `docker compose --profile full up -d --force-recreate slow-loop` — `restart` 는 env 반영 안 됨
- **G5**: Scout primary=Opus 가 실제 작동 중인지 scout_runs 최신 row 로 확인

### 전제 오류 기록 (다음 리뷰어가 같은 함정에 안 빠지도록)
- **kospi_20d_vol 58% 는 계산 버그가 아님** — 실제 시장 변동성. 로그 수익률 미사용도
  평시 오차 작아 무의미
- **threshold 35% 는 재설계 불필요** — 58% 장세에 정당히 closed 트리거 작동
- **slow_loop 정기 실행은 살아있음** — 14일치 scout_runs 가 smoke 뿐이었던 것은 Macro
  closed 로 Scout phase 스킵 + 서비스 배포 직후라 샘플이 적었을 뿐
- **engine_error 15건은 대부분 FK race 의 catch-all** — StrategyEngine 자체 버그 아님 (이미 fix)

### MS-01 배포 파이프라인
`main` push → MS-01 `ms-01-v3` runner → `docker compose --profile full up -d --build`.
env 변경 필요 시 runner 가 자동 recreate 안 함 → 수동 `--force-recreate`.

### 참조 파일
- 상세 조사 리포트: `docs/INVESTIGATION_2026-04-19_REPORT.md`
- 민지 리뷰 원본: `../INVESTIGATION_TASKS_2026-04-19.md` (projects 폴더)
- Real mode checklist: `docs/REAL_MODE_MIGRATION_CHECKLIST.md` (G1~G5)
- 세션 타임라인: `docs/SESSION_HANDOFF_TIMELINE.md`

---
**세션 종료**. gh 활성 계정 youngs7596 유지.
