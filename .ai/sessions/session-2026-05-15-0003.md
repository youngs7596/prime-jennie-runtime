# Session Handoff — 2026-05-15-0003

> KST 12:00 ~ 14:35 약 2시간 35분. 직전 세션 5-15-0002 (audit 6개 fix) 의 운영 검증
> 시작 → **이전 fix 5종이 모두 잘못된 SQL 키로 무용지물이었던 사고 발견** → emergency
> hotfix → Scout overextension guards 도입 첫 단계 (G1 + G5).

## 프로젝트 개요

**prime-jennie-runtime** — v3 실행 엔진. 본 세션은 사용자의 근본 질문 ("scout 가 왜 상투에
물린 종목을 좋게 봤는가") 에서 출발해 **데이터 검증 → design → 코드 → emergency hotfix →
G1 outcome 피드백 도입 → G5 today_exit_cooldown 가드 도입** 4 commit + 1 design doc + 1
글로벌 메모리 업데이트. 사용자 결정 누적: `feedback_prompt_control_limit` 원칙 (prompt 의존
X, 결정론 layer enforcement) 강화.

## 핵심 발견 — emergency hotfix 사고

### 5-15-0002 의 5종 cooldown 가드 코드는 들어갔으나 SQL 키 잘못 → 작동 0건

`persistence.py:143` 의 `record_sell` 는 metadata 에 `{"exit_reason": reason}` 으로 저장.
그러나 같은 세션에 도입된 5 파일 (fast_loop/cooldown_check, slow_loop/strategy/engine,
coordinator/policies/recent_stoploss_cooldown, slow_loop/scout/context_builder, +
migrations/019 우리가 작성) 모두 `metadata_json->>'reason'` (잘못된 키) 으로 SELECT.

발견 경로: G1 outcome view 작성 후 5-15 손절 12건이 view 에 1건만 나옴 (manual_telegram
backfill 만 — 그것만 reason 키 anomaly) → executions 실제 metadata 조사 → exit_reason
키 확인 → 5종 가드 SQL 전수 검사 → 전부 잘못.

영향: 어제 (5-14) 같은 종목 다중 매수 (001440 09:30/13:31 등) 와 손절 직후 재매수 (5-14
066970 89분 후 재진입) 모두 본 사고로 인해 발생. handoff 의 "audit 6개 fix" 가 실은
**5종이 영구 false negative** 였음.

## 최근 작업 내역 — 4 commit (+ 1 ruff)

| commit | 영역 | 핵심 |
|---|---|---|
| `aefda5a` fix(guards) | 5 파일 SQL | metadata_json->>`reason` → `exit_reason`. + tests/integration/test_metadata_exit_reason_contract.py (writer/reader 정적 contract 검증, drift 시 fail) |
| `f4b5679` feat(scout) G1 | scout context | migrations/019 scout_outcomes_v1 view + ScoutContext.previous_outcomes + prompts v0.5→v0.6 outcomes 섹션 |
| `b895d78` fix(scout) | G1 SQL | asyncpg syntax (:days::int * interval) → make_interval(days => :days) |
| `048b915` feat(guards) G5 | strategy engine | today_exit_cooldown — sheet 발행 3c 단계. 같은 KST 거래일 청산 (익절/손절 무관) ticker reject |

5-15-0002 handoff 의 6 commit + 본 세션 4 commit = **5-15 하루 누적 10 commit**.

## 데이터 분석 — 사용자 질문 의 정량 근거

### 5-15 손절 10종목의 어제 (5-14) 종가까지 overextension 통계

10건 중:
- **60일 고가 95% 이상**: 5건 (003670 98.95%, 009540 95.56%/20d 99.41%, 012330 95.53%, 062040 97.83%, 402340 97.71%)
- 60일 고가 89% 이상: 9건
- 5일 누적 +20% 이상: 3건 (001440 +23.97%, 062040 +29.06%, 402340 +20.07%)
- 20일 누적 +25% 이상: 3건 (001440 +28%, 028050 +40.37%, 079550 +32.53%)

세 룰 (60d 고가 95% / 20d +25% / 5d +20%) OR 적용 시 10건 중 **8건 차단** (G2 future).

### Scout 반복 추천 패턴

| ticker | 5-13~5-15 시트 발행 횟수 |
|---|---|
| 003670 | 9회 |
| 001440 | 7회 |
| 009540 | 7회 |
| 006260 | 6회 |
| 079550 | 5회 |

가설이 거의 동일: "KOSPI 강세 + 반도체/IT 섹터 모멘텀(+36%) + 실적 호재". 5-15 11:10
시점 003670, 006260 또 추천 — **자기 추천이 오늘 09:00~11:13 사이 손절된 사실을 모름**.

### 7일간 같은 거래일 재매매 패턴 (G5 도입 근거)

| 패턴 | 건수 | net |
|---|---|---|
| 손절→재진입→손절 | 11 | -55%p (평균 -5%) |
| 익절→재진입→손절 | 2 | -9%p (011070 + 278470) |
| 익절→재진입→회복 | 2 | +7%p (5-08 278470, 001440 breakeven 후 늦은 익절) |
| net | | **+48%p 손실 회피** (보수 차단 압도적 유리) |

5-15 011070: 10:17 trailing_tp +3.66% → 10:30 재매수 @779,000 → 13:24 fixed_sl -4.11%.
**기존 (3b) recent_stoploss_cooldown 은 익절 제외라 못 막음.** G5 가 정공법.

## Sheet 발행 단계 가드 3종 (Strategy Engine `_evaluate_candidate`)

| 단계 | 가드 | 차단 조건 |
|---|---|---|
| 3 | duplicate_today | 같은 거래일 같은 ticker 시트 이미 발행 |
| 3b | recent_stoploss_cooldown | 24h 내 STOP_REASONS (fixed_sl/stop_loss/breakeven_stop) |
| **3c** | **today_exit_cooldown** | 같은 KST 거래일 sell 1건 이상 (익절/손절/manual 무관) |

## G1 outcome 피드백 (5-15 도입)

`migrations/019_scout_outcomes_view.sql` — `scout_outcomes_v1` view (position_sheets ×
executions LEFT JOIN, 분할 체결 가중평균 + 최근 30일).

`ScoutContext.previous_outcomes` (최근 7일, 50건 cap). prompts.py 의 v0.7 에 informational
섹션 추가:

```
- 손절율: 36/50 = 72% (by tag: EARNINGS_DRIFT 14/16, GAP_UP_REBOUND 2/4,
                       MEAN_REVERT_RSI 1/1, SECTOR_MOMENTUM 19/29)
- 평균 PnL: -2.37%
- 최근 손절 5건: 011070 -4.11%, 062040 -3.94%, 012330 -5.02%, 006260 -5.09%, 402340 -5.02%
```

**Strategy tag 별 손절율 새 시그널** — EARNINGS_DRIFT 14/16 = **87.5%**. 사용자 표현
"상투 추격" 의 정량 증거. G2 future 의 strategy_tag 별 차등 임계값 후보 (EARNINGS_DRIFT
는 60일 고가 90% 등 더 엄격).

## 현재 상태 (KST ~14:35)

### 코드
- HEAD `048b915`
- 영향 영역 67~72 tests pass, ruff 통과
- 신규 regression test: `tests/integration/test_metadata_exit_reason_contract.py` —
  writer/reader 키 정적 contract. drift 시 fail.

### 운영 (MS-01)
- 4 deploy 완료 (13:25 hotfix + 13:29 G1 + 13:38 G1 SQL fix + 13:57 G5)
- 컨테이너 전부 Up, 안정
- 가드 직접 호출 검증 완료 (recent_stop_loss_tickers 11, recently_exited_today_tickers 12)
- 14:30 scout cron 발화 — `macro_closed` (KOSPI -4% CRITICAL 지속) → scout LLM 미실행

### KOSPI / risk
- 오늘 누적 손절 11건 (handoff 후 011070 13:24 추가) + 익절 2건 + manual 1
- intraday throttle 0.00 지속
- 시트 발행 14:30 cron 까지 0건 (macro_closed)

### Migration
- 019 적용 완료 (DROP + 재적용 — exit_reason 키 fix 포함)
- pj_admin 권한 필요 (`POSTGRES_ADMIN_PASSWORD=dev_admin` 사용)
- deploy 자동 적용 안 함 (직전 세션 학습) — 본 세션도 수동 적용

## 다음 단계 (TODO)

### 5-18 (월) 시초 자연 검증 — 가장 큰 검증 이벤트
- [ ] 09:30 scout cron 첫 발화 시 `recently_exited_today_tickers` 비어있음 (어제 청산은 24h+) 확인
- [ ] `recent_stop_loss_tickers` 가 오늘 손절 11종목 + 다른 종목 포함 (24h window)
- [ ] scout prompt v0.7 의 outcomes 섹션이 LLM 입력으로 들어가는지 (scout_runs.prompt_version 확인)
- [ ] **LLM 행동 변화 관찰** — outcome 87% 손절율 본 LLM 이 같은 패턴 유지 vs 분산 시도
  - 시나리오 A (변화 X): `feedback_prompt_control_limit` 원칙 강화 → G2 결정론 차단 우선순위 상승
  - 시나리오 B (분산 시도): informational prompt 의 일부 효과 확인 — 그래도 enforcement 필요
- [ ] sheet 발행 시 today_exit_cooldown / recent_stoploss_cooldown reject 자연 발화 확인

### Phase 0 #1 (conviction-outcome correlation) — G1 데이터로 진척
- [ ] 1주 데이터 누적 후 strategy_tag × conviction × outcome 분포 분석
- [ ] EARNINGS_DRIFT 87.5% 손절 패턴이 conviction 분포 어디서 집중되는지

### G2 overextension validator (다음 우선순위)
- design 의 §4 임계값 (60일 고가 95% / 20일 +25% / 5일 +20%) — 5-15 데이터로 검증됨 (8/10 차단)
- **strategy_tag 별 차등 임계값** 검토 (EARNINGS_DRIFT 더 엄격)
- 구현 시점: 5-18 ~ 5-22 동안 시초 자연 데이터 1주 누적 후

### G3 시초 갭다운 + G4 시초 추격 손절 (디자인만, 구현은 추후)
- G3: fast_loop entry path — KIS snapshot 기반 갭 -3% reject
- G4: fast_loop exit_evaluator — 09:00~09:05 신규 손실 진입 즉시 손절 보류 (Phase 0 #2 데이터 후)

### 후속 검토 항목
- **익절 후 재진입 손실 패턴 누적 감시** — 011070 외 다른 케이스 (5-12 278470) 도 있음.
  G5 가 차단하지만 sample 더 모이면 별도 분석 필요.
- **deploy 가 cron 정각과 겹치는 timing 회피** — 오늘 13:25/13:29 deploy 가 13:30 cron
  과 충돌해 cron 1회 누락. 메모리 적재 대안: deploy.yml 에 5분 (cron tick) 회피 로직.
- **migration 자동 적용** — deploy.yml 에 step 추가 필요. 5-15 두 migration 모두 수동
  적용 (`POSTGRES_ADMIN_PASSWORD` 필요).

## 글로벌 메모리 갱신 완료 (`youngs7596/claude-global-memory`)

- `trading-domain.md` 갱신:
  - Cooldown 24h 가드 진화 (5-15 도입 + 키 버그 + fix + today_exit_cooldown 추가)
  - executions.metadata_json contract key = `exit_reason` 명시 + regression test 위치
  - Sheet 발행 단계 가드 3종 표 신규
  - G5 도입 근거 데이터 (1주일 11 vs 2 vs 2건)
  - design 링크 추가

## 중요 참고사항

### architectural 학습

1. **외부 contract 정적 검증의 가치** — `record_sell` 와 가드 SQL 5종의 키 contract 가
   검증 없이 drift. integration regression test (`tests/integration/test_metadata_exit_reason_contract.py`)
   가 미래 동일 사고 방지. **writer/reader 양쪽이 같은 키를 보는지 정적 assertion** 패턴
   은 다른 metadata 영역에도 일반화 후보 (provenance_json scout_run_id 등).

2. **데이터 기반 결정의 가치** — G5 도입은 임계값 추측이 아니라 1주일 outcomes 의 정확한
   카운트 (11 손절 재진입 / 2 익절 재진입 손실 / 2 회복) 로 net +48%p 산정 후 결정.
   `project_audit_2026_05_14` 의 "외톨이 패턴" 위험과 동일 — 결정론은 데이터로.

3. **manual trigger + 직접 호출 검증** — 14:30 cron 이 macro_closed 라 자연 검증 못 함.
   smoke_slow_loop_once.py 패턴 + 가드 직접 호출 (`PgActiveSheetChecker.has_recent_exit_today`)
   로 운영 환경 동작 100% 확인. 자연 검증 기다리지 않고 즉시 검증 가능한 패턴.

4. **fail-open 정책의 1차 효과** — G1 SQL bug 가 운영에 들어갔지만 fail-open 으로 의사결정
   영향 0 (빈 list 반환). 의사결정에 영향 주는 가드 vs 정보용 가드의 fail 정책 분리는 옳음.

### 사용자의 운영 결정 누적

1. **장중 deploy 4회 override 허용** (오늘 누적 6회) — intraday throttle 0.00 이라 매매
   영향 사실상 0. 다만 향후 룰은 준수.
2. **자연 검증 vs 즉시 검증 균형** — manual trigger 적극 사용 결정 (13:30 cron 누락 시).
3. **데이터 우선** — 사용자 직관 ("상투 추격") → 실측 데이터로 검증 → 정량 임계값 → 구현
   의 사고 흐름. prompt 변경이 아닌 결정론 코드 layer 일관.
4. **5종 가드 무용지물 사고 즉시 hotfix 결정** — 일반적 룰 (장중 deploy 금지) 보다 가드
   부재 위험이 더 큼.
5. **today_exit_cooldown 의 회복 케이스 기회비용 (5-08 278470 +3.86%) 감수** — net +48%p
   계산 보고 결정.

## 참조

- 직전 세션: `.ai/sessions/session-2026-05-15-0002.md`
- 본 세션 commit 4개 (위 표)
- design 신규: `.ai/designs/2026-05-15-scout-overextension-guards.md` (G1~G5)
- migration: `migrations/019_scout_outcomes_view.sql`
- regression: `tests/integration/test_metadata_exit_reason_contract.py`
- 글로벌 메모리: `trading-domain.md` 갱신 (cooldown 가드 진화 + 3종 가드 표 + G5 근거)

---

**세션 종료 (KST ~14:35)**. 다음 세션 시작 시:

1. AGENTS.md resume 절차 (글로벌 메모리 pull + 본 세션 읽기)
2. 5-18 시초 자연 검증 결과 분석 (위 TODO 5종)
3. G2 overextension validator 구현 결정 (strategy_tag 별 차등 임계값 포함)
4. Phase 0 #1 데이터 분석 (1주 후 권장)
