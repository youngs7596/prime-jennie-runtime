# Session Handoff — 2026-04-25-0003

## 프로젝트 개요

**prime-jennie-runtime** — v3 실행 엔진. 2026-04-25 (토) 세 번째 세션.

`session-2026-04-25-0002` (인프라/문서/세션 도구 정비 위주 — README v2 스타일 재작성, git
ownership 해소, Agent Teams 활성화) 종료 후, 같은 날 토요일 저녁 세션. **이번 세션의
무게중심은 단연 Phase 3 Stage 0 풀 구현** — 그동안 미생성이었던 `prime-jennie-meta`
리포를 처음부터 끝까지 만들고, 이어서 Stage 1 promotion 직전 후속 3건과 telegram
escalate hook 까지 마무리.

Agent Teams (Desktop 첫 시도) 로 다수 subagent 를 병렬 spawn 해 약 2~3시간 wall-time 에
6시간+ 분량 작업 완료.

## 최근 작업 내역

### 1) Phase 3 Stage 0 풀 구현 — Agent Teams 첫 시도

#### 1-1. Team 생성

`TeamCreate` 호출 → `prime-jennie-meta-stage0` 팀 생성 (config:
`C:/Users/ilhsr/.claude/teams/prime-jennie-meta-stage0/config.json`). Desktop 앱에서
`Agent` 도구의 `team_name`/`name` 파라미터 미노출이 확인됨 — 결과적으로 spawn 된
agent 들은 일반 subagent 로 동작했지만 **mailbox (SendMessage) 는 정상 작동** — 팀원이
보낸 메시지가 `<teammate-message>` 태그로 큐잉되어 일괄 전달됨. 즉 comms 레이어는
살아있고 spawn/UI 만 degraded.

#### 1-2. 4 팀원 spawn 결과 (병렬 처리)

| 팀원 | 산출물 | 리포 | 커밋 수 | 테스트 |
|------|-------|------|--------|-------|
| **adapter-builder** | BacktestToolAdapter + grid_search 드라이버 | runtime | 2 | +19 (38 total) |
| **meta-architect** | prime-jennie-meta 신규 GitHub 리포 + skeleton | meta (NEW) | 1 | 8 |
| **eval-builder** | Eval Pipeline Stage 0 (8 모듈) | meta | 6 | +25 (33 total) |
| **pr-builder** | PR Generator Stage 0 (8 모듈) | meta | 4 | +53 (86 total) |

**meta-architect** 의 첫 push 가 GitHub Actions CI 트리거 — 17초에 통과. eval+pr 두
번째 push 도 27초 통과. 양쪽 다 ruff format+check + pytest 모두 green.

### 2) prime-jennie-meta 리포 신규 생성 (영구)

- **Repo**: https://github.com/youngs7596/prime-jennie-meta (Private, owner youngs7596)
- **Local**: `/home/youngs75/projects/prime-jennie-meta`
- **첫 commit**: `a2fabd6` (skeleton)
- **현재 HEAD**: `c90c2e2` (Stage 1 prep 후 — 16 commits 누적)
- **CI workflow**: `.github/workflows/ci.yml` — ruff format check + check + pytest

#### 디렉터리 구조

```
prime-jennie-meta/
├─ pyproject.toml (minyoung-mah 0.1.8 + PyGithub 2.3 + sqlalchemy[asyncio] + asyncpg)
├─ AGENTS.md (권한 격리 + Stage 정책)
├─ README.md (272줄, v2 스타일 배지 + ToC)
├─ prime_jennie_meta/
│   ├─ __init__.py / __main__.py (eval / pr-generator sub-command)
│   ├─ infra/  ← architect 작품, immutable
│   │   ├─ config.py (Pydantic Settings 4 sections)
│   │   ├─ runtime_db.py (async read-only PG engine, postgresql_readonly=True)
│   │   ├─ logger.py (structlog JSON)
│   │   └─ observability.py (pj.meta.* namespace ObserverEvent emitter)
│   ├─ eval/
│   │   ├─ candidate.py (EvalCandidate, scout_prompt 한정)
│   │   ├─ metrics.py (PortfolioMetrics, FitnessVerdict)
│   │   ├─ report.py (EvalReport, diff_against_main 보관 — Stage 1 prep)
│   │   ├─ runtime_reader.py (async read-only PG SELECT)
│   │   ├─ baseline.py (production outcomes → baseline metrics)
│   │   ├─ replay.py (CandidateReplayer + BacktestAdapterProtocol duck-typed)
│   │   ├─ storage.py (SQLite, idempotent ALTER 마이그레이션)
│   │   └─ pipeline.py (EvalPipeline 오케스트레이터)
│   └─ pr_generator/
│       ├─ safety_gates.py (token scope + path allowlist + 5 forbidden 리포)
│       ├─ github_client.py (PyGithub async facade)
│       ├─ diff_applier.py (tempdir + git apply)
│       ├─ stage_policy.py (Stage 0 → always draft)
│       ├─ rate_limiter.py (KST 1/day + 3 open + revoke 컬럼 5 추가됨)
│       ├─ templates.py (PR title/body 템플릿)
│       ├─ generator.py (PRGenerator 9-step 오케스트레이터)
│       ├─ revoke.py (RevokeWatcher 48h 자동 revert) ← Stage 1 prep
│       └─ runtime_checkout.py (apply/ruff/pytest 3-stage dry-run) ← Stage 1 prep
├─ tests/ (111 tests, 모두 green)
└─ docs/ (META_DESIGN.md + STAGE_PROMOTION_POLICY.md + EVAL/PR_GENERATOR_SPEC.md)
```

### 3) Stage 1 promotion 직전 후속 3건 (commit `9222cd1` `6517ca3` `c90c2e2`)

`stage1-prep` subagent 가 처리:

#### 3-1. `revoke.py` — 48h 자동 revert 안전망
- `RevokeWatcher.watch_and_revoke()`: 머지된 PR 의 production outcomes (`pnl_pct_7d`, `win_rate_7d`) 가 `pre_merge_baseline` 대비 임계치 이상 악화 → 자동 revert PR 생성
- Merge commit 과 일반 commit 모두 fallback 처리 (`git revert --mainline 1` ↔ plain)
- `RateLimiter` 에 5 컬럼 추가 (`merged_at`, `merged_sha`, `pre_merge_baseline_json`, `revoked_at`, `revoke_pr_url`) — idempotent ALTER

#### 3-2. `runtime_checkout.py` — pre-PR sandbox dry-run
- 3-stage: `git apply --check` → `ruff check` → `pytest -x`
- 캐시 TTL 5분 — 동일 clone 재사용으로 후속 PR 빠르게
- 첫 stage 에서 실패 시 즉시 reject. 결과: `DryRunResult(passed, stage, details, duration_ms)`
- `PRGenerator` 에 optional 주입 — 미주입 시 skip (backward-compat). 새 status: `rejected_dry_run`

#### 3-3. EvalReport.diff_against_main 보관 + end-to-end `generate(candidate_id)`
- `EvalReport` 에 `diff_against_main: str` 필드 추가 (default empty)
- `EvalStorage` schema 마이그레이션 — 기존 SQLite 도 idempotent ALTER 검증 케이스 포함
- `EvalPipeline.evaluate()` 가 `candidate.diff_against_main` 을 report 로 carry-over
- `PRGenerator.generate(candidate_id)` 이 storage 에서 직접 diff 읽음. 외부 주입 없이 동작
- `__main__.py` 의 `--diff-file` 이제 optional

테스트 86 → 111 (+25). ruff clean.

### 4) Telegram Escalate Hook (commit `9b7e010` on runtime)

`telegram-escalate-hook` subagent 가 처리:

- **신규 모듈**: `prime_jennie_runtime/slow_loop/scout/observers/escalate_telegram.py`
- **클래스**: `EscalateTelegramObserver` — `pj.scout.escalate` 만 필터, 한국어 plain-text 알림 포맷, send 실패 시 swallow + 로그 (slow_loop pipeline 깨뜨리지 않음)
- **Wiring**: `slow_loop/app.py` 의 `_build_observer()` 가 `NullObserver()` →
  `CompositePJObserver(StructlogPJObserver(), EscalateTelegramObserver(bot))` 로 교체. Telegram
  설정 미존재 시 structlog-only fallback
- 8 신규 tests — 227 total slow_loop 테스트 green
- **알림 형식 한계**: code_loop.py 의 emission payload 에 `scout_run_id` / `code_hash` 부재 →
  최소 정보(attempts, last_error, KST timestamp)만 포함. 풍부한 알림 원하면 emission 확장
  필요 (별도 PR — 이번 작업은 "MUST NOT touch code_loop.py")

### 5) 작은 후속 — Docstring 수정 (commit `b8b3169`)

pr-builder 가 발견했던 `EvalCandidate.diff_against_main` docstring 모호함. 본 세션에서
team-lead (claude) 가 직접 수정 — Eval 단계 (traceability only) vs PR Generator 단계
(`git apply` 적용) 의 역할 분리를 명시적으로 적음.

### 6) Agent Teams 첫 시도 — 솔직 평가 (다음 세션 의사결정용)

**작동한 것**:
- `TeamCreate` ✅
- 5개 subagent 병렬/순차 spawn ✅ (각자 독립 컨텍스트, 깊이 있는 작업)
- Mailbox 메시지 큐잉 + 일괄 전달 ✅ (`<teammate-message>` 태그)
- 의존성 순서 (architect → eval/pr → stage1-prep) 수동 조정 ✅
- 토큰 비용 N배지만 사용자 MAX 200 구독이라 무관

**작동 안 한 것**:
- `Agent` 도구의 `team_name`/`name` 파라미터 미노출 → 일반 subagent fallback
- 팀 config.json `members` 배열 업데이트 안 됨
- Desktop UI 에 teammate 패널 / Shift+Down 사이클링 없음

**결론**: Desktop 에서 Agent Teams 의 본체 spawn/UI 는 미지원이지만 comms 만으로도
충분히 강력. 진짜 분할 창 + 실시간 panel 원하면 WSL 터미널 Claude Code 로 가야 함.
이번 세션의 작업량 보면 Desktop+subagent 패턴도 충분히 효율적.

## 이번 세션 커밋 요약

### prime-jennie-runtime (`claude/distracted-varahamihira-ee564b` worktree → origin pushed)

| 해시 | 작성자 | 요약 |
|---|---|---|
| `599ff9b` | team-lead | docs(readme): v2 스타일 재작성 + Scout 패러다임 전환 헤드라인화 |
| `9e5a253` | team-lead | docs(handoff): session-2026-04-25-0002 |
| `997eed8` | adapter-builder | feat(backtest): minyoung-mah ToolAdapter wrapping simulate_sheet |
| `63d238c` | adapter-builder | feat(scripts): grid_search driver |
| `9b7e010` | telegram-escalate-hook | feat(slow_loop/scout): telegram escalate hook |
| (다음) | team-lead | docs(handoff): session-2026-04-25-0003 |

### prime-jennie-meta (NEW repo, `main` → origin pushed)

| 해시 | 작성자 | 요약 |
|---|---|---|
| `a2fabd6` | meta-architect | feat: initial repo skeleton — Phase 3 Stage 0 |
| `a19f9bd` ~ `13dd69b` | eval-builder | 6 commits: Eval Pipeline Stage 0 (모델/메트릭/reader/baseline/replay/storage/pipeline + 25 tests + spec) |
| `e55effd` ~ `97d51cc` | pr-builder | 4 commits: PR Generator Stage 0 (safety_gates/github/diff/stage/rate/templates/generator/CLI + 53 tests) |
| `b8b3169` | team-lead | docs(eval): clarify diff_against_main role across stages |
| `9222cd1` | stage1-prep | feat(eval,pr_generator): persist diff_against_main on EvalReport |
| `6517ca3` | stage1-prep | feat(pr_generator): add RuntimeCheckout pre-PR sandbox dry-run |
| `c90c2e2` | stage1-prep | feat(pr_generator): add RevokeWatcher 48h auto-revert safety net |

총: runtime 5 + meta 16 = **21 commits 이번 세션**.

## 현재 상태 (2026-04-25 토 저녁)

### 코드
- runtime HEAD: `9b7e010 feat(slow_loop/scout): telegram escalate hook` (origin/branch pushed)
- meta HEAD: `c90c2e2 feat(pr_generator): RevokeWatcher` (origin/main pushed)
- runtime 879 + 8 신규 = **887 tests** (전체 green)
- meta 0 + 111 신규 = **111 tests** (전체 green)
- ruff format + check 양쪽 모두 clean
- 미커밋 변경: 이번 handoff 파일 1건 (commit 직후 0)
- 현재 브랜치 (runtime): `claude/distracted-varahamihira-ee564b` (worktree). main 미머지

### CI
- meta GHA: 2/2 push 모두 통과 (17초 + 27초)
- runtime: deploy.yml 트리거 안 됨 — README + scripts + slow_loop 변경은 `paths` 에
  포함되지만 worktree 브랜치는 main 이 아니라 deploy 안 일어남

### 운영 (MS-01)
- 17 컨테이너 healthy 유지 (이번 세션 이미지 변경 0 — runtime 변경분 main 미머지)
- trading_flags:stop=1 유지
- 다음 평일 04-27 월 08:30 KST cron 부터 scout 검증 루프 + escalate→Telegram hook 첫
  실 적용. 단 **runtime worktree 브랜치를 main 에 머지해야 escalate hook 이 활성화됨**.
  안 하면 04-27 월에는 검증 루프 (이전 세션 작업) 만 돌고 텔레그램 알림은 못 받음

### Stage 0 → Stage 1 전환 시 외부 작업 (Phase 3.1+)

stage1-prep 이 명시한 5 외부 의존:

1. **`record_merged` 호출 webhook** — GitHub merge webhook 수신부 신설 (Stage 1+ 별도 서비스). 현재는 외부 호출 가정 only
2. **RevokeWatcher cron schedule** — 시간당 1회 등 결정 필요 (k8s job / GHA cron / runner)
3. **RuntimeCheckout first-run latency** — depth=1 clone + 첫 pytest 가 분 단위. cron 으로 prewarm vs PR 발생 시 lazy 둘 중 결정
4. **baseline 자동 측정** — `pre_merge_baseline` 의 윈도우/지표 자동 산출. 현재는 외부에서 dict 로 주입 가정
5. **`_fetch_original_title`** — 현재 `"candidate <id> (<sha>)"` stub. `GitHubClient.get_pull_request(url)` 추가하면 실 PR title 인용 가능

## 다음 단계 (TODO)

### 즉시 (다음 평일 첫 가동, 2026-04-27 월)
- [ ] **runtime worktree 브랜치 → main 머지** (이번 세션의 모든 runtime 변경분 — backtest adapter, grid_search, telegram escalate hook 활성화). PR 또는 직접 머지. main push 시 ghcr.yml 트리거 → MS-01 자동 재기동
- [ ] **04-27 월 08:30 KST** 첫 실운영 scout 결과 모니터링. Loki 에서 `pj.scout.code_attempt` / `pj.scout.code_repeated` / `pj.scout.escalate` 검색. Telegram 으로 escalate 알림 받는지 확인 (만약 escalate 발생 시)
- [ ] 04-27 발행 시트 수가 0 이 아니면 핵심 목표 달성

### 짧은 후속 (Phase 3 외 단발성, 0001/0002 이월 잔여)
- [ ] **`scout_runs.candidates_count` 의미 수정** — 현재 LLM 예측치라 모니터링 속음. `len(actual_candidates)` 로 교체 + fallback 제거. `persistence.py:343` 근처
- [ ] **control-ui Scout 페이지 attempts 노출** — 별도 리포 작업 (`prime-jennie-control-ui`). dashboard router + UI 시도별 상세 (번호/에러/code_hash/후보 수)
- [ ] **Legacy 테이블 drop** — `daily_macro_insights` + `legacy_*` 4개 + `global_macro_snapshots`. 마이그레이션 013 추가, 의존성 grep 선행
- [ ] **BOK_ECOS_API_KEY** 설정 → briefing USD/KRW 복원. 키 발급은 사용자 작업
- [ ] **Eval fixture 15 → 30~50건 확장** + impact 기준 프롬프트 튜닝
- [ ] **code_loop.py 의 escalate emission payload 확장** — `scout_run_id`, `code_hash` 시퀀스 포함. 그러면 telegram alert 가 풍부해짐 (현재는 attempts + last_error 만)

### Phase 3 후속 (Stage 0 → Stage 1 promotion 준비)
- [ ] **GitHub merge webhook 수신부** — webhook 서비스 신설 (혹은 GHA workflow_dispatch + RateLimiter.record_merged 호출 lambda)
- [ ] **RevokeWatcher cron** — 시간당 1회 GHA 또는 k8s job
- [ ] **RuntimeCheckout prewarm vs lazy 결정** — first-run latency 정책
- [ ] **baseline 자동 측정** — `pre_merge_baseline_json` 자동 산출 윈도우/지표
- [ ] **`_fetch_original_title` 실 PR title** — `GitHubClient.get_pull_request(url)` 추가
- [ ] **Stage 0 첫 실 evaluation** — candidates 2-3주 누적 (~05-05 충분) 후 첫 EvalReport 생성 + 첫 draft PR 한 번 열어보기

## 중요 참고사항

### 이번 세션의 Agent Teams 패턴 (재현 가능)

다음 세션에서도 같은 패턴 활용 가능:
1. `TeamCreate` 로 팀 생성 (선택사항 — subagent 만으로도 동작)
2. `Agent` 도구로 subagent spawn — Desktop 에선 일반 subagent 로 동작하지만 SendMessage 큐잉 작동
3. 의존성 있는 작업은 순차 spawn, 독립 작업은 병렬 message 안에서 spawn
4. team-lead (claude) 가 중앙 조율, 결과 종합 후 push

### prime-jennie-meta 리포 운영 규칙

- **Token scope**: meta 의 GitHub PAT 는 `youngs7596/prime-jennie-runtime` 에만 write 가능해야 함. `safety_gates.py` 가 startup 시 검증 — 자기 리포 (meta) / minyoung-mah / v2 / control-ui / claude-global-memory 어디라도 write 가능하면 즉시 abort
- **Stage 정책**: 현재 Stage 0. 모든 PR draft. 자동 머지 없음. Stage 1+ 은 운영 1~2 개월 데이터 누적 후 별도 결정
- **prime-jennie-runtime dependency**: `pyproject.toml` 에 의도적으로 미포함 — 운영 환경에선 git+ssh URL 수동 wiring. dev 환경에선 path install (`pip install -e ../prime-jennie-runtime`)

### Worktree 브랜치 머지 전략

이번 세션 종료 시점 runtime 의 5 commits 가 worktree 브랜치 (`claude/distracted-varahamihira-ee564b`) 에 있고 main 미머지. 다음 세션 진입 시 옵션:

**A. PR 로 올리기** — 리뷰 흔적 남기고 싶으면. `gh pr create --title "Phase 3 Stage 0 + escalate hook" --body "..."`. 다만 self-review 라 형식적
**B. 직접 머지** — 가장 빠름. `git checkout main && git merge claude/distracted-varahamihira-ee564b && git push origin main`. main push 시 ghcr.yml 트리거 → MS-01 재기동 (이미지 안 변하는 docs 만 빼고). 주의: README 변경 (599ff9b) 은 ghcr paths 에 없으니 무시되지만 backtest/scripts/slow_loop 변경은 트리거됨

### 글로벌 메모리 sync

이번 세션 글로벌 메모리 (`prime-jennie-family.md`) 추가/변경 없음 — Phase 3 의 영구
사실들 (prime-jennie-meta 리포 신설, Stage 0/1 정책 등) 은 **해당 리포 자체** 와
**docs/META_DESIGN.md** 에 있어서 글로벌 메모리는 손 댈 일 없었음. 미래 세션 개시
때 prime-jennie-family.md 의 "Repo 구조 (4개)" 표가 stale 한지 검토 필요 — meta 리포가
"Phase 3 미생성" 으로 적혀있을 수 있음.

→ **개선 액션 (다음 세션)**: prime-jennie-family.md 의 4 repo 구조 표 갱신 (meta 가
이제 존재). 한 줄 수정.

### Communication Style 4 원칙 (글로벌 메모리)

정직 / 돌려말 금지 / 코드 용어 금지 / 평문 위주.
`~/.claude/global-memory-youngs7596/communication-style.md`

## 참조

- 직전 세션: `.ai/sessions/session-2026-04-25-0002.md` (README v2 재작성 + Agent Teams Desktop 활성화 + git ownership)
- 글로벌 메모리:
  - `~/.claude/global-memory-youngs7596/prime-jennie-family.md` (4 repo 표 갱신 필요 — meta 신설 반영)
  - `~/.claude/global-memory-youngs7596/communication-style.md`
  - `~/.claude/global-memory-youngs7596/trading-domain.md`
  - `~/.claude/global-memory-youngs7596/minyoung-mah.md`
- 신규 리포: https://github.com/youngs7596/prime-jennie-meta
- 신규 docs (meta repo 안):
  - `docs/META_DESIGN.md` — 4 pillar / data flow / 권한 격리 다이어그램
  - `docs/STAGE_PROMOTION_POLICY.md` — Stage 0/1/2/3 승격 + 강등 조건
  - `docs/EVAL_PIPELINE_SPEC.md` — Stage 0 9 섹션 채움
  - `docs/PR_GENERATOR_SPEC.md` — Stage 0 9 섹션 채움

---

**세션 종료**. Phase 3 Stage 0 self-evolving 토대 완성 + Stage 1 prep 안전망 구축 +
운영 임박한 telegram escalate hook 통합. Agent Teams Desktop 첫 시도에서 5
subagent 병렬로 200+ tests 와 30+ commits 생산. 다음 세션 진입 시 runtime worktree
브랜치 main 머지가 04-27 월 가동 활성화의 prerequisite.
